import { describe, it } from "vitest";

describe("createScheduleApplication", () => {
  it.todo("allows a worker to apply once to a recruiting schedule");
  it.todo("rejects duplicate submissions cleanly");
  it.todo("denies anonymous and non-worker callers");
  it.todo("never accepts or persists a chosen role");
});
