import { describe, it } from "vitest";

describe("WorkerSchedulesPage", () => {
  it.todo("renders a lightweight recruiting schedule list for worker users");
  it.todo("shows applied state for schedules the current worker already submitted");
});
