---
phase: 04-attendance-check-in
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/queries/attendance/types/workerAttendanceStatus.ts
  - src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
  - src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts
  - src/mutations/attendance/components/AttendanceCheckInCard.tsx
  - src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
  - src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
autonomous: true
requirements:
  - ATTD-01
must_haves:
  truths:
    - "A worker sees check-in availability and submission state on the existing confirmed-assignment page, not on a separate attendance route."
    - "A worker can submit one check-in from the confirmed-assignment page once the backend contract reports the window is open."
    - "After a successful check-in, the page shows the recorded state and prevents resubmission."
  artifacts:
    - path: src/queries/attendance/types/workerAttendanceStatus.ts
      provides: "Worker attendance DTO keyed to confirmed assignments"
    - path: src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
      provides: "Worker check-in status read model keyed to confirmed assignments"
    - path: src/mutations/attendance/components/AttendanceCheckInCard.tsx
      provides: "Confirmed-assignment check-in CTA, helper copy, and one-shot submit UI"
    - path: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      provides: "Worker confirmed-assignment page extended with attendance status and CTA"
  key_links:
    - from: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      to: src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
      via: "server-first worker page composition joins confirmed assignments to attendance status"
      pattern: "listWorkerAttendanceStatuses"
    - from: src/mutations/attendance/components/AttendanceCheckInCard.tsx
      to: src/mutations/attendance/actions/submitAttendanceCheckIn.ts
      via: "client geolocation submit wrapper posts the one-shot check-in payload"
      pattern: "submitAttendanceCheckIn"
---

<objective>
Expose attendance availability and one-shot check-in on the existing worker confirmed-assignment flow using the backend contract from Plan 04-01.

Purpose: satisfy the worker experience portion of ATTD-01 without re-opening backend contract design.
Output: worker attendance read model, check-in card, confirmed-assignment page integration, and worker-facing tests.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-attendance-check-in/04-CONTEXT.md
@.planning/phases/04-attendance-check-in/04-RESEARCH.md
@.planning/phases/04-attendance-check-in/04-VALIDATION.md
@.planning/phases/04-attendance-check-in/04-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
@src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
@src/queries/assignment/types/workerAssignmentPreview.ts
@src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
@src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
@src/mutations/attendance/actions/createAttendanceCheckIn.ts

<interfaces>
From src/queries/assignment/types/workerAssignmentPreview.ts:
```ts
export interface WorkerAssignmentPreview extends PayPreviewBreakdown {
  assignmentId: string;
  scheduleId: string;
  scheduleRoleSlotId: string;
  roleCode: string;
  startsAt: string;
  endsAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the worker attendance read model on top of confirmed assignments</name>
  <files>
    src/queries/attendance/types/workerAttendanceStatus.ts
    src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
    src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts
  </files>
  <behavior>
    - Test 1: the worker attendance query starts from the worker's confirmed assignments and returns one attendance status per confirmed assignment even when no attendance row exists.
    - Test 2: the read model derives `checkInOpensAt`, `windowStatus`, and `submissionStatus` using the backend timing rules from Plan 04-01.
    - Test 3: already-submitted assignments return stable post-check-in state without requiring UI-side attendance math.
  </behavior>
  <action>Create an attendance-focused worker read model in `src/queries/attendance/dal/listWorkerAttendanceStatuses.ts` that starts from the worker's confirmed assignments, left-joins `attendance_check_ins`, derives `checkInOpensAt`, `windowStatus`, and `submissionStatus`, and returns data keyed by `assignmentId` or `scheduleAssignmentId` for page composition. Keep attendance reads in the attendance query slice rather than moving them into the assignment slice.</action>
  <verify>
    <automated>pnpm exec vitest src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts -x</automated>
  </verify>
  <done>The worker page has a dedicated attendance read contract built on top of confirmed assignments, with timing and submission states already derived.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the confirmed-assignment page with geolocation submit UI</name>
  <files>
    src/mutations/attendance/components/AttendanceCheckInCard.tsx
    src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
    src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
  </files>
  <action>Extend `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx` so it keeps the existing confirmed-assignment route and pay-preview sections, but adds an `AttendanceCheckInCard.tsx` for each confirmed assignment. The card must follow the approved worker priority order from `04-UI-SPEC.md`: status or availability headline first, `Check in now` CTA second, helper or location detail third. Use browser geolocation only inside the client card, show blocked states for denied permission, insecure context, timeout, and already-submitted cases, and submit through `submitAttendanceCheckIn.ts`. Do not create a new `/worker/attendance` route, and do not move worker attendance reads into the assignment slice.</action>
  <verify>
    <automated>pnpm exec vitest src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx -x</automated>
  </verify>
  <done>Workers can see why check-in is blocked or open, submit attendance once from the confirmed-assignment page, and see the recorded state after success.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx -x`.
Confirm the worker surface remains `/worker/assignments`, geolocation stays inside the client card, and the UI reflects the backend contract from Plan 04-01 instead of duplicating validation logic.
</verification>

<success_criteria>
The existing worker confirmed-assignment page becomes the only worker attendance entry point, backed by the backend contract from Plan 04-01 and without adding a separate attendance route.
</success_criteria>

<output>
After completion, create `.planning/phases/04-attendance-check-in/04-attendance-check-in-02-SUMMARY.md`
</output>
