---
phase: 04-attendance-check-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260403_phase4_attendance_check_in.sql
  - src/shared/config/attendance.ts
  - src/shared/config/cacheTags.ts
  - src/mutations/attendance/schemas/attendanceCheckIn.ts
  - src/mutations/attendance/schemas/attendanceCheckIn.test.ts
  - src/mutations/attendance/utils/calculateAttendanceWindow.ts
  - src/mutations/attendance/utils/calculateDistanceMeters.ts
  - src/mutations/attendance/dal/attendanceDal.ts
  - src/mutations/attendance/dal/attendanceDal.test.ts
  - src/mutations/attendance/actions/createAttendanceCheckIn.ts
  - src/mutations/attendance/actions/createAttendanceCheckIn.test.ts
  - src/mutations/attendance/actions/submitAttendanceCheckIn.ts
autonomous: true
requirements:
  - ATTD-01
  - ATTD-02
must_haves:
  truths:
    - "The attendance backend accepts a worker check-in only for that worker's confirmed assignment and only once per assignment."
    - "The server enforces the first-ceremony open-window rule and single-venue radius rule before any attendance row is inserted."
    - "The attendance write path stores the submitted location snapshot and derived validation fields needed for later worker and admin reads."
  artifacts:
    - path: supabase/migrations/20260403_phase4_attendance_check_in.sql
      provides: "Attendance persistence, uniqueness, indexes, and RLS for one-shot worker check-ins"
    - path: src/shared/config/attendance.ts
      provides: "Single-venue latitude, longitude, and radius configuration for Phase 4"
    - path: src/mutations/attendance/dal/attendanceDal.ts
      provides: "Database insert/read helpers enforcing confirmed-assignment ownership and duplicate handling"
    - path: src/mutations/attendance/actions/createAttendanceCheckIn.ts
      provides: "Server-side attendance validation and write orchestration"
    - path: src/mutations/attendance/actions/submitAttendanceCheckIn.ts
      provides: "Worker-authenticated submit entry point with cache invalidation"
  key_links:
    - from: src/mutations/attendance/actions/createAttendanceCheckIn.ts
      to: supabase/migrations/20260403_phase4_attendance_check_in.sql
      via: "server validation writes one attendance row per confirmed schedule assignment"
      pattern: "attendance_check_ins|schedule_assignment_id"
    - from: src/mutations/attendance/actions/createAttendanceCheckIn.ts
      to: src/shared/config/attendance.ts
      via: "server-side geofence validation uses the single venue config"
      pattern: "ATTENDANCE_VENUE_LATITUDE|ATTENDANCE_VENUE_LONGITUDE|ATTENDANCE_ALLOWED_RADIUS_METERS"
    - from: src/mutations/attendance/actions/submitAttendanceCheckIn.ts
      to: src/shared/config/cacheTags.ts
      via: "successful submits invalidate attendance and worker-confirmed assignment caches"
      pattern: "cacheTags\\.attendance|cacheTags\\.assignments\\.workerConfirmed"
---

<objective>
Lock the attendance backend contract before any worker or admin UI integration depends on it.

Purpose: satisfy the backend portion of ATTD-01 and ATTD-02 by isolating schema, config, validation logic, and authenticated submit orchestration into one wave-1 plan.
Output: attendance migration, env-backed venue config, attendance mutation slice, cache-tag additions, and backend-focused tests.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-attendance-check-in/04-CONTEXT.md
@.planning/phases/04-attendance-check-in/04-RESEARCH.md
@.planning/phases/04-attendance-check-in/04-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
@supabase/migrations/20260401_phase2_schedule_publishing.sql
@supabase/migrations/20260402_phase3_assignment_and_pay_preview.sql
@src/shared/config/cacheTags.ts

<interfaces>
From src/shared/config/cacheTags.ts:
```ts
export const cacheTags = {
  assignments: {
    all: "assignments",
    detail: (scheduleId: string) => `assignments:detail:${scheduleId}`,
    workerConfirmed: (workerUserId: string) => `assignments:worker-confirmed:${workerUserId}`,
  },
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the attendance persistence contract and validation primitives</name>
  <files>
    supabase/migrations/20260403_phase4_attendance_check_in.sql
    src/shared/config/attendance.ts
    src/shared/config/cacheTags.ts
    src/mutations/attendance/schemas/attendanceCheckIn.ts
    src/mutations/attendance/schemas/attendanceCheckIn.test.ts
    src/mutations/attendance/utils/calculateAttendanceWindow.ts
    src/mutations/attendance/utils/calculateDistanceMeters.ts
    src/mutations/attendance/dal/attendanceDal.ts
    src/mutations/attendance/dal/attendanceDal.test.ts
  </files>
  <behavior>
    - Test 1: the Phase 4 attendance window uses `schedules.starts_at` as the canonical first-ceremony timestamp for D-01 through D-03a, opening at `08:20` for `10:00` starts and `1 hour 50 minutes` before starts at `11:00` or later.
    - Test 2: a worker can create one attendance row for an owned `schedule_assignments.status = 'confirmed'` record, and a second submit returns a stable duplicate outcome instead of a second row.
    - Test 3: the DAL persists submitted latitude, longitude, optional accuracy, computed distance, allowed radius snapshot, `within_allowed_radius`, and `is_late`.
    - Test 4: unauthorized, before-window, and out-of-radius submissions are rejected before insert.
  </behavior>
  <action>Create `supabase/migrations/20260403_phase4_attendance_check_in.sql` to add `attendance_check_ins` keyed by `schedule_assignment_id` with a unique constraint, worker/admin read indexes, and worker-owned RLS insert/select rules that only allow the authenticated worker to write against their own confirmed assignment per D-05 and D-06. Use the locked Phase 4 v1 rule that the first ceremony time is `schedules.starts_at`; do not add a second schedule clock field in this phase. Put the single-venue geofence in `src/shared/config/attendance.ts` backed by `ATTENDANCE_VENUE_LATITUDE`, `ATTENDANCE_VENUE_LONGITUDE`, and `ATTENDANCE_ALLOWED_RADIUS_METERS`, then snapshot the effective allowed radius into each attendance row per D-04. Keep parsing in `schemas/`, pure time and distance logic in `utils/`, and database writes in `src/mutations/attendance/dal/attendanceDal.ts`.</action>
  <verify>
    <automated>pnpm exec vitest src/mutations/attendance/dal/attendanceDal.test.ts src/mutations/attendance/schemas/attendanceCheckIn.test.ts -x</automated>
  </verify>
  <done>The attendance schema, config, parsing, distance math, timing math, and DAL contract exist and are tested for the locked timing/location rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authenticated attendance submission orchestration</name>
  <files>
    src/mutations/attendance/actions/createAttendanceCheckIn.ts
    src/mutations/attendance/actions/createAttendanceCheckIn.test.ts
    src/mutations/attendance/actions/submitAttendanceCheckIn.ts
    src/shared/config/cacheTags.ts
  </files>
  <behavior>
    - Test 1: the server action rejects unauthorized users and non-worker callers before touching the DAL.
    - Test 2: a valid worker submit returns a stable success result and invalidates worker-confirmed plus attendance cache tags.
    - Test 3: duplicate, too-early, and out-of-radius failures map to stable result codes instead of unhandled errors.
  </behavior>
  <action>Implement `createAttendanceCheckIn.ts` and `submitAttendanceCheckIn.ts` as orchestration-only actions that call the DAL, translate timing/location/duplicate failures into stable result codes, and invalidate attendance plus worker-confirmed cache tags. Keep this plan backend-only: do not add worker read models or UI wiring here. The goal is to provide a stable contract that downstream worker and admin plans can consume without reopening schema or mutation boundaries.</action>
  <verify>
    <automated>pnpm exec vitest src/mutations/attendance/actions/createAttendanceCheckIn.test.ts src/mutations/attendance/dal/attendanceDal.test.ts src/mutations/attendance/schemas/attendanceCheckIn.test.ts -x</automated>
  </verify>
  <done>The worker-authenticated submit contract is fully defined and tested, and downstream plans can consume stable action result codes and cache behavior.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest src/mutations/attendance/actions/createAttendanceCheckIn.test.ts src/mutations/attendance/dal/attendanceDal.test.ts src/mutations/attendance/schemas/attendanceCheckIn.test.ts -x`.
Confirm the backend enforces the first-ceremony timing rules, one-shot uniqueness, and single-venue radius before any UI integration begins.
</verification>

<success_criteria>
Phase 4 has a stable backend contract: one confirmed assignment can produce at most one server-validated attendance row, and the mutation layer exposes consistent outcomes for worker and admin consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-attendance-check-in/04-attendance-check-in-01-SUMMARY.md`
</output>
