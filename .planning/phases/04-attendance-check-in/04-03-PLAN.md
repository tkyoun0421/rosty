---
phase: 04-attendance-check-in
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/queries/attendance/types/adminScheduleAttendanceDetail.ts
  - src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
  - src/queries/attendance/dal/getAdminScheduleAttendanceDetail.test.ts
  - src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx
  - src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
  - src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
autonomous: true
requirements:
  - ATTD-03
must_haves:
  truths:
    - "An admin can review attendance inside the existing schedule detail flow instead of going to a separate dashboard."
    - "The schedule detail shows each confirmed worker as checked in, late, not checked in, or not open yet."
    - "The attendance review shows schedule-level counts plus per-worker status details."
  artifacts:
    - path: src/queries/attendance/types/adminScheduleAttendanceDetail.ts
      provides: "Schedule-centric attendance DTO for admin review"
    - path: src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
      provides: "Confirmed-assignment plus attendance read model for one schedule"
    - path: src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx
      provides: "Admin schedule detail attendance summary and worker status panel"
    - path: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
      provides: "Existing schedule detail flow extended with attendance review"
  key_links:
    - from: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
      to: src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
      via: "existing schedule detail flow loads attendance review alongside assignment detail"
      pattern: "getAdminScheduleAttendanceDetail"
    - from: src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
      to: supabase/migrations/20260403_phase4_attendance_check_in.sql
      via: "schedule-centric admin query left-joins confirmed assignments to attendance rows"
      pattern: "attendance_check_ins|schedule_assignments"
    - from: src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx
      to: src/queries/attendance/types/adminScheduleAttendanceDetail.ts
      via: "render-ready admin DTO drives summary counts and worker status labels"
      pattern: "AdminScheduleAttendanceDetail"
---

<objective>
Extend the existing admin schedule detail flow with attendance and lateness review.

Purpose: satisfy ATTD-03 with a schedule-centric admin read model and a schedule-detail panel that shows worker attendance outcomes without creating a new dashboard, after the backend contract lands in Plan 04-01.
Output: admin attendance DTO, admin query, attendance review panel, and updated schedule-detail tests.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-attendance-check-in/04-CONTEXT.md
@.planning/phases/04-attendance-check-in/04-RESEARCH.md
@.planning/phases/04-attendance-check-in/04-VALIDATION.md
@.planning/phases/04-attendance-check-in/04-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
@src/queries/assignment/types/adminScheduleAssignmentDetail.ts
@src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
@src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
@src/mutations/attendance/actions/createAttendanceCheckIn.ts

<interfaces>
From src/queries/assignment/types/adminScheduleAssignmentDetail.ts:
```ts
export interface AdminScheduleAssignmentDetail {
  schedule: {
    id: string;
    startsAt: string;
    endsAt: string;
    status: ScheduleStatus;
  };
  roleSlots: Array<{
    id: string;
    roleCode: string;
    headcount: number;
    assignedCount: number;
  }>;
  applicants: Array<{
    workerUserId: string;
    workerName: string | null;
    appliedAt: string;
    assignmentStatus: AdminApplicantAssignmentStatus;
    assignedRoleSlotId: string | null;
    assignedRoleCode: string | null;
  }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the schedule-centric admin attendance read model</name>
  <read_first>
    .planning/phases/04-attendance-check-in/04-CONTEXT.md
    .planning/phases/04-attendance-check-in/04-RESEARCH.md
    .planning/phases/04-attendance-check-in/04-VALIDATION.md
    src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
    src/queries/assignment/types/adminScheduleAssignmentDetail.ts
    src/mutations/attendance/actions/createAttendanceCheckIn.ts
  </read_first>
  <files>
    src/queries/attendance/types/adminScheduleAttendanceDetail.ts
    src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
    src/queries/attendance/dal/getAdminScheduleAttendanceDetail.test.ts
  </files>
  <behavior>
    - Test 1: the admin attendance query starts from confirmed assignments for one schedule and returns one row per confirmed worker even when no attendance row exists.
    - Test 2: rows map to stable labels for `checked_in`, `late`, `not_checked_in`, and `not_open_yet`.
    - Test 3: lateness uses the same canonical schedule clock as Plan 04-01, meaning `schedules.starts_at` is both the first-ceremony source and the late cutoff for this phase.
    - Test 4: the DTO exposes schedule-level summary counts plus per-worker metadata needed for the review table or list.
  </behavior>
  <action>Create `src/queries/attendance/types/adminScheduleAttendanceDetail.ts` and `src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts` as an attendance-focused admin slice that begins with confirmed `schedule_assignments`, left-joins `attendance_check_ins`, joins `profiles` and `schedule_role_slots`, and maps render-ready status data for D-07 and D-08. Reuse the locked Phase 4 timing choice from Plan 04-01 and `04-CONTEXT.md`: `schedules.starts_at` is the canonical first-ceremony timestamp for v1 and also the lateness cutoff for this phase. The query must remain schedule-centric, return workers with no attendance row as explicit missing states, and avoid moving attendance math into the React layer.</action>
  <acceptance_criteria>The admin attendance query returns schedule-level counts and per-worker statuses from the confirmed-assignment context, with lateness and not-yet-open states already derived before rendering.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest src/queries/attendance/dal/getAdminScheduleAttendanceDetail.test.ts -x</automated>
  </verify>
  <done>The schedule-centric read model for ATTD-03 exists and is tested independently of the UI, ready to consume the backend contract from Plan 04-01.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the existing admin schedule detail page with attendance review</name>
  <read_first>
    .planning/phases/04-attendance-check-in/04-UI-SPEC.md
    src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
  </read_first>
  <files>
    src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
  </files>
  <action>Create `src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx` and wire it into `AdminScheduleAssignmentPage.tsx` so the existing schedule detail page loads both assignment detail and attendance detail in the same flow. Preserve the current route and assignment review surface, but extend it with an attendance-first summary block and worker status list that follow `04-UI-SPEC.md`: headline metric or summary first, worker attendance list/table second, metadata after that. Render short labels exactly as approved: `Checked in`, `Late`, `Not checked in`, and `Not open yet`. Do not add a new admin attendance route or a dashboard view; the only admin attendance UX in this phase is the schedule detail extension.</action>
  <acceptance_criteria>The existing `/admin/schedules/[scheduleId]` flow shows attendance and lateness review in-place, with no separate route and no worker-centric dashboard introduced.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest src/queries/attendance/dal/getAdminScheduleAttendanceDetail.test.ts src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx -x</automated>
  </verify>
  <done>Admins can review attendance state and lateness from the existing schedule detail flow using a schedule-centric read model.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest src/queries/attendance/dal/getAdminScheduleAttendanceDetail.test.ts src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx -x`.
Confirm the only admin attendance surface is `/admin/schedules/[scheduleId]`, and the page shows schedule-level summary plus per-worker attendance/lateness states.
</verification>

<success_criteria>
Admins can stay inside the existing schedule detail flow and review attendance plus lateness for all confirmed workers on a schedule without relying on a separate dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/04-attendance-check-in/04-attendance-check-in-03-SUMMARY.md`
</output>
