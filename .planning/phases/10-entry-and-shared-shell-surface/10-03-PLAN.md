---
phase: 10-entry-and-shared-shell-surface
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/page.tsx
  - src/app/admin/operations/page.tsx
  - src/flows/auth-shell/components/RootRedirectPage.tsx
  - src/flows/auth-shell/components/RootRedirectPage.test.tsx
  - src/flows/worker-shell/components/WorkerShellPage.tsx
  - src/flows/worker-shell/components/WorkerShellPage.test.tsx
  - src/flows/admin-shell/components/AdminShellPage.tsx
  - src/flows/admin-shell/components/AdminShellPage.test.tsx
autonomous: true
requirements:
  - ENTRY-03
  - ENTRY-04
must_haves:
  truths:
    - "Signed-in users can understand their next step from `/`, `/worker`, and `/admin` without reading developer-facing scaffolds."
    - "Worker and admin landing pages use a consistent shell language with role-appropriate cards and links."
    - "The admin route has an intentional landing experience instead of bypassing the shell and leaving `AdminShellPage` as dead placeholder code."
  artifacts:
    - path: src/flows/auth-shell/components/RootRedirectPage.tsx
      provides: "The common home that routes signed-in users toward the correct role-specific surface."
    - path: src/flows/worker-shell/components/WorkerShellPage.tsx
      provides: "The worker home shell for schedule and confirmed-work entry."
    - path: src/flows/admin-shell/components/AdminShellPage.tsx
      provides: "The admin landing shell for schedules, operations, invites, and worker-rate work."
    - path: src/app/admin/operations/page.tsx
      provides: "A thin route that preserves direct access to the existing operations dashboard after `/admin` becomes a shell."
  key_links:
    - from: src/app/admin/page.tsx
      to: src/flows/admin-shell/components/AdminShellPage.tsx
      via: "the live `/admin` route now renders the admin shell instead of bypassing it"
      pattern: "AdminShellPage"
    - from: src/app/admin/operations/page.tsx
      to: src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage
      via: "the operations dashboard remains accessible through a thin child route"
      pattern: "AdminOperationsDashboardPage"
    - from: src/flows/worker-shell/components/WorkerShellPage.test.tsx
      to: src/flows/worker-shell/components/WorkerShellPage.tsx
      via: "the worker shell test proves the new heading and role-scoped primary links"
      pattern: "Worker home|Open schedules|Confirmed work"
    - from: src/flows/admin-shell/components/AdminShellPage.test.tsx
      to: src/flows/admin-shell/components/AdminShellPage.tsx
      via: "the admin shell test proves the new cards and links for admin next steps"
      pattern: "Admin workspace|Open schedules|Open operations dashboard"
---

<objective>
Align the role-aware home surfaces so the product has a coherent landing experience after sign-in and onboarding.

Purpose: complete the shell portion of Phase 10 by making `/`, `/worker`, and `/admin` intentional landing pages with obvious next destinations.
Output: updated common home, rebuilt worker and admin shells, and a thin admin operations route that preserves dashboard access.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-entry-and-shared-shell-surface/10-RESEARCH.md
@.planning/phases/10-entry-and-shared-shell-surface/10-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/app/admin/page.tsx
@src/flows/auth-shell/components/RootRedirectPage.tsx
@src/flows/auth-shell/components/RootRedirectPage.test.tsx
@src/flows/worker-shell/components/WorkerShellPage.tsx
@src/flows/worker-shell/components/WorkerShellPage.test.tsx
@src/flows/admin-shell/components/AdminShellPage.tsx
@src/flows/admin-shell/components/AdminShellPage.test.tsx
@src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
@src/queries/access/dal/getCurrentUser.ts
@src/queries/access/dal/getCurrentUserProfile.ts
@src/shared/ui/card.tsx
@src/shared/ui/badge.tsx
@vitest.config.ts

<interfaces>
From src/app/admin/page.tsx:
```ts
import { AdminOperationsDashboardPage } from "#flows/admin-operations-dashboard/components/AdminOperationsDashboardPage";

export default async function AdminPage() {
  return await AdminOperationsDashboardPage();
}
```

From src/flows/worker-shell/components/WorkerShellPage.tsx:
```tsx
<nav aria-label="Worker navigation">
  <ul>
    <li><Link href="/worker/schedules">Open schedules</Link></li>
    <li><Link href="/worker/assignments">Confirmed work</Link></li>
  </ul>
</nav>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the common home and worker shell into readable role-aware landing surfaces</name>
  <read_first>
    .planning/phases/10-entry-and-shared-shell-surface/10-RESEARCH.md
    .planning/phases/10-entry-and-shared-shell-surface/10-VALIDATION.md
    src/flows/auth-shell/components/RootRedirectPage.tsx
    src/flows/auth-shell/components/RootRedirectPage.test.tsx
    src/flows/worker-shell/components/WorkerShellPage.tsx
    src/flows/worker-shell/components/WorkerShellPage.test.tsx
    src/queries/access/dal/getCurrentUser.ts
    src/queries/access/dal/getCurrentUserProfile.ts
    src/shared/ui/card.tsx
    src/shared/ui/badge.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/auth-shell/components/RootRedirectPage.tsx
    src/flows/auth-shell/components/RootRedirectPage.test.tsx
    src/flows/worker-shell/components/WorkerShellPage.tsx
    src/flows/worker-shell/components/WorkerShellPage.test.tsx
  </files>
  <behavior>
    - The root common home must stay the sign-in/profile/role gatekeeper.
    - The worker shell must clearly present worker-specific next actions using the same visual language as the root home.
    - Existing role guards and route links must remain intact.
  </behavior>
  <action>Refine `src/flows/auth-shell/components/RootRedirectPage.tsx` so the common home includes more explicit role-aware copy such as a personalized welcome, a clearer section label for worker versus admin next steps, and concise card descriptions that point at scheduling, confirmed work, or admin workspace. Keep the existing redirect rules for anonymous, incomplete-profile, and roleless users unchanged. Rewrite `src/flows/worker-shell/components/WorkerShellPage.tsx` so it mirrors the root-home card style: add a badge like `Worker workspace`, a heading, short explanatory copy, and two or more cards linking to `/worker/schedules` and `/worker/assignments`. Expand `RootRedirectPage.test.tsx` and `WorkerShellPage.test.tsx` so they still assert redirects and role gating, but now also assert the upgraded headings, badge text, and primary link names.</action>
  <acceptance_criteria>
    - `src/flows/auth-shell/components/RootRedirectPage.tsx` still contains redirects to `/sign-in`, `/onboarding`, and `/unauthorized`.
    - `src/flows/auth-shell/components/RootRedirectPage.tsx` contains readable role-aware copy for worker and admin paths.
    - `src/flows/worker-shell/components/WorkerShellPage.tsx` contains `Worker workspace`.
    - `src/flows/worker-shell/components/WorkerShellPage.tsx` still links to `/worker/schedules` and `/worker/assignments`.
    - `src/flows/worker-shell/components/WorkerShellPage.test.tsx` asserts the new heading or badge text.
    - `src/flows/auth-shell/components/RootRedirectPage.test.tsx` still asserts the existing redirect behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/auth-shell/components/RootRedirectPage.test.tsx src/flows/worker-shell/components/WorkerShellPage.test.tsx</automated>
  </verify>
  <done>The root and worker landing flows now feel like a coherent role-aware product shell instead of routing scaffolds.</done>
</task>

<task type="auto">
  <name>Task 2: Turn `/admin` into a real admin landing shell and preserve dashboard access through a thin child route</name>
  <read_first>
    .planning/phases/10-entry-and-shared-shell-surface/10-RESEARCH.md
    .planning/phases/10-entry-and-shared-shell-surface/10-VALIDATION.md
    src/app/admin/page.tsx
    src/flows/admin-shell/components/AdminShellPage.tsx
    src/flows/admin-shell/components/AdminShellPage.test.tsx
    src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
    src/queries/access/dal/getCurrentUser.ts
    src/shared/ui/card.tsx
    src/shared/ui/badge.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/app/admin/page.tsx
    src/app/admin/operations/page.tsx
    src/flows/admin-shell/components/AdminShellPage.tsx
    src/flows/admin-shell/components/AdminShellPage.test.tsx
  </files>
  <behavior>
    - `/admin` must become a readable admin home rather than a direct dashboard bypass.
    - The admin shell must keep the admin-only guard and offer clear links into schedules, operations, invites, and worker-rate management.
    - The existing operations dashboard must remain reachable through a thin route file.
  </behavior>
  <action>Rewrite `src/flows/admin-shell/components/AdminShellPage.tsx` to use the same card language as the root and worker shells: add a badge like `Admin workspace`, a heading, a short description, and cards linking to `/admin/schedules`, `/admin/operations`, `/admin/invites`, and `/admin/worker-rates`. Keep the current admin-only guard by returning a readable access-required card or message when the user is missing or not an admin. Change `src/app/admin/page.tsx` so the live `/admin` route returns `AdminShellPage()` instead of `AdminOperationsDashboardPage()`. Add `src/app/admin/operations/page.tsx` as a thin route that returns `AdminOperationsDashboardPage()` so the existing dashboard remains accessible without duplicating logic. Expand `src/flows/admin-shell/components/AdminShellPage.test.tsx` to assert the admin heading plus the four destination links, and keep the non-admin denial assertion explicit.</action>
  <acceptance_criteria>
    - `src/app/admin/page.tsx` imports `AdminShellPage`.
    - `src/app/admin/page.tsx` does not import `AdminOperationsDashboardPage`.
    - `src/app/admin/operations/page.tsx` exists.
    - `src/app/admin/operations/page.tsx` imports `AdminOperationsDashboardPage`.
    - `src/flows/admin-shell/components/AdminShellPage.tsx` contains links to `/admin/schedules`, `/admin/operations`, `/admin/invites`, and `/admin/worker-rates`.
    - `src/flows/admin-shell/components/AdminShellPage.test.tsx` asserts the admin heading and destination links.
    - `src/flows/admin-shell/components/AdminShellPage.test.tsx` still covers non-admin denial.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/admin-shell/components/AdminShellPage.test.tsx</automated>
  </verify>
  <done>The admin landing flow now has an intentional home route and the existing operations dashboard remains available through a thin child route.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/flows/auth-shell/components/RootRedirectPage.test.tsx src/flows/worker-shell/components/WorkerShellPage.test.tsx src/flows/admin-shell/components/AdminShellPage.test.tsx` and confirm the common home, worker shell, and admin shell all render the new role-aware headings and links while preserving the current guards.
</verification>

<success_criteria>
Signed-in users can understand where to go next from the common home, worker route, and admin route without relying on implementation scaffolds or dead placeholder pages.
</success_criteria>

<output>
After completion, create `.planning/phases/10-entry-and-shared-shell-surface/10-entry-and-shared-shell-surface-03-SUMMARY.md`
</output>
