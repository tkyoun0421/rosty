---
phase: 10-entry-and-shared-shell-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx
  - src/flows/profile-onboarding/components/ProfileOnboardingPage.test.tsx
  - src/mutations/auth/components/ProfileOnboardingForm.tsx
  - src/app/unauthorized/page.tsx
  - src/app/unauthorized/page.test.tsx
  - src/app/loading.tsx
  - src/app/error.tsx
  - src/app/onboarding/loading.tsx
  - src/app/worker/loading.tsx
  - src/app/admin/loading.tsx
autonomous: true
requirements:
  - ENTRY-04
must_haves:
  truths:
    - "Primary entry routes no longer fail into blank or placeholder markup during onboarding, authorization denial, loading, or recoverable errors."
    - "The onboarding page and form use the same product surface language as sign-in and invite instead of raw labels and mojibake copy."
    - "Unauthorized and failure surfaces give the user a concrete recovery path instead of a dead-end heading."
  artifacts:
    - path: src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx
      provides: "A readable onboarding shell that preserves the current redirect rules."
    - path: src/mutations/auth/components/ProfileOnboardingForm.tsx
      provides: "The polished onboarding form and inline error feedback."
    - path: src/app/unauthorized/page.tsx
      provides: "A readable unauthorized recovery surface."
    - path: src/app/loading.tsx
      provides: "A top-level loading surface for entry routes."
    - path: src/app/error.tsx
      provides: "A top-level recoverable error surface for entry routes."
  key_links:
    - from: src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx
      to: src/flows/auth-shell/components/EntrySurfaceFrame.tsx
      via: "the onboarding route reuses the shared entry surface layout from Plan 01"
      pattern: "EntrySurfaceFrame"
    - from: src/mutations/auth/components/ProfileOnboardingForm.tsx
      to: src/mutations/auth/actions/submitProfileOnboarding
      via: "the polished form preserves the existing onboarding submit action while improving surface and feedback"
      pattern: "submitProfileOnboarding"
    - from: src/app/unauthorized/page.test.tsx
      to: src/app/unauthorized/page.tsx
      via: "the route test proves the new unauthorized heading, supporting text, and recovery CTA"
      pattern: "access|home|sign in"
---

<objective>
Finish the states around the entry flow so onboarding, unauthorized access, loading, and recoverable failures feel like part of the product.

Purpose: extend the shared entry-surface language from Plan 01 into onboarding and route-state handling, closing the biggest `ENTRY-04` gaps.
Output: polished onboarding page and form, readable unauthorized page, and route-level loading/error surfaces for the primary entry routes.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-entry-and-shared-shell-surface/10-RESEARCH.md
@.planning/phases/10-entry-and-shared-shell-surface/10-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/flows/auth-shell/components/EntrySurfaceFrame.tsx
@src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx
@src/mutations/auth/components/ProfileOnboardingForm.tsx
@src/mutations/auth/actions/submitProfileOnboarding.ts
@src/app/unauthorized/page.tsx
@src/shared/config/authConfig.ts
@src/shared/ui/alert.tsx
@src/shared/ui/button.tsx
@src/shared/ui/card.tsx
@vitest.config.ts

<interfaces>
From src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx:
```ts
if (!profile) {
  redirect(SIGN_IN_PATH);
}

if (profile.isProfileComplete) {
  redirect(ROOT_PATH);
}
```

From src/mutations/auth/components/ProfileOnboardingForm.tsx:
```ts
await submitProfileOnboarding(formData);
router.replace(ROOT_PATH);
router.refresh();
```

From src/shared/config/authConfig.ts:
```ts
export const ROOT_PATH = "/";
export const SIGN_IN_PATH = "/sign-in";
export const UNAUTHORIZED_PATH = "/unauthorized";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild onboarding around the shared entry surface and readable feedback</name>
  <read_first>
    .planning/phases/10-entry-and-shared-shell-surface/10-RESEARCH.md
    .planning/phases/10-entry-and-shared-shell-surface/10-VALIDATION.md
    src/flows/auth-shell/components/EntrySurfaceFrame.tsx
    src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx
    src/mutations/auth/components/ProfileOnboardingForm.tsx
    src/mutations/auth/actions/submitProfileOnboarding.ts
    src/shared/config/authConfig.ts
    src/shared/ui/alert.tsx
    src/shared/ui/button.tsx
    src/shared/ui/card.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx
    src/flows/profile-onboarding/components/ProfileOnboardingPage.test.tsx
    src/mutations/auth/components/ProfileOnboardingForm.tsx
  </files>
  <behavior>
    - Onboarding must keep the existing redirect rules for anonymous and already-complete profiles.
    - The page must present readable onboarding copy, a clear submit CTA, and inline error feedback.
    - The form must keep avatar preview, required fields, and `UNAUTHORIZED` redirect behavior.
  </behavior>
  <action>Rewrite `src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx` to wrap the onboarding experience in `EntrySurfaceFrame` with a badge like `Profile setup`, a heading such as `Complete your profile`, and supporting copy that explains the user needs full name, gender, birth date, and profile image before entering the product. Rebuild `src/mutations/auth/components/ProfileOnboardingForm.tsx` so the form uses readable field labels, grouped spacing, the shared `Button` primitive for submit, and `Alert` for error presentation while preserving the existing `submitProfileOnboarding(formData)`, `router.replace(ROOT_PATH)`, `router.refresh()`, and `UNAUTHORIZED` redirect flow. Add `src/flows/profile-onboarding/components/ProfileOnboardingPage.test.tsx` that proves the anonymous redirect, complete-profile redirect, and rendered onboarding heading on the active path.</action>
  <acceptance_criteria>
    - `src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx` contains `Complete your profile`.
    - `src/flows/profile-onboarding/components/ProfileOnboardingPage.tsx` imports `EntrySurfaceFrame`.
    - `src/mutations/auth/components/ProfileOnboardingForm.tsx` imports `#shared/ui/button`.
    - `src/mutations/auth/components/ProfileOnboardingForm.tsx` imports `#shared/ui/alert`.
    - `src/mutations/auth/components/ProfileOnboardingForm.tsx` still contains `submitProfileOnboarding(formData)`.
    - `src/flows/profile-onboarding/components/ProfileOnboardingPage.test.tsx` exists.
    - `src/flows/profile-onboarding/components/ProfileOnboardingPage.test.tsx` asserts the onboarding heading and redirect behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/profile-onboarding/components/ProfileOnboardingPage.test.tsx</automated>
  </verify>
  <done>The onboarding flow now feels like part of the same product entry system while preserving the current data and redirect logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add readable unauthorized, loading, and recoverable error surfaces for entry routes</name>
  <read_first>
    .planning/phases/10-entry-and-shared-shell-surface/10-RESEARCH.md
    .planning/phases/10-entry-and-shared-shell-surface/10-VALIDATION.md
    src/flows/auth-shell/components/EntrySurfaceFrame.tsx
    src/app/unauthorized/page.tsx
    src/shared/config/authConfig.ts
    src/shared/ui/alert.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/app/unauthorized/page.tsx
    src/app/unauthorized/page.test.tsx
    src/app/loading.tsx
    src/app/error.tsx
    src/app/onboarding/loading.tsx
    src/app/worker/loading.tsx
    src/app/admin/loading.tsx
  </files>
  <behavior>
    - Unauthorized users must see a readable explanation and a clear next step.
    - Primary entry routes must expose intentional loading surfaces instead of blank waits.
    - Recoverable route errors must render a readable message and a retry path.
  </behavior>
  <action>Rewrite `src/app/unauthorized/page.tsx` to use `EntrySurfaceFrame` plus a destructive `Alert`, a heading such as `You do not have access to this area`, a short explanation about using an invited account, and one or two recovery links back to `/` and `/sign-in`. Add `src/app/unauthorized/page.test.tsx` that proves the heading, supporting text, and recovery links. Add `src/app/loading.tsx` as the default entry loading surface using the same card pattern with text like `Loading your workspace...`. Add route-specific `loading.tsx` files for `src/app/onboarding`, `src/app/worker`, and `src/app/admin` with role-appropriate copy. Add `src/app/error.tsx` as a client error boundary component that renders the shared entry pattern, a destructive alert, and a retry button using the provided `reset()` callback. Keep these surfaces copy-driven and route-thin; do not move auth or routing logic into them.</action>
  <acceptance_criteria>
    - `src/app/unauthorized/page.tsx` contains `You do not have access to this area`.
    - `src/app/unauthorized/page.tsx` contains links to `/` and `/sign-in`.
    - `src/app/unauthorized/page.test.tsx` exists.
    - `src/app/loading.tsx` exists.
    - `src/app/error.tsx` exists.
    - `src/app/error.tsx` exports a client component and calls `reset`.
    - `src/app/onboarding/loading.tsx`, `src/app/worker/loading.tsx`, and `src/app/admin/loading.tsx` exist.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/app/unauthorized/page.test.tsx</automated>
  </verify>
  <done>Unauthorized, loading, and recoverable error states are now explicit product surfaces instead of dead-end placeholders.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/flows/profile-onboarding/components/ProfileOnboardingPage.test.tsx src/app/unauthorized/page.test.tsx` and confirm onboarding, unauthorized, and route-state surfaces render the new headings, feedback, and recovery paths while preserving the current redirect rules.
</verification>

<success_criteria>
Phase 10 no longer depends on raw onboarding or state placeholders; the main entry routes now communicate loading, blocked access, and recoverable failure clearly.
</success_criteria>

<output>
After completion, create `.planning/phases/10-entry-and-shared-shell-surface/10-entry-and-shared-shell-surface-02-SUMMARY.md`
</output>
