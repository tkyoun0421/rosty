---
phase: 10-entry-and-shared-shell-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/flows/auth-shell/components/EntrySurfaceFrame.tsx
  - src/flows/auth-shell/components/SignInPage.tsx
  - src/flows/auth-shell/components/SignInPage.test.tsx
  - src/app/invite/[token]/page.tsx
  - src/app/invite/[token]/page.test.tsx
  - src/mutations/auth/components/GoogleSignInButton.tsx
  - src/mutations/auth/components/GoogleSignInButton.test.tsx
autonomous: true
requirements:
  - ENTRY-01
  - ENTRY-02
must_haves:
  truths:
    - "Signed-out users land on a readable sign-in surface with a clear Google primary action and supporting copy."
    - "Invited users land on a distinct invite-acceptance surface that clearly says they are finishing an invite flow, not generic sign-in."
    - "Sign-in and invite surfaces share the same visual hierarchy, spacing, and CTA treatment instead of diverging into one-off markup."
  artifacts:
    - path: src/flows/auth-shell/components/EntrySurfaceFrame.tsx
      provides: "A shared entry-surface wrapper for sign-in, invite, onboarding, unauthorized, and other single-column entry states."
    - path: src/flows/auth-shell/components/SignInPage.tsx
      provides: "The production sign-in surface for ENTRY-01."
    - path: src/app/invite/[token]/page.tsx
      provides: "The invite-specific entry surface for ENTRY-02."
    - path: src/mutations/auth/components/GoogleSignInButton.tsx
      provides: "A product-grade CTA that visually matches the shared entry surfaces."
  key_links:
    - from: src/flows/auth-shell/components/SignInPage.tsx
      to: src/mutations/auth/components/GoogleSignInButton.tsx
      via: "the sign-in surface renders the shared Google CTA with the production label text"
      pattern: "Continue with Google|Google"
    - from: src/app/invite/[token]/page.tsx
      to: src/mutations/auth/components/GoogleSignInButton.tsx
      via: "the invite route passes the invite token into the shared CTA while presenting invite-specific copy"
      pattern: "inviteToken|accept"
    - from: src/flows/auth-shell/components/SignInPage.test.tsx
      to: src/flows/auth-shell/components/SignInPage.tsx
      via: "the render test proves the sign-in heading, supporting copy, and primary CTA label"
      pattern: "Continue with Google|Sign in"
    - from: src/app/invite/[token]/page.test.tsx
      to: src/app/invite/[token]/page.tsx
      via: "the route test proves invite-specific copy and CTA text"
      pattern: "Invite|accept"
---

<objective>
Create the polished entry surfaces that users hit before onboarding or role-aware routing begins.

Purpose: replace the raw sign-in and invite pages with a shared entry pattern, readable product copy, and a clear Google CTA that still preserves the existing auth behavior.
Output: a reusable entry-surface wrapper, completed sign-in and invite pages, and regression tests for the new hierarchy and CTA labels.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-entry-and-shared-shell-surface/10-RESEARCH.md
@.planning/phases/10-entry-and-shared-shell-surface/10-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/flows/auth-shell/components/SignInPage.tsx
@src/app/invite/[token]/page.tsx
@src/mutations/auth/components/GoogleSignInButton.tsx
@src/mutations/auth/components/GoogleSignInButton.test.tsx
@src/flows/auth-shell/components/RootRedirectPage.tsx
@src/shared/ui/button.tsx
@src/shared/ui/card.tsx
@src/shared/ui/alert.tsx
@src/shared/ui/badge.tsx
@vitest.config.ts

<interfaces>
From src/mutations/auth/components/GoogleSignInButton.tsx:
```ts
startGoogleSignIn(window.location.origin, inviteToken);
```

From src/flows/auth-shell/components/RootRedirectPage.tsx:
```tsx
<main className="min-h-screen bg-background px-4 py-8 sm:px-6 lg:px-8">
  <div className="mx-auto grid max-w-5xl gap-8">
```

From src/shared/ui/button.tsx:
```ts
export const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared entry frame and rebuild the sign-in surface</name>
  <read_first>
    .planning/phases/10-entry-and-shared-shell-surface/10-RESEARCH.md
    .planning/phases/10-entry-and-shared-shell-surface/10-VALIDATION.md
    src/flows/auth-shell/components/SignInPage.tsx
    src/flows/auth-shell/components/RootRedirectPage.tsx
    src/mutations/auth/components/GoogleSignInButton.tsx
    src/shared/ui/button.tsx
    src/shared/ui/card.tsx
    src/shared/ui/alert.tsx
    src/shared/ui/badge.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/auth-shell/components/EntrySurfaceFrame.tsx
    src/flows/auth-shell/components/SignInPage.tsx
    src/flows/auth-shell/components/SignInPage.test.tsx
    src/mutations/auth/components/GoogleSignInButton.tsx
    src/mutations/auth/components/GoogleSignInButton.test.tsx
  </files>
  <behavior>
    - The sign-in page must render a badge, a readable heading, supporting copy, and one primary Google CTA.
    - The CTA must remain powered by `startGoogleSignIn(window.location.origin, inviteToken)` and stay disabled while pending.
    - The visual structure must follow the same card-and-spacing language already established in `RootRedirectPage.tsx`.
  </behavior>
  <action>Create `src/flows/auth-shell/components/EntrySurfaceFrame.tsx` as a reusable single-column layout component that renders a full-height `main`, a centered `Card`, an optional `Badge`, a heading, a description block, an optional `Alert`, and a body slot for CTA or form content. Rebuild `src/flows/auth-shell/components/SignInPage.tsx` to use that frame with badge text `Sign in`, heading `Continue with Google`, supporting copy that explains the invited account will sign in first and finish profile setup next, and a single `GoogleSignInButton` label `Continue with Google`. Update `src/mutations/auth/components/GoogleSignInButton.tsx` to use the existing `Button` primitive with a large primary style, preserve `startGoogleSignIn(window.location.origin, inviteToken)`, and show a pending label such as `Connecting to Google...` while the transition is running. Add `src/flows/auth-shell/components/SignInPage.test.tsx` to assert the rendered heading, description, and CTA label, and expand `src/mutations/auth/components/GoogleSignInButton.test.tsx` so it still proves the invite token is forwarded through the styled button.</action>
  <acceptance_criteria>
    - `src/flows/auth-shell/components/EntrySurfaceFrame.tsx` exists.
    - `src/flows/auth-shell/components/SignInPage.tsx` contains `Continue with Google`.
    - `src/flows/auth-shell/components/SignInPage.tsx` imports `EntrySurfaceFrame`.
    - `src/mutations/auth/components/GoogleSignInButton.tsx` imports `#shared/ui/button`.
    - `src/mutations/auth/components/GoogleSignInButton.tsx` contains `Connecting to Google`.
    - `src/flows/auth-shell/components/SignInPage.test.tsx` exists.
    - `src/flows/auth-shell/components/SignInPage.test.tsx` asserts the sign-in heading and CTA label.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/auth-shell/components/SignInPage.test.tsx src/mutations/auth/components/GoogleSignInButton.test.tsx</automated>
  </verify>
  <done>The sign-in route is now a readable product entry surface backed by a shared entry-frame component and a production-grade Google CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the invite acceptance route on the shared entry pattern</name>
  <read_first>
    .planning/phases/10-entry-and-shared-shell-surface/10-RESEARCH.md
    .planning/phases/10-entry-and-shared-shell-surface/10-VALIDATION.md
    src/app/invite/[token]/page.tsx
    src/flows/auth-shell/components/EntrySurfaceFrame.tsx
    src/mutations/auth/components/GoogleSignInButton.tsx
    src/mutations/auth/components/GoogleSignInButton.test.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/app/invite/[token]/page.tsx
    src/app/invite/[token]/page.test.tsx
  </files>
  <behavior>
    - The invite page must explicitly say the user is accepting an invite.
    - The CTA label must be invite-specific while still using the existing token-aware Google button.
    - The surface must explain what happens next after the user signs in.
  </behavior>
  <action>Rewrite `src/app/invite/[token]/page.tsx` to use `EntrySurfaceFrame` with a badge like `Invite`, a heading such as `Accept your invite`, and supporting copy that explains the user will sign in with Google and then finish profile setup to complete invite acceptance. Keep the route parameter contract `params: Promise<{ token: string }>` and pass the resolved token into `GoogleSignInButton`. Set the button label to `Accept invite with Google`. Add `src/app/invite/[token]/page.test.tsx` that renders the resolved route component with a real token value, asserts the invite-specific heading and supporting text, and asserts the rendered button name is `Accept invite with Google`.</action>
  <acceptance_criteria>
    - `src/app/invite/[token]/page.tsx` contains `Accept your invite`.
    - `src/app/invite/[token]/page.tsx` contains `Accept invite with Google`.
    - `src/app/invite/[token]/page.tsx` imports `EntrySurfaceFrame`.
    - `src/app/invite/[token]/page.tsx` still passes `inviteToken={token}` to `GoogleSignInButton`.
    - `src/app/invite/[token]/page.test.tsx` exists.
    - `src/app/invite/[token]/page.test.tsx` asserts the invite heading and CTA label.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/app/invite/[token]/page.test.tsx src/mutations/auth/components/GoogleSignInButton.test.tsx</automated>
  </verify>
  <done>The invite route now communicates invite acceptance clearly and keeps the existing token-aware auth behavior intact.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/flows/auth-shell/components/SignInPage.test.tsx src/app/invite/[token]/page.test.tsx src/mutations/auth/components/GoogleSignInButton.test.tsx` and confirm the completed entry surfaces render the new headings, descriptions, and CTA labels while preserving the Google sign-in action contract.
</verification>

<success_criteria>
Signed-out users and invited users both land on readable, intentional entry surfaces that clearly explain what the Google action will do next.
</success_criteria>

<output>
After completion, create `.planning/phases/10-entry-and-shared-shell-surface/10-entry-and-shared-shell-surface-01-SUMMARY.md`
</output>
