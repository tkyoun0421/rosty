---
phase: 12-worker-work-surface-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/queries/schedule/dal/listRecruitingSchedules.ts
  - src/queries/schedule/dal/listRecruitingSchedules.test.ts
  - src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
  - src/flows/worker-schedules/components/WorkerScheduleList.tsx
  - src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx
  - src/flows/worker-schedules/utils/formatSchedule.ts
  - src/mutations/application/components/ApplyToScheduleButton.tsx
  - src/mutations/application/actions/submitScheduleApplication.test.ts
autonomous: true
requirements:
  - WORKUI-01
  - WORKUI-03
must_haves:
  truths:
    - "Workers can scan recruiting schedules as readable cards that show schedule time, open roles, and current apply/applied state at a glance."
    - "The recruiting surface keeps using the existing worker apply mutation flow instead of introducing a new detail route or multi-step application workflow."
    - "Recruiting empty and waiting states tell workers what to do next instead of showing placeholder copy or raw blank lists."
  artifacts:
    - path: src/queries/schedule/dal/listRecruitingSchedules.ts
      provides: "A card-ready recruiting read model with role-slot summary for the worker recruiting surface."
    - path: src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
      provides: "The rebuilt server flow entry for `/worker/schedules`."
    - path: src/flows/worker-schedules/components/WorkerScheduleList.tsx
      provides: "Readable recruiting cards with open-role summary, apply state, and next-step guidance."
    - path: src/mutations/application/components/ApplyToScheduleButton.tsx
      provides: "Inline apply UI aligned with the rebuilt recruiting cards while preserving the submit wrapper."
  key_links:
    - from: src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
      to: src/queries/schedule/dal/listRecruitingSchedules.ts
      via: "the rebuilt worker recruiting page still performs the server-side recruiting read through the existing query slice"
      pattern: "listRecruitingSchedules"
    - from: src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
      to: src/queries/application/dal/listMyScheduleApplicationIds.ts
      via: "the page still merges current-worker applied state on the server before rendering"
      pattern: "listMyScheduleApplicationIds"
    - from: src/flows/worker-schedules/components/WorkerScheduleList.tsx
      to: src/mutations/application/components/ApplyToScheduleButton.tsx
      via: "each recruiting card still uses the dedicated apply control instead of inlining a new mutation path"
      pattern: "ApplyToScheduleButton"
    - from: src/mutations/application/components/ApplyToScheduleButton.tsx
      to: src/mutations/application/actions/submitScheduleApplication.ts
      via: "worker apply continues to post through the existing submit wrapper"
      pattern: "submitScheduleApplication"
---

<objective>
`/worker/schedules` 를 읽기 쉬운 모집 일정 surface 로 재구성해, worker 가 시간대, 모집 중 역할, 현재 지원 상태를 첫 화면에서 바로 이해할 수 있게 만든다.

Purpose: bare list 수준의 recruiting UI 를 product-grade card surface 로 교체하되, 기존 worker application write contract 는 유지한다.
Output: role-slot summary 가 포함된 recruiting read model, 카드형 모집 일정 화면, inline apply control, 그리고 회귀 테스트.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-worker-work-surface-completion/12-CONTEXT.md
@.planning/phases/12-worker-work-surface-completion/12-RESEARCH.md
@.planning/phases/12-worker-work-surface-completion/12-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@src/app/worker/schedules/page.tsx
@src/flows/worker-shell/components/WorkerShellPage.tsx
@src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
@src/flows/worker-schedules/components/WorkerScheduleList.tsx
@src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx
@src/flows/worker-schedules/utils/formatSchedule.ts
@src/queries/schedule/dal/listRecruitingSchedules.ts
@src/queries/schedule/dal/listRecruitingSchedules.test.ts
@src/queries/application/dal/listMyScheduleApplicationIds.ts
@src/mutations/application/components/ApplyToScheduleButton.tsx
@src/mutations/application/actions/submitScheduleApplication.ts
@src/mutations/application/actions/submitScheduleApplication.test.ts
@src/shared/ui/button.tsx
@src/shared/ui/card.tsx
@src/shared/ui/badge.tsx
@src/shared/ui/alert.tsx
@vitest.config.ts
</context>

<interfaces>
From src/queries/schedule/dal/listRecruitingSchedules.ts:
```ts
export interface RecruitingScheduleListItem {
  id: string;
  startsAt: string;
  endsAt: string;
  status: "recruiting";
}
```

From src/mutations/application/components/ApplyToScheduleButton.tsx:
```tsx
<form action={submitScheduleApplication}>
  <input type="hidden" name="scheduleId" value={scheduleId} />
</form>
```

From src/queries/application/dal/listMyScheduleApplicationIds.ts:
```ts
export async function listMyScheduleApplicationIds(workerUserId: string): Promise<string[]>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expand the recruiting read model for card-ready worker summaries</name>
  <read_first>
    .planning/phases/12-worker-work-surface-completion/12-CONTEXT.md
    .planning/phases/12-worker-work-surface-completion/12-RESEARCH.md
    .planning/phases/12-worker-work-surface-completion/12-VALIDATION.md
    src/queries/schedule/dal/listRecruitingSchedules.ts
    src/queries/schedule/dal/listRecruitingSchedules.test.ts
    src/queries/schedule/dal/listAdminSchedules.ts
    src/flows/worker-schedules/utils/formatSchedule.ts
    vitest.config.ts
  </read_first>
  <files>
    src/queries/schedule/dal/listRecruitingSchedules.ts
    src/queries/schedule/dal/listRecruitingSchedules.test.ts
    src/flows/worker-schedules/utils/formatSchedule.ts
  </files>
  <behavior>
    - The recruiting query must stay limited to schedules whose status is `recruiting`.
    - The worker-facing DTO must expose enough summary information for cards without requiring client-side guesses.
    - Formatting helpers must return readable schedule-window and role-summary strings for the rebuilt recruiting cards.
  </behavior>
  <action>Update `src/queries/schedule/dal/listRecruitingSchedules.ts` so the select clause becomes exactly `id, starts_at, ends_at, status, schedule_role_slots(role_code, headcount)` and the exported `RecruitingScheduleListItem` adds a `roleSlotSummary` field shaped as `Array<{ roleCode: string; headcount: number }>` mapped from `schedule_role_slots`. Keep the query ordered by `starts_at` ascending and keep the status filter `.eq("status", "recruiting")`. Update `src/flows/worker-schedules/utils/formatSchedule.ts` to export `formatScheduleWindow(startsAt, endsAt)` for a readable local date/time range and `formatRoleSlotSummary(roleSlotSummary)` for strings like `Captain x2, Server x4`. Expand `src/queries/schedule/dal/listRecruitingSchedules.test.ts` so it asserts the new select clause, the mapped `roleSlotSummary`, and the preserved recruiting-only ordering.</action>
  <acceptance_criteria>
    - `src/queries/schedule/dal/listRecruitingSchedules.ts` contains `schedule_role_slots(role_code, headcount)`.
    - `src/queries/schedule/dal/listRecruitingSchedules.ts` contains `roleSlotSummary`.
    - `src/queries/schedule/dal/listRecruitingSchedules.ts` still contains `.eq("status", "recruiting")`.
    - `src/flows/worker-schedules/utils/formatSchedule.ts` exports `formatScheduleWindow`.
    - `src/flows/worker-schedules/utils/formatSchedule.ts` exports `formatRoleSlotSummary`.
    - `src/queries/schedule/dal/listRecruitingSchedules.test.ts` asserts the new `roleSlotSummary` mapping.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/queries/schedule/dal/listRecruitingSchedules.test.ts</automated>
  </verify>
  <done>The worker recruiting query now exposes the exact summary information needed for a readable card surface.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the worker recruiting page and inline apply presentation</name>
  <read_first>
    .planning/phases/12-worker-work-surface-completion/12-CONTEXT.md
    .planning/phases/12-worker-work-surface-completion/12-RESEARCH.md
    .planning/phases/12-worker-work-surface-completion/12-VALIDATION.md
    src/flows/worker-shell/components/WorkerShellPage.tsx
    src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
    src/flows/worker-schedules/components/WorkerScheduleList.tsx
    src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx
    src/flows/worker-schedules/utils/formatSchedule.ts
    src/mutations/application/components/ApplyToScheduleButton.tsx
    src/mutations/application/actions/submitScheduleApplication.ts
    src/mutations/application/actions/submitScheduleApplication.test.ts
    src/shared/ui/button.tsx
    src/shared/ui/card.tsx
    src/shared/ui/badge.tsx
    src/shared/ui/alert.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
    src/flows/worker-schedules/components/WorkerScheduleList.tsx
    src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx
    src/mutations/application/components/ApplyToScheduleButton.tsx
    src/mutations/application/actions/submitScheduleApplication.test.ts
  </files>
  <behavior>
    - The page must keep the server-first merge of recruiting schedules and current-worker application IDs.
    - The recruiting surface must show schedule window, recruiting state, open roles, and apply/applied state without relying on raw list markup.
    - Empty states must explain the next step and link the worker toward confirmed work rather than dead-ending.
  </behavior>
  <action>Rewrite `src/flows/worker-schedules/components/WorkerSchedulesPage.tsx` into a full-height worker screen using the same family language as `WorkerShellPage.tsx`, with badge text `Worker recruiting`, heading `Recruiting schedules`, and supporting copy `Review upcoming openings, confirm whether you already applied, and check the roles still being staffed.`. Rewrite `src/flows/worker-schedules/components/WorkerScheduleList.tsx` so it renders each schedule as a `Card` that shows the formatted schedule window from `formatScheduleWindow`, a status badge labeled `Recruiting`, an `Open roles` summary using `formatRoleSlotSummary`, and the inline apply control. The empty-state card title must be exactly `No recruiting schedule is open right now`, the description must say `Check back later or open confirmed work to review assignments that are already finalized.`, and it must include a CTA link labeled `Review confirmed work` pointing to `/worker/assignments`. Update `src/mutations/application/components/ApplyToScheduleButton.tsx` so the existing `<form action={submitScheduleApplication}>` path stays intact, but the labels become exactly `Apply now` for open schedules and `Application submitted` for already-applied schedules using shared button styling. Expand `src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx` to assert the page heading, readable schedule window, `Open roles` summary, `Apply now` versus `Application submitted` state, and the recruiting empty-state CTA. Update `src/mutations/application/actions/submitScheduleApplication.test.ts` only if needed to keep the current invalidation contract covered after the UI refactor.</action>
  <acceptance_criteria>
    - `src/flows/worker-schedules/components/WorkerSchedulesPage.tsx` contains `Recruiting schedules`.
    - `src/flows/worker-schedules/components/WorkerScheduleList.tsx` contains `Open roles`.
    - `src/flows/worker-schedules/components/WorkerScheduleList.tsx` contains `No recruiting schedule is open right now`.
    - `src/flows/worker-schedules/components/WorkerScheduleList.tsx` contains `Review confirmed work`.
    - `src/mutations/application/components/ApplyToScheduleButton.tsx` still contains `submitScheduleApplication`.
    - `src/mutations/application/components/ApplyToScheduleButton.tsx` contains `Apply now`.
    - `src/mutations/application/components/ApplyToScheduleButton.tsx` contains `Application submitted`.
    - `src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx` asserts the page heading and recruiting empty-state CTA.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx src/mutations/application/actions/submitScheduleApplication.test.ts src/queries/schedule/dal/listRecruitingSchedules.test.ts</automated>
  </verify>
  <done>The worker recruiting route now reads as a real product surface while preserving the current worker apply data flow and invalidation rules.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/queries/schedule/dal/listRecruitingSchedules.test.ts src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx src/mutations/application/actions/submitScheduleApplication.test.ts` and confirm the rebuilt recruiting surface preserves the current server-side schedule/application merge plus the existing apply invalidation contract.
</verification>

<success_criteria>
Workers can understand recruiting schedules, open roles, and their current application state from `/worker/schedules` without raw list defaults or placeholder text.
</success_criteria>

<output>
After completion, create `.planning/phases/12-worker-work-surface-completion/12-worker-work-surface-completion-01-SUMMARY.md`
</output>
