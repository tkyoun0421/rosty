---
phase: 12-worker-work-surface-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/queries/assignment/types/workerAssignmentPreview.ts
  - src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
  - src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
  - src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
  - src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts
  - src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
  - src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
  - src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx
  - src/flows/worker-assignment-preview/utils/workerAssignmentPreview.ts
autonomous: true
requirements:
  - WORKUI-02
  - WORKUI-03
must_haves:
  truths:
    - "Workers can understand confirmed shifts, expected pay readiness, and attendance state from one coherent confirmed-work screen."
    - "Confirmed assignments remain visible even when worker rate data is missing, and the page explains that pay details are not ready instead of pretending no work exists."
    - "Attendance stays attached to each confirmed assignment and continues to rely on the existing attendance submit and status contracts."
  artifacts:
    - path: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      provides: "A confirmed-work read model that preserves assignment visibility when pay data is unavailable."
    - path: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      provides: "The rebuilt confirmed-work hub for assignments, pay context, and attendance guidance."
    - path: src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx
      provides: "A pay summary card that distinguishes total-ready and pay-unavailable states."
    - path: src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
      provides: "Attendance statuses that still render for confirmed assignments even when pay is unavailable."
  key_links:
    - from: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      to: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      via: "the rebuilt confirmed-work page still reads confirmed assignments through the existing assignment query slice"
      pattern: "listConfirmedWorkerAssignments"
    - from: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      to: src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
      via: "attendance context still comes from the worker attendance query instead of UI-side timing math"
      pattern: "listWorkerAttendanceStatuses"
    - from: src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
      to: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      via: "attendance status generation still derives one record per confirmed assignment"
      pattern: "listConfirmedWorkerAssignments"
    - from: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      to: src/mutations/attendance/components/AttendanceCheckInCard.tsx
      via: "each assignment card still embeds the dedicated check-in interaction"
      pattern: "AttendanceCheckInCard"
---

<objective>
`/worker/assignments` 를 확정 근무 허브로 재구성해, worker 가 확정된 역할, 예상 급여 준비 상태, 체크인 가능 여부를 한 화면에서 자연스럽게 이해할 수 있게 만든다.

Purpose: 현재 assignment preview 기반을 유지하면서 confirmed-work hierarchy 를 강화하고, rate 미설정 상태에서도 assignment 와 attendance 맥락이 사라지지 않도록 read contract 를 보강한다.
Output: pay-ready / pay-unavailable 상태를 구분하는 confirmed-work query, 재구성된 confirmed-work page, pay summary card, 그리고 회귀 테스트.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-worker-work-surface-completion/12-CONTEXT.md
@.planning/phases/12-worker-work-surface-completion/12-RESEARCH.md
@.planning/phases/12-worker-work-surface-completion/12-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@src/app/worker/assignments/page.tsx
@src/app/worker/loading.tsx
@src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
@src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
@src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx
@src/flows/worker-assignment-preview/utils/workerAssignmentPreview.ts
@src/queries/assignment/types/workerAssignmentPreview.ts
@src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
@src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
@src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
@src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts
@src/mutations/attendance/components/AttendanceCheckInCard.tsx
@src/mutations/attendance/actions/submitAttendanceCheckIn.ts
@src/shared/ui/card.tsx
@src/shared/ui/badge.tsx
@src/shared/ui/alert.tsx
@src/shared/ui/button.tsx
@vitest.config.ts
</context>

<interfaces>
From src/queries/assignment/types/workerAssignmentPreview.ts:
```ts
export interface WorkerAssignmentPreview extends PayPreviewBreakdown {
  assignmentId: string;
  scheduleId: string;
  scheduleRoleSlotId: string;
  roleCode: string;
  startsAt: string;
  endsAt: string;
}
```

From src/queries/assignment/dal/listConfirmedWorkerAssignments.ts:
```ts
if (!workerRateResult.data) {
  return [];
}
```

From src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx:
```tsx
const [assignments, attendanceStatuses] = await Promise.all([
  listConfirmedWorkerAssignments(currentUser.id),
  listWorkerAttendanceStatuses(currentUser.id),
]);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Preserve confirmed-work visibility when pay data is unavailable</name>
  <read_first>
    .planning/phases/12-worker-work-surface-completion/12-CONTEXT.md
    .planning/phases/12-worker-work-surface-completion/12-RESEARCH.md
    .planning/phases/12-worker-work-surface-completion/12-VALIDATION.md
    src/queries/assignment/types/workerAssignmentPreview.ts
    src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
    src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
    src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
    src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts
    src/queries/assignment/utils/calculatePayPreview.ts
    vitest.config.ts
  </read_first>
  <files>
    src/queries/assignment/types/workerAssignmentPreview.ts
    src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
    src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
    src/queries/attendance/dal/listWorkerAttendanceStatuses.ts
    src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts
  </files>
  <behavior>
    - Confirmed assignments must stay visible even when worker-rate data is missing.
    - The assignment read model must distinguish `ready` pay data from `missing_worker_rate` without losing schedule identity, role, or time window.
    - Attendance status generation must continue to return one status per confirmed assignment regardless of pay availability.
  </behavior>
  <action>Update `src/queries/assignment/types/workerAssignmentPreview.ts` so it introduces `export type WorkerAssignmentPayStatus = "ready" | "missing_worker_rate"` and adds `payStatus: WorkerAssignmentPayStatus` to `WorkerAssignmentPreview`. Change `hourlyRateCents`, `regularHours`, `overtimeHours`, `regularPayCents`, `overtimePayCents`, and `totalPayCents` to `number | null`, while keeping `overtimeApplied` as `boolean`. In `src/queries/assignment/dal/listConfirmedWorkerAssignments.ts`, remove the current early return `return []` when `workerRateResult.data` is missing. Instead, map every confirmed assignment row to a `payStatus: "missing_worker_rate"` object with the nullable pay fields set to `null` and `overtimeApplied: false`; keep the existing `calculatePayPreview(...)` path for `payStatus: "ready"`. Update `src/queries/attendance/dal/listWorkerAttendanceStatuses.ts` so it derives status from assignment identity and schedule timing only, without assuming pay fields are present. Expand both test files to assert the exact `payStatus` contract and that attendance statuses still exist for confirmed assignments when the worker rate is missing.</action>
  <acceptance_criteria>
    - `src/queries/assignment/types/workerAssignmentPreview.ts` contains `WorkerAssignmentPayStatus`.
    - `src/queries/assignment/types/workerAssignmentPreview.ts` contains `payStatus`.
    - `src/queries/assignment/types/workerAssignmentPreview.ts` contains `number | null`.
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.ts` contains `missing_worker_rate`.
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.ts` contains `hourlyRateCents: null`.
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.ts` contains `totalPayCents: null`.
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts` asserts `payStatus: "missing_worker_rate"`.
    - `src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts` asserts attendance status creation when pay data is unavailable.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts</automated>
  </verify>
  <done>The confirmed-work data layer now keeps real assignments visible even when pay metadata is still being prepared.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the confirmed-work surface, pay summary, and state guidance</name>
  <read_first>
    .planning/phases/12-worker-work-surface-completion/12-CONTEXT.md
    .planning/phases/12-worker-work-surface-completion/12-RESEARCH.md
    .planning/phases/12-worker-work-surface-completion/12-VALIDATION.md
    src/app/worker/loading.tsx
    src/flows/worker-shell/components/WorkerShellPage.tsx
    src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
    src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
    src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx
    src/flows/worker-assignment-preview/utils/workerAssignmentPreview.ts
    src/mutations/attendance/components/AttendanceCheckInCard.tsx
    src/shared/ui/card.tsx
    src/shared/ui/badge.tsx
    src/shared/ui/alert.tsx
    src/shared/ui/button.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
    src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
    src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx
    src/flows/worker-assignment-preview/utils/workerAssignmentPreview.ts
  </files>
  <behavior>
    - The confirmed-work page must remain the single worker destination for confirmed shifts, pay context, and attendance guidance.
    - The page must render separate branches for no confirmed work and pay-unavailable confirmed work.
    - Attendance check-in must stay embedded on each assignment card and keep the current `Check in now` action.
  </behavior>
  <action>Rewrite `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx` so the page uses badge text `Confirmed work`, heading `Confirmed shifts`, and supporting copy `Review your confirmed roles, expected pay, and check-in status before arriving on site.`. The empty-state card title must be exactly `No confirmed shift yet`, with description `When an admin finalizes your role, it will appear here with pay and check-in details.` and a CTA link labeled `Browse recruiting schedules` to `/worker/schedules`. Add a dedicated pay-unavailable branch that still renders the confirmed assignment card and attendance card, but also shows a card titled `Pay details are not ready yet` with description `Your shift is confirmed, but your hourly rate is still being assigned. Contact an admin if this stays unresolved.`. Update `src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx` so when any assignment has `payStatus === "missing_worker_rate"` it shows the exact headline `Expected pay not available yet` and helper copy `Hourly rate is still being assigned for at least one confirmed shift.` instead of a numeric total. Update `src/flows/worker-assignment-preview/utils/workerAssignmentPreview.ts` so `getAssignmentBreakdown()` returns the exact fallback label `Pending admin rate` for null rate and pay values, while `formatScheduleWindow()` remains the assignment header formatter. Expand `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx` to assert the new heading, empty-state CTA, pay-unavailable branch, total-ready branch, and that the `Check in now` button remains present for open attendance states.</action>
  <acceptance_criteria>
    - `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx` contains `Confirmed shifts`.
    - `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx` contains `No confirmed shift yet`.
    - `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx` contains `Browse recruiting schedules`.
    - `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx` contains `Pay details are not ready yet`.
    - `src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx` contains `Expected pay not available yet`.
    - `src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx` contains `Hourly rate is still being assigned for at least one confirmed shift.`.
    - `src/flows/worker-assignment-preview/utils/workerAssignmentPreview.ts` contains `Pending admin rate`.
    - `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx` asserts the empty-state CTA and pay-unavailable branch.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts</automated>
  </verify>
  <done>The worker confirmed-work route now explains confirmed shifts, pay readiness, and attendance next steps without hiding real work behind missing metadata.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts src/queries/attendance/dal/listWorkerAttendanceStatuses.test.ts src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts` and confirm the rebuilt confirmed-work surface preserves assignment visibility, pay-readiness guidance, and attendance state behavior.
</verification>

<success_criteria>
Workers can understand confirmed work, expected pay readiness, and attendance context from `/worker/assignments`, including explicit empty and unavailable states.
</success_criteria>

<output>
After completion, create `.planning/phases/12-worker-work-surface-completion/12-worker-work-surface-completion-02-SUMMARY.md`
</output>
