---
phase: 11-admin-scheduling-surface-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/flows/admin-schedules/components/AdminSchedulesPage.tsx
  - src/flows/admin-schedules/components/CreateScheduleForm.tsx
  - src/flows/admin-schedules/components/CreateScheduleForm.test.tsx
  - src/flows/admin-schedules/components/ScheduleTable.tsx
  - src/flows/admin-schedules/components/ScheduleStatusForm.tsx
  - src/flows/admin-schedules/components/AdminSchedulesPage.test.tsx
  - src/flows/admin-schedules/utils/formatSchedule.ts
autonomous: true
requirements:
  - ADMINUI-01
  - ADMINUI-02
must_haves:
  truths:
    - "Admins can understand the schedule creation form without mojibake copy or raw fieldset defaults."
    - "Saved schedules render with readable time, status, and staffing summary instead of a bare raw table."
    - "The list page still uses the existing `submitSchedule` and `submitScheduleStatus` contracts rather than introducing new write paths."
  artifacts:
    - path: src/flows/admin-schedules/components/CreateScheduleForm.tsx
      provides: "A readable schedule-creation surface for ADMINUI-01."
    - path: src/flows/admin-schedules/components/AdminSchedulesPage.tsx
      provides: "The production admin schedule-management landing screen."
    - path: src/flows/admin-schedules/components/ScheduleTable.tsx
      provides: "A readable saved-schedules surface with detail entry and status context."
    - path: src/flows/admin-schedules/components/ScheduleStatusForm.tsx
      provides: "Inline status controls that stay limited to recruiting/assigning."
  key_links:
    - from: src/flows/admin-schedules/components/CreateScheduleForm.tsx
      to: src/mutations/schedule/actions/submitSchedule.ts
      via: "the rebuilt create surface still posts directly to the existing schedule submit wrapper"
      pattern: "action={submitSchedule}|submitSchedule"
    - from: src/flows/admin-schedules/components/ScheduleStatusForm.tsx
      to: src/mutations/schedule/actions/submitScheduleStatus.ts
      via: "the saved-schedules surface still uses the existing inline status wrapper"
      pattern: "action={submitScheduleStatus}|submitScheduleStatus"
    - from: src/flows/admin-schedules/components/AdminSchedulesPage.tsx
      to: src/queries/schedule/dal/listAdminSchedules.ts
      via: "the admin schedule screen still performs the server-side schedule list read"
      pattern: "listAdminSchedules"
    - from: src/flows/admin-schedules/components/AdminSchedulesPage.test.tsx
      to: src/flows/admin-schedules/components/AdminSchedulesPage.tsx
      via: "the new page test proves the readable shell, empty state, and saved schedule presentation"
      pattern: "Schedule management|No schedules created yet"
---

<objective>
Turn `/admin/schedules` into a readable schedule-management screen where creation and saved schedule review feel like one coherent admin workflow.

Purpose: replace the raw creation form and bare saved-schedules table with a product-grade management surface while preserving the existing schedule write contracts.
Output: a rebuilt schedule creation form, a readable saved-schedules surface, and regression tests for the new admin schedule-management shell.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-admin-scheduling-surface-completion/11-RESEARCH.md
@.planning/phases/11-admin-scheduling-surface-completion/11-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@src/app/admin/schedules/page.tsx
@src/flows/admin-schedules/components/AdminSchedulesPage.tsx
@src/flows/admin-schedules/components/CreateScheduleForm.tsx
@src/flows/admin-schedules/components/ScheduleTable.tsx
@src/flows/admin-schedules/components/ScheduleStatusForm.tsx
@src/flows/admin-schedules/utils/formatSchedule.ts
@src/mutations/schedule/actions/submitSchedule.ts
@src/mutations/schedule/actions/submitScheduleStatus.ts
@src/mutations/schedule/schemas/schedule.ts
@src/mutations/schedule/schemas/updateScheduleStatus.ts
@src/queries/schedule/dal/listAdminSchedules.ts
@src/shared/ui/button.tsx
@src/shared/ui/card.tsx
@src/shared/ui/alert.tsx
@src/shared/ui/badge.tsx
@vitest.config.ts
</context>

<interfaces>
From src/flows/admin-schedules/components/CreateScheduleForm.tsx:
```tsx
<form action={submitSchedule}>
```

From src/mutations/schedule/schemas/schedule.ts:
```ts
date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
startTime: z.string().regex(timePattern),
endTime: z.string().regex(timePattern),
roleSlots: z.array(scheduleRoleSlotSchema).min(1),
```

From src/mutations/schedule/schemas/updateScheduleStatus.ts:
```ts
export const inlineScheduleStatuses = ["recruiting", "assigning"] as const;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the schedule creation surface and role-slot editor</name>
  <read_first>
    .planning/phases/11-admin-scheduling-surface-completion/11-RESEARCH.md
    .planning/phases/11-admin-scheduling-surface-completion/11-VALIDATION.md
    src/flows/admin-schedules/components/CreateScheduleForm.tsx
    src/mutations/schedule/actions/submitSchedule.ts
    src/mutations/schedule/schemas/schedule.ts
    src/shared/ui/button.tsx
    src/shared/ui/card.tsx
    src/shared/ui/alert.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/admin-schedules/components/CreateScheduleForm.tsx
    src/flows/admin-schedules/components/CreateScheduleForm.test.tsx
  </files>
  <behavior>
    - The form must keep `action={submitSchedule}` and the existing field names `date`, `startTime`, `endTime`, `roleCode`, and `headcount`.
    - The form must expose readable labels, a clear primary action, and explicit add/remove role-slot controls.
    - The form must never allow the last role slot to be removed.
  </behavior>
  <action>Rewrite `src/flows/admin-schedules/components/CreateScheduleForm.tsx` into a card-based creation surface with the heading `Create schedule`, supporting copy that explains new schedules start in `Recruiting`, and labeled controls `Work date`, `Starts at`, `Ends at`, `Role code`, and `Headcount`. Keep the existing local `roleSlots` state shape and still post directly to `submitSchedule`, but replace raw fieldsets and raw buttons with a two-section layout that uses `Card`, `Button`, and readable Tailwind-styled native inputs. The role-slot editor must render one card per slot, show `Role slot {index + 1}`, expose `Remove role` and `Add another role` buttons, and keep the submit label `Save schedule`. Add `src/flows/admin-schedules/components/CreateScheduleForm.test.tsx` that renders the component, asserts the heading, supporting copy, all field labels, the `Add another role` button, the `Remove role` button disabled state when only one slot remains, and the primary button name `Save schedule`.</action>
  <acceptance_criteria>
    - `src/flows/admin-schedules/components/CreateScheduleForm.tsx` contains `Create schedule`.
    - `src/flows/admin-schedules/components/CreateScheduleForm.tsx` contains `Save schedule`.
    - `src/flows/admin-schedules/components/CreateScheduleForm.tsx` still contains `action={submitSchedule}`.
    - `src/flows/admin-schedules/components/CreateScheduleForm.tsx` still renders inputs named `date`, `startTime`, `endTime`, `roleCode`, and `headcount`.
    - `src/flows/admin-schedules/components/CreateScheduleForm.tsx` contains `Add another role`.
    - `src/flows/admin-schedules/components/CreateScheduleForm.test.tsx` exists.
    - `src/flows/admin-schedules/components/CreateScheduleForm.test.tsx` asserts the heading and `Save schedule` button.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/admin-schedules/components/CreateScheduleForm.test.tsx src/mutations/schedule/actions/createSchedule.test.ts src/mutations/schedule/actions/submitSchedule.test.ts</automated>
  </verify>
  <done>The schedule creation workflow reads like a product surface while preserving the existing server-action and schema contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the saved-schedules surface and inline status controls</name>
  <read_first>
    .planning/phases/11-admin-scheduling-surface-completion/11-RESEARCH.md
    .planning/phases/11-admin-scheduling-surface-completion/11-VALIDATION.md
    src/flows/admin-schedules/components/AdminSchedulesPage.tsx
    src/flows/admin-schedules/components/ScheduleTable.tsx
    src/flows/admin-schedules/components/ScheduleStatusForm.tsx
    src/flows/admin-schedules/utils/formatSchedule.ts
    src/queries/schedule/dal/listAdminSchedules.ts
    src/mutations/schedule/actions/submitScheduleStatus.ts
    src/mutations/schedule/schemas/updateScheduleStatus.ts
    src/shared/ui/button.tsx
    src/shared/ui/card.tsx
    src/shared/ui/badge.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/admin-schedules/components/AdminSchedulesPage.tsx
    src/flows/admin-schedules/components/ScheduleTable.tsx
    src/flows/admin-schedules/components/ScheduleStatusForm.tsx
    src/flows/admin-schedules/components/AdminSchedulesPage.test.tsx
    src/flows/admin-schedules/utils/formatSchedule.ts
  </files>
  <behavior>
    - The page must keep the admin-only gate and the server-side `listAdminSchedules()` read.
    - The saved-schedules surface must show start time, end time, status, role summary, and a detail entry without relying on a raw browser table.
    - Inline status controls must still limit changes to `recruiting` and `assigning`, and confirmed schedules must not show the inline status control.
  </behavior>
  <action>Rewrite `src/flows/admin-schedules/components/AdminSchedulesPage.tsx` into a full-height admin workspace screen with the heading `Schedule management`, readable supporting copy, an admin denial card with the exact heading `Admin access is required.`, and two primary sections: the creation surface from Task 1 and a saved-schedules section. Rebuild `src/flows/admin-schedules/components/ScheduleTable.tsx` to render saved schedules as stacked `Card` rows instead of a bare HTML table. Each row must show a status badge from `scheduleStatusLabels`, a readable local start/end window from `formatSchedule.ts`, the role summary, a detail CTA labeled `Open assignment detail`, and the inline status control region. Update `src/flows/admin-schedules/components/ScheduleStatusForm.tsx` to keep `action={submitScheduleStatus}` and `inlineScheduleStatuses`, but render a labeled `Move to` select plus a `Button` labeled `Update status`; confirmed schedules must still return `null`. Expand `src/flows/admin-schedules/utils/formatSchedule.ts` with helper output needed by the new schedule row presentation while preserving `scheduleStatusLabels`. Add `src/flows/admin-schedules/components/AdminSchedulesPage.test.tsx` that mocks `requireAdminUser()` and `listAdminSchedules()`, asserts the page heading, the empty-state copy `No schedules created yet.`, the saved schedule status and role summary, the `Open assignment detail` href, and the absence of `Update status` for a confirmed schedule.</action>
  <acceptance_criteria>
    - `src/flows/admin-schedules/components/AdminSchedulesPage.tsx` contains `Schedule management`.
    - `src/flows/admin-schedules/components/AdminSchedulesPage.tsx` still contains `listAdminSchedules`.
    - `src/flows/admin-schedules/components/ScheduleTable.tsx` contains `Open assignment detail`.
    - `src/flows/admin-schedules/components/ScheduleTable.tsx` contains `No schedules created yet.`.
    - `src/flows/admin-schedules/components/ScheduleStatusForm.tsx` still contains `action={submitScheduleStatus}`.
    - `src/flows/admin-schedules/components/ScheduleStatusForm.tsx` contains `Update status`.
    - `src/flows/admin-schedules/components/AdminSchedulesPage.test.tsx` exists.
    - `src/flows/admin-schedules/components/AdminSchedulesPage.test.tsx` asserts the page heading and empty state.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/admin-schedules/components/AdminSchedulesPage.test.tsx src/queries/schedule/dal/listAdminSchedules.test.ts src/mutations/schedule/actions/updateScheduleStatus.test.ts</automated>
  </verify>
  <done>The admin schedule landing route now combines creation and saved-schedule management in one readable workspace without changing the underlying admin schedule contracts.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/flows/admin-schedules/components/CreateScheduleForm.test.tsx src/flows/admin-schedules/components/AdminSchedulesPage.test.tsx src/mutations/schedule/actions/createSchedule.test.ts src/mutations/schedule/actions/submitSchedule.test.ts src/queries/schedule/dal/listAdminSchedules.test.ts src/mutations/schedule/actions/updateScheduleStatus.test.ts` and confirm the rebuilt creation and saved-schedule surfaces preserve the existing action and read-model contracts.
</verification>

<success_criteria>
Admins can create schedules from a readable form and scan saved schedules without raw-table defaults, while the existing schedule actions and list DTO remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-scheduling-surface-completion/11-admin-scheduling-surface-completion-01-SUMMARY.md`
</output>
