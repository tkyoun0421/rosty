---
phase: 11-admin-scheduling-surface-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
  - src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx
  - src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx
  - src/flows/admin-schedule-assignment/components/AssignmentSummaryCard.tsx
  - src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx
  - src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
autonomous: true
requirements:
  - ADMINUI-03
must_haves:
  truths:
    - "The schedule detail route shows schedule facts, attendance context, and assignment status in a readable hierarchy before the admin edits anything."
    - "Draft save and final confirm remain separate actions with distinct inline feedback."
    - "The detail surface keeps using the existing assignment and attendance read models plus the current submit wrappers."
  artifacts:
    - path: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
      provides: "The server flow entry for the rebuilt admin schedule detail surface."
    - path: src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx
      provides: "The readable assignment editor with inline draft-save and confirm feedback."
    - path: src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx
      provides: "A readable confirmation checkpoint before final publish."
    - path: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
      provides: "Regression coverage for hierarchy, draft save, confirm, and detail-page feedback."
  key_links:
    - from: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
      to: src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
      via: "the server detail page still loads assignment detail through the existing read model"
      pattern: "getAdminScheduleAssignmentDetail"
    - from: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
      to: src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
      via: "the detail page still loads attendance detail through the existing read model"
      pattern: "getAdminScheduleAttendanceDetail"
    - from: src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx
      to: src/mutations/assignment/actions/submitScheduleAssignmentDraft.ts
      via: "draft-save feedback still posts through the dedicated draft wrapper"
      pattern: "submitScheduleAssignmentDraft"
    - from: src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx
      to: src/mutations/assignment/actions/submitScheduleAssignmentConfirm.ts
      via: "final confirm still posts through the dedicated confirm wrapper"
      pattern: "submitScheduleAssignmentConfirm"
---

<objective>
Turn `/admin/schedules/[scheduleId]` into a readable schedule-detail workflow where assignment review, attendance context, draft save, and final confirm all make sense in one place.

Purpose: keep the current detail data and mutation contracts intact while replacing the remaining inline-style heavy editor with a Phase 10/11-aligned management surface.
Output: a rebuilt schedule detail shell, clearer summary and attendance regions, a polished applicant assignment editor, and updated regression coverage.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-admin-scheduling-surface-completion/11-RESEARCH.md
@.planning/phases/11-admin-scheduling-surface-completion/11-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@src/app/admin/schedules/[scheduleId]/page.tsx
@src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
@src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx
@src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx
@src/flows/admin-schedule-assignment/components/AssignmentSummaryCard.tsx
@src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx
@src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
@src/flows/admin-schedule-assignment/utils/adminScheduleAssignment.ts
@src/flows/admin-schedule-assignment/utils/attendanceReview.ts
@src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
@src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
@src/mutations/assignment/actions/submitScheduleAssignmentDraft.ts
@src/mutations/assignment/actions/submitScheduleAssignmentConfirm.ts
@src/shared/ui/button.tsx
@src/shared/ui/card.tsx
@src/shared/ui/alert.tsx
@src/shared/ui/badge.tsx
@vitest.config.ts
</context>

<interfaces>
From src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx:
```ts
const [assignmentDetail, attendanceDetail] = await Promise.all([
  getAdminScheduleAssignmentDetail(scheduleId),
  getAdminScheduleAttendanceDetail(scheduleId),
]);
```

From src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx:
```ts
await submitScheduleAssignmentDraft(buildDraftFormData());
await submitScheduleAssignmentConfirm(formData);
```

From src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx:
```ts
expect(screen.getByText("Draft saved. You can keep reviewing assignments on this page.")).toBeInTheDocument();
expect(screen.getByText("Assignments confirmed. Workers can now see final roles and pay previews.")).toBeInTheDocument();
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the schedule-detail shell, schedule facts, and summary regions</name>
  <read_first>
    .planning/phases/11-admin-scheduling-surface-completion/11-RESEARCH.md
    .planning/phases/11-admin-scheduling-surface-completion/11-VALIDATION.md
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
    src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx
    src/flows/admin-schedule-assignment/components/AssignmentSummaryCard.tsx
    src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
    src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
    src/shared/ui/card.tsx
    src/shared/ui/badge.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
    src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx
    src/flows/admin-schedule-assignment/components/AssignmentSummaryCard.tsx
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
  </files>
  <behavior>
    - The page must keep the admin-only guard and the paired server-side reads for assignment and attendance detail.
    - The page must show schedule facts and current schedule status before the editing controls.
    - Attendance review and assignment summary must remain distinct regions rather than being collapsed into one mixed panel.
  </behavior>
  <action>Rewrite `src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx` so the page starts with a schedule-detail header using badge text `Schedule detail`, heading `Schedule assignment detail`, supporting copy about reviewing staffing and attendance before final publish, and a readable schedule facts strip that shows the schedule window and current status. Keep the existing `Promise.all` detail reads, `requireAdminUser()` guard, and `notFound()` behavior. Refine `src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx` and `src/flows/admin-schedule-assignment/components/AssignmentSummaryCard.tsx` so both use shared `Card` language, clearer summary copy, and consistent visual hierarchy with the Phase 10 workspace surfaces. Expand `src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx` to assert the new detail heading, the schedule facts copy, and that attendance review still renders before applicant controls.</action>
  <acceptance_criteria>
    - `src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx` contains `Schedule assignment detail`.
    - `src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx` still contains `getAdminScheduleAssignmentDetail`.
    - `src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx` still contains `getAdminScheduleAttendanceDetail`.
    - `src/flows/admin-schedule-assignment/components/AttendanceReviewPanel.tsx` imports `#shared/ui/card`.
    - `src/flows/admin-schedule-assignment/components/AssignmentSummaryCard.tsx` imports `#shared/ui/card`.
    - `src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx` asserts the detail heading and attendance-first ordering.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts src/queries/attendance/dal/getAdminScheduleAttendanceDetail.test.ts</automated>
  </verify>
  <done>The detail route now reads as a schedule review workspace with clear schedule facts, attendance context, and summary hierarchy before the admin edits assignments.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the applicant assignment controls and confirmation checkpoint</name>
  <read_first>
    .planning/phases/11-admin-scheduling-surface-completion/11-RESEARCH.md
    .planning/phases/11-admin-scheduling-surface-completion/11-VALIDATION.md
    src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx
    src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
    src/flows/admin-schedule-assignment/utils/adminScheduleAssignment.ts
    src/mutations/assignment/actions/submitScheduleAssignmentDraft.ts
    src/mutations/assignment/actions/submitScheduleAssignmentConfirm.ts
    src/shared/ui/button.tsx
    src/shared/ui/card.tsx
    src/shared/ui/alert.tsx
    src/shared/ui/badge.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx
    src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
  </files>
  <behavior>
    - The applicant editor must preserve local assignment state and keep draft-save and final-confirm as separate actions.
    - Inline feedback must remain on the current page and must still surface the existing success and failure messages.
    - The confirmation dialog must summarize filled seats, unfilled slots, and role-slot counts before the final confirm button.
  </behavior>
  <action>Rewrite `src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx` to replace the current inline-style-heavy regions with shared `Card`, `Button`, `Alert`, and `Badge` presentation while preserving the current editable assignment state, slot selection logic, `buildDraftAssignmentsPayload(assignments)`, `submitScheduleAssignmentDraft(buildDraftFormData())`, and `submitScheduleAssignmentConfirm(formData)`. Keep the exact success text `Draft saved. You can keep reviewing assignments on this page.` and `Assignments confirmed. Workers can now see final roles and pay previews.` and keep failure fallbacks `Draft save failed.` and `Confirmation failed.`. Rewrite `src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx` so it presents the confirmation summary in the same visual language and still blocks final confirm behind an explicit dialog. Expand `src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx` so it continues proving draft-save payloads and confirm payloads, while also asserting the readable dialog copy and inline feedback remain visible on the same page.</action>
  <acceptance_criteria>
    - `src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx` still contains `submitScheduleAssignmentDraft`.
    - `src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx` still contains `submitScheduleAssignmentConfirm`.
    - `src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx` contains `Draft saved. You can keep reviewing assignments on this page.`.
    - `src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx` contains `Assignments confirmed. Workers can now see final roles and pay previews.`.
    - `src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx` contains `Confirm assignments`.
    - `src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx` still asserts the draft-save and confirm success paths.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts src/mutations/assignment/actions/confirmScheduleAssignments.test.ts</automated>
  </verify>
  <done>The schedule-detail editor now makes draft save, final confirm, and assignment review understandable without changing the underlying detail flow or submit wrappers.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts src/queries/attendance/dal/getAdminScheduleAttendanceDetail.test.ts src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts src/mutations/assignment/actions/confirmScheduleAssignments.test.ts` and confirm the rebuilt detail surface preserves the current read and write contracts.
</verification>

<success_criteria>
Admins can understand the schedule detail workflow, review attendance context, save drafts, and confirm assignments from one readable surface with explicit inline feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-scheduling-surface-completion/11-admin-scheduling-surface-completion-02-SUMMARY.md`
</output>
