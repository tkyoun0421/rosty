---
phase: 03-assignment-and-pay-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260402_phase3_assignment_and_pay_preview.sql
  - src/shared/config/cacheTags.ts
  - src/shared/model/assignment.ts
  - src/shared/model/assignment.test.ts
  - src/queries/assignment/types/adminScheduleAssignmentDetail.ts
  - src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
  - src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts
  - src/mutations/assignment/schemas/saveScheduleAssignmentDraft.ts
  - src/mutations/assignment/schemas/saveScheduleAssignmentDraft.test.ts
  - src/mutations/assignment/dal/assignmentDal.ts
  - src/mutations/assignment/dal/assignmentDal.test.ts
  - src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts
  - src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts
  - src/mutations/assignment/actions/submitScheduleAssignmentDraft.ts
autonomous: true
requirements:
  - APPL-03
  - ASGN-01
must_haves:
  truths:
    - "An admin can open one schedule detail read model and see role slots, applicants, and each applicant's current assignment state."
    - "An admin can save a draft assignment plan without confirming it to workers."
    - "One worker cannot be drafted into two role slots on the same schedule, and one slot cannot exceed its headcount."
    - "A worker-side confirmed assignment read can use the session-bound Supabase client because the Phase 3 DB contract exposes confirmed schedule data, confirmed role-slot data, and the worker's own hourly rate safely under RLS."
  artifacts:
    - path: supabase/migrations/20260402_phase3_assignment_and_pay_preview.sql
      provides: "Assignment enum, schedule_assignments table, indexes, RLS, confirmed worker-read policies, and database-side invariants for draft and confirmed rows"
    - path: src/shared/model/assignment.ts
      provides: "Shared assignment status primitives and canonical assignment row contracts"
    - path: src/queries/assignment/types/adminScheduleAssignmentDetail.ts
      provides: "Admin schedule-detail DTO for applicant review and slot assignment"
    - path: src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
      provides: "Server read model for schedule-centered applicant review per D-01, D-02, and D-03"
    - path: src/mutations/assignment/schemas/saveScheduleAssignmentDraft.ts
      provides: "Whole-schedule draft-save input parsing and validation"
    - path: src/mutations/assignment/dal/assignmentDal.ts
      provides: "Transactional draft replacement write path for one schedule"
    - path: src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts
      provides: "Admin-only draft-save server action"
  key_links:
    - from: src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
      to: supabase/migrations/20260402_phase3_assignment_and_pay_preview.sql
      via: "join of schedules, role slots, applications, profiles, and schedule_assignments"
      pattern: "schedule_assignments|schedule_applications|schedule_role_slots"
    - from: src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts
      to: src/mutations/assignment/schemas/saveScheduleAssignmentDraft.ts
      via: "server action parses FormData into a whole-schedule payload"
      pattern: "parseSaveScheduleAssignmentDraft"
    - from: src/mutations/assignment/dal/assignmentDal.ts
      to: src/shared/config/cacheTags.ts
      via: "assignment detail cache invalidation tags"
      pattern: "assignments"
    - from: supabase/migrations/20260402_phase3_assignment_and_pay_preview.sql
      to: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      via: "worker-confirmed read path uses direct session-bound reads over schedule_assignments, schedules, schedule_role_slots, and self worker_rates under explicit Phase 3 RLS policies"
      pattern: "confirmed|worker_rates|schedule_assignments|schedule_role_slots|schedules"
---

<objective>
Create the Phase 3 assignment data contract, admin schedule-detail read model, and whole-schedule draft-save mutation.

Purpose: establish the schedule-centered applicant review and role-slot draft assignment foundation required by D-01 through D-09 before building confirm UI or worker preview.
Output: assignment migration, shared assignment contracts, admin detail query, whole-schedule draft-save schema/DAL/action, and colocated tests.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
@.planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
@.planning/phases/03-assignment-and-pay-preview/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01-access-foundation/01-access-foundation-04-SUMMARY.md
@.planning/phases/02-schedule-publishing/02-schedule-publishing-01-SUMMARY.md
@.planning/phases/02-schedule-publishing/02-schedule-publishing-02-SUMMARY.md
@.planning/phases/02-schedule-publishing/02-schedule-publishing-03-SUMMARY.md
@CLAUDE.md
@src/shared/model/schedule.ts
@src/shared/model/access.ts
@src/shared/config/cacheTags.ts
@src/queries/schedule/dal/listAdminSchedules.ts
@src/mutations/schedule/dal/scheduleDal.ts
@src/mutations/application/actions/createScheduleApplication.ts

<interfaces>
From src/shared/model/schedule.ts:
```ts
export type ScheduleStatus = "recruiting" | "assigning" | "confirmed";

export interface ScheduleRecord {
  id: string;
  startsAt: string;
  endsAt: string;
  status: ScheduleStatus;
  createdBy: string;
  createdAt: string;
  updatedAt: string;
}
```

From src/shared/model/access.ts:
```ts
export interface WorkerRateRecord {
  userId: string;
  hourlyRateCents: number;
  updatedBy: string;
  updatedAt: string;
}
```

From src/shared/config/cacheTags.ts:
```ts
export const cacheTags = {
  schedules: {
    all: "schedules",
    adminList: "schedules:admin-list",
    recruitingList: "schedules:recruiting-list",
  },
  applications: {
    all: "applications",
    workerScheduleIds: "applications:worker-schedule-ids",
  },
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 3 assignment database contract and interface scaffolding</name>
  <read_first>
    .planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
    .planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
    .planning/phases/03-assignment-and-pay-preview/03-VALIDATION.md
    supabase/migrations/20260401_phase2_schedule_publishing.sql
    src/shared/model/schedule.ts
    src/shared/config/cacheTags.ts
  </read_first>
  <files>
    supabase/migrations/20260402_phase3_assignment_and_pay_preview.sql
    src/shared/config/cacheTags.ts
    src/shared/model/assignment.ts
    src/shared/model/assignment.test.ts
    src/queries/assignment/types/adminScheduleAssignmentDetail.ts
    src/mutations/assignment/schemas/saveScheduleAssignmentDraft.ts
    src/mutations/assignment/schemas/saveScheduleAssignmentDraft.test.ts
    src/mutations/assignment/dal/assignmentDal.test.ts
    src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts
    src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts
  </files>
  <action>Per D-04, D-05, D-07, D-08, D-13, and D-19 through D-21, add `supabase/migrations/20260402_phase3_assignment_and_pay_preview.sql` with exactly one new enum `public.assignment_status` containing `draft` and `confirmed`, and one new table `public.schedule_assignments` containing `id uuid`, `schedule_id uuid`, `schedule_role_slot_id uuid`, `worker_user_id uuid`, `status public.assignment_status`, `confirmed_at timestamptz`, `confirmed_by uuid`, `created_at timestamptz`, and `updated_at timestamptz`. Enforce `unique (schedule_id, worker_user_id)` so one worker cannot occupy two slots on one schedule, add an index on `(schedule_role_slot_id, status)`, add a partial index on `(worker_user_id, schedule_id)` where `status = 'confirmed'`, and enable RLS for admin/service-role writes plus authenticated worker reads limited to `status = 'confirmed'`. In the same migration, explicitly lock the worker confirmed-read contract to direct RLS-backed base-table reads: add worker-select policies that let authenticated workers read only their own confirmed `schedule_assignments` rows, the matching `schedules` rows when `status = 'confirmed'`, the matching `schedule_role_slots` rows for those confirmed assignments, and their own `worker_rates` row. Do not introduce a `security_invoker` view or RPC in this phase plan. Extend `src/shared/config/cacheTags.ts` with stable assignment tags for schedule detail and worker confirmed assignments. Create `src/shared/model/assignment.ts` and `src/queries/assignment/types/adminScheduleAssignmentDetail.ts` as the canonical Phase 3 contracts. Add the missing Wave 0 test files named in `03-VALIDATION.md` for the admin detail query, draft schema, DAL, and action.</action>
  <acceptance_criteria>Migration defines the assignment table and invariants; the DB contract explicitly exposes a worker-safe confirmed assignment + pay-basis read path through direct RLS-backed reads on `schedule_assignments`, `schedules`, `schedule_role_slots`, and self `worker_rates`; cache tags include assignment-specific keys; shared and query contract files exist; all missing test files for APPL-03 and ASGN-01 exist in the exact target paths.</acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/shared/model/assignment.test.ts src/mutations/assignment/schemas/saveScheduleAssignmentDraft.test.ts src/mutations/assignment/dal/assignmentDal.test.ts src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts</automated>
  </verify>
  <done>The repo has the concrete Phase 3 assignment schema, contracts, cache tags, and colocated test scaffolding needed for implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the schedule-centered admin detail query for applicant review</name>
  <read_first>
    .planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
    .planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
    src/queries/schedule/dal/listAdminSchedules.ts
    src/shared/lib/supabase/adminClient.ts
    src/queries/assignment/types/adminScheduleAssignmentDetail.ts
    src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts
  </read_first>
  <files>
    src/queries/assignment/types/adminScheduleAssignmentDetail.ts
    src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
    src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts
  </files>
  <behavior>
    - Test 1: `getAdminScheduleAssignmentDetail(scheduleId)` returns the schedule window, current schedule status, and every role slot with `headcount` plus `assignedCount`.
    - Test 2: applicants are returned from `schedule_applications` with one derived status label from `unassigned`, `draft_assigned`, or `confirmed_assigned`.
    - Test 3: a drafted or confirmed role assignment is surfaced as `assignedRoleSlotId` and `assignedRoleCode` for the applicant.
    - Test 4: unknown schedule ids return `null`.
  </behavior>
  <action>Implement `src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts` as the admin read model required by D-01, D-02, and D-03. Use the admin Supabase client and a schedule-centered nested select that joins `schedules`, `schedule_role_slots`, `schedule_applications`, and `schedule_assignments`, then map the result into `src/queries/assignment/types/adminScheduleAssignmentDetail.ts`. The DTO must contain a `schedule` object, a `roleSlots` array, and an `applicants` array. Derive applicant status labels in query-side mapping code, not in React components, and use the exact three labels from the research contract: `unassigned`, `draft_assigned`, `confirmed_assigned`. Cache the read under assignment-detail tags, not a broad path revalidation.</action>
  <acceptance_criteria>The admin detail query returns one schedule-centered DTO with applicants, slot fill counts, and derived applicant state labels that a detail page can render without extra data shaping.</acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts</automated>
  </verify>
  <done>APPL-03 has a concrete read model that supports schedule-detail applicant review and later admin UI work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the whole-schedule draft-save mutation</name>
  <read_first>
    .planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
    .planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
    src/mutations/schedule/actions/createSchedule.ts
    src/mutations/application/actions/createScheduleApplication.ts
    src/queries/access/dal/requireAdminUser.ts
    src/mutations/assignment/schemas/saveScheduleAssignmentDraft.test.ts
    src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts
    src/mutations/assignment/dal/assignmentDal.test.ts
  </read_first>
  <files>
    src/mutations/assignment/schemas/saveScheduleAssignmentDraft.ts
    src/mutations/assignment/schemas/saveScheduleAssignmentDraft.test.ts
    src/mutations/assignment/dal/assignmentDal.ts
    src/mutations/assignment/dal/assignmentDal.test.ts
    src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts
    src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts
    src/mutations/assignment/actions/submitScheduleAssignmentDraft.ts
  </files>
  <behavior>
    - Test 1: valid whole-schedule input with `scheduleId` and `assignments[{scheduleRoleSlotId, workerUserId}]` replaces all current draft rows for that schedule and returns the saved draft rows.
    - Test 2: duplicate `workerUserId` values in one payload are rejected before persistence.
    - Test 3: slot counts above the `headcount` for a given `schedule_role_slot_id` are rejected.
    - Test 4: `workerUserId` values not present in `schedule_applications` for the target schedule are rejected.
    - Test 5: non-admin callers throw `FORBIDDEN`.
  </behavior>
  <action>Implement the whole-schedule draft-save path recommended in research and required by D-04 through D-09. Put parsing in `src/mutations/assignment/schemas/saveScheduleAssignmentDraft.ts` and keep exact payload keys `scheduleId` and `assignments`. Put transactional write logic in `src/mutations/assignment/dal/assignmentDal.ts`; the DAL must delete existing `draft` rows for the target `schedule_id`, validate slot capacity and application membership, insert the replacement draft rows, and leave `confirmed` rows untouched. Add `src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts` as the admin-only server action and `src/mutations/assignment/actions/submitScheduleAssignmentDraft.ts` as the `FormData -> Promise<void>` wrapper. Revalidate assignment detail tags and the admin schedule list tag after a successful save. Do not confirm assignments here and do not change `schedules.status` to `confirmed` in this task.</action>
  <acceptance_criteria>Admins can save a role-slot assignment draft for one schedule in one operation; invalid payloads are rejected before partial writes; the worker-facing surface remains unchanged because only draft rows are written.</acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/mutations/assignment/schemas/saveScheduleAssignmentDraft.test.ts src/mutations/assignment/dal/assignmentDal.test.ts src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts</automated>
  </verify>
  <done>ASGN-01 is implemented with a transactional draft-save path that matches the schedule-level application model and draft-versus-confirm split.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/shared/model/assignment.test.ts src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts src/mutations/assignment/schemas/saveScheduleAssignmentDraft.test.ts src/mutations/assignment/dal/assignmentDal.test.ts src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts`.
Confirm the migration enforces one-worker-per-schedule uniqueness, the admin detail query returns the exact three applicant state labels, and the draft-save mutation does not expose draft rows to worker reads.
</verification>

<success_criteria>
The codebase has a complete Phase 3 foundation: assignment persistence exists, admins can read one schedule-centered applicant review model, admins can save draft role-slot assignments without confirming them, and worker confirmed-assignment reads have a safe DB contract to build on.
</success_criteria>

<output>
After completion, create `.planning/phases/03-assignment-and-pay-preview/03-assignment-and-pay-preview-01-SUMMARY.md`
</output>
