---
phase: 03-assignment-and-pay-preview
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/queries/assignment/types/workerAssignmentPreview.ts
  - src/queries/assignment/utils/calculatePayPreview.ts
  - src/queries/assignment/utils/calculatePayPreview.test.ts
  - src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
  - src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
  - src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
  - src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
  - src/flows/worker-assignment-preview/components/ConfirmedAssignmentList.tsx
  - src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx
  - src/app/worker/assignments/page.tsx
  - src/flows/worker-shell/components/WorkerShellPage.tsx
autonomous: true
requirements:
  - ASGN-03
  - PAY-02
  - PAY-03
  - PAY-04
must_haves:
  truths:
    - "A worker can open a confirmed-work page and see only assignments that have been explicitly confirmed."
    - "The page shows one total expected pay amount plus the calculation basis summary for confirmed work."
    - "Any schedule longer than 9 hours applies the overtime premium at 1.5x for the hours above 9."
  artifacts:
    - path: src/queries/assignment/utils/calculatePayPreview.ts
      provides: "Canonical pay formula used for confirmed assignment preview"
    - path: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      provides: "Worker-scoped confirmed-assignment read model"
    - path: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      provides: "Worker-facing confirmed-work and pay-preview flow"
    - path: src/app/worker/assignments/page.tsx
      provides: "Thin route for worker confirmed assignments"
  key_links:
    - from: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      to: src/queries/assignment/utils/calculatePayPreview.ts
      via: "each confirmed assignment row gets one derived pay breakdown"
      pattern: "calculatePayPreview"
    - from: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      to: supabase/migrations/20260402_phase3_assignment_and_pay_preview.sql
      via: "session-bound worker query relies on the Phase 3 direct RLS-backed base-table contract for confirmed assignments, confirmed schedules, confirmed role slots, and self worker_rates"
      pattern: "schedule_assignments|schedules|schedule_role_slots|worker_rates"
    - from: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      to: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      via: "server-first worker page composition"
      pattern: "listConfirmedWorkerAssignments"
    - from: src/app/worker/assignments/page.tsx
      to: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      via: "thin route to one flow"
      pattern: "WorkerAssignmentPreviewPage"
---

<objective>
Build the worker-facing confirmed assignment and pay preview experience on top of the Phase 3 assignment foundation.

Purpose: satisfy ASGN-03 and PAY-02 through PAY-04 with one canonical confirmed-work read path and one canonical pay formula.
Output: pay preview utility, worker confirmed-assignment query, worker page flow, and route/test coverage.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
@.planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
@.planning/phases/03-assignment-and-pay-preview/03-UI-SPEC.md
@.planning/phases/03-assignment-and-pay-preview/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/shared/model/access.ts
@src/queries/access/dal/getCurrentUser.ts
@src/queries/worker-rate/dal/listWorkerRates.ts
@src/flows/worker-schedules/components/WorkerSchedulesPage.tsx

<interfaces>
From src/shared/model/access.ts:
```ts
export interface WorkerRateRecord {
  userId: string;
  hourlyRateCents: number;
  updatedBy: string;
  updatedAt: string;
}
```

From src/queries/access/dal/getCurrentUser.ts:
```ts
export interface CurrentUser {
  id: string;
  email: string;
  role: "admin" | "worker" | null;
}

export async function getCurrentUser(): Promise<CurrentUser | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the canonical confirmed-assignment pay preview read path</name>
  <read_first>
    .planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
    .planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
    src/shared/model/access.ts
    src/queries/access/dal/getCurrentUser.ts
    src/queries/worker-rate/dal/listWorkerRates.ts
    src/queries/assignment/utils/calculatePayPreview.test.ts
    src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
  </read_first>
  <files>
    src/queries/assignment/types/workerAssignmentPreview.ts
    src/queries/assignment/utils/calculatePayPreview.ts
    src/queries/assignment/utils/calculatePayPreview.test.ts
    src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
    src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
  </files>
  <behavior>
    - Test 1: `calculatePayPreview({ startsAt, endsAt, hourlyRateCents })` splits the first 9 hours into regular time and any remaining hours into overtime.
    - Test 2: overtime pay uses `hourlyRateCents * 1.5` for hours above 9 and reports `overtimeApplied: true`.
    - Test 3: `listConfirmedWorkerAssignments(workerUserId)` returns only `schedule_assignments.status = 'confirmed'` rows and never includes `draft` rows.
    - Test 4: each worker assignment result includes role, schedule window, hourly rate, regular hours, overtime hours, and total pay cents.
  </behavior>
  <action>Implement the canonical pay formula in `src/queries/assignment/utils/calculatePayPreview.ts` per D-10 through D-13. Use exact output keys `regularHours`, `overtimeHours`, `overtimeApplied`, `hourlyRateCents`, `regularPayCents`, `overtimePayCents`, and `totalPayCents`. Then implement `src/queries/assignment/dal/listConfirmedWorkerAssignments.ts` using the session-bound server Supabase client, not the admin client, so RLS remains the safety boundary established in Phase 2. Consume the direct RLS-backed base-table contract created in `03-01`: join only `schedule_assignments.status = 'confirmed'` rows to `schedules.status = 'confirmed'`, the matching `schedule_role_slots`, and the signed-in worker's own `worker_rates` row exposed by the Phase 3 policies. Do not introduce or depend on a view/RPC in this plan. Map each row into `src/queries/assignment/types/workerAssignmentPreview.ts` using `calculatePayPreview`. Do not duplicate the pay formula in SQL and TypeScript; keep one canonical TypeScript calculation path in this plan.</action>
  <acceptance_criteria>The worker read model returns confirmed work only, relies on the direct RLS-backed base-table contract from `03-01`, and includes one authoritative pay breakdown per assignment using the 9-hour, 1.5x overtime rule.</acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/queries/assignment/utils/calculatePayPreview.test.ts src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts</automated>
  </verify>
  <done>ASGN-03, PAY-02, and PAY-03 have a tested read/calculation foundation.</done>
</task>

<task type="auto">
  <name>Task 2: Build the worker confirmed-work and pay preview page</name>
  <read_first>
    .planning/phases/03-assignment-and-pay-preview/03-UI-SPEC.md
    src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
    src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
    src/flows/worker-shell/components/WorkerShellPage.tsx
  </read_first>
  <files>
    src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
    src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
    src/flows/worker-assignment-preview/components/ConfirmedAssignmentList.tsx
    src/flows/worker-assignment-preview/components/PayPreviewTotalCard.tsx
    src/app/worker/assignments/page.tsx
    src/flows/worker-shell/components/WorkerShellPage.tsx
  </files>
  <action>Create the thin route `src/app/worker/assignments/page.tsx` and the matching server-first flow `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx`. Read the signed-in worker with `getCurrentUser()`, guard for `role === "worker"`, then load the confirmed assignment preview query from Task 1. Follow the UI-SPEC order exactly: total pay card first, calculation basis second, confirmed assignments list third. `PayPreviewTotalCard.tsx` should show the single summed `totalPayCents` across confirmed assignments. `ConfirmedAssignmentList.tsx` should render one summary card per assignment with the four fixed facts from UI-SPEC: date/time, role, hourly rate, overtime applied yes/no. Add an empty state that uses the approved copy contract and never renders draft state. If `src/flows/worker-shell/components/WorkerShellPage.tsx` owns worker navigation, add a link to `/worker/assignments` there; do not change `/worker/schedules` to mix recruiting and confirmed work on the same route.</action>
  <acceptance_criteria>Workers can open a dedicated confirmed-work page, see a single total expected pay amount and its basis summary, and never see draft assignments or recruiting-only UI mixed into the screen.</acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx</automated>
  </verify>
  <done>PAY-04 is visible in the worker UI through a dedicated route that only exposes confirmed work and canonical pay breakdowns.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/queries/assignment/utils/calculatePayPreview.test.ts src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx`.
Confirm the worker route is `/worker/assignments`, draft rows never appear in the worker query, and overtime is applied only to hours above 9 at 1.5x.
</verification>

<success_criteria>
Workers can view confirmed assignments and trusted pay previews from one dedicated route backed by a single calculation path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-assignment-and-pay-preview/03-assignment-and-pay-preview-04-SUMMARY.md`
</output>
