---
phase: 03-assignment-and-pay-preview
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/mutations/assignment/schemas/confirmScheduleAssignments.ts
  - src/mutations/assignment/schemas/confirmScheduleAssignments.test.ts
  - src/mutations/assignment/dal/assignmentDal.ts
  - src/mutations/assignment/dal/assignmentDal.test.ts
  - src/mutations/assignment/actions/confirmScheduleAssignments.ts
  - src/mutations/assignment/actions/confirmScheduleAssignments.test.ts
  - src/mutations/assignment/actions/submitScheduleAssignmentConfirm.ts
  - src/mutations/schedule/schemas/updateScheduleStatus.ts
  - src/mutations/schedule/actions/updateScheduleStatus.ts
  - src/mutations/schedule/actions/updateScheduleStatus.test.ts
  - src/mutations/schedule/actions/submitScheduleStatus.ts
  - src/flows/admin-schedules/components/ScheduleStatusForm.tsx
  - src/flows/admin-schedules/components/ScheduleTable.tsx
autonomous: true
requirements:
  - ASGN-02
must_haves:
  truths:
    - "An admin must take a separate explicit confirm action before workers can see final assignments."
    - "Confirming assignments updates both assignment rows and the schedule status together."
    - "The older schedule-list status path cannot publish `confirmed` outside the explicit confirm mutation."
  artifacts:
    - path: src/mutations/assignment/actions/confirmScheduleAssignments.ts
      provides: "Dedicated confirm action separate from draft save"
    - path: src/mutations/assignment/dal/assignmentDal.ts
      provides: "Transactional confirm write path that freezes assignment rows and updates schedule status"
    - path: src/mutations/schedule/actions/updateScheduleStatus.ts
      provides: "Phase 2 status-path hardening so list-page changes cannot publish `confirmed`"
    - path: src/flows/admin-schedules/components/ScheduleStatusForm.tsx
      provides: "List-page control limited to lightweight non-confirm transitions"
  key_links:
    - from: src/mutations/assignment/actions/confirmScheduleAssignments.ts
      to: src/mutations/assignment/dal/assignmentDal.ts
      via: "one confirm action for both row-state promotion and schedule status update"
      pattern: "confirmScheduleAssignments"
    - from: src/mutations/assignment/actions/confirmScheduleAssignments.ts
      to: src/shared/config/cacheTags.ts
      via: "confirm publish revalidates admin detail plus worker confirmed-assignment/pay-preview tags so workers do not see stale data"
      pattern: "workerConfirmed|assignments"
    - from: src/mutations/schedule/actions/updateScheduleStatus.ts
      to: src/flows/admin-schedules/components/ScheduleStatusForm.tsx
      via: "list-page status control removes direct `confirmed` publication"
      pattern: "confirmed"
---

<objective>
Add the explicit backend confirm workflow and close the Phase 2 publication bypass.

Purpose: satisfy D-07 through D-09 and ASGN-02 by making `confirmScheduleAssignments` the only path that can publish final assignments and the `confirmed` schedule state.
Output: confirm schema/DAL/action, worker-facing cache invalidation, and list-page status-path hardening with tests.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
@.planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
@.planning/phases/03-assignment-and-pay-preview/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts
@src/mutations/assignment/dal/assignmentDal.ts
@src/mutations/schedule/actions/updateScheduleStatus.ts
@src/mutations/schedule/schemas/updateScheduleStatus.ts
@src/flows/admin-schedules/components/ScheduleStatusForm.tsx
@src/flows/admin-schedules/components/ScheduleTable.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the dedicated confirm mutation and cache publication path</name>
  <read_first>
    .planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
    .planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
    src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts
    src/mutations/assignment/dal/assignmentDal.ts
    src/mutations/assignment/dal/assignmentDal.test.ts
  </read_first>
  <files>
    src/mutations/assignment/schemas/confirmScheduleAssignments.ts
    src/mutations/assignment/schemas/confirmScheduleAssignments.test.ts
    src/mutations/assignment/dal/assignmentDal.ts
    src/mutations/assignment/dal/assignmentDal.test.ts
    src/mutations/assignment/actions/confirmScheduleAssignments.ts
    src/mutations/assignment/actions/confirmScheduleAssignments.test.ts
    src/mutations/assignment/actions/submitScheduleAssignmentConfirm.ts
  </files>
  <behavior>
    - Test 1: confirming a schedule with existing `draft` rows promotes those rows to `confirmed`, sets `confirmed_at`, sets `confirmed_by`, and returns the confirmed rows.
    - Test 2: the same transaction updates `schedules.status` to the exact string `confirmed`.
    - Test 3: non-admin callers throw `FORBIDDEN`.
    - Test 4: confirming a schedule with no draft rows throws a stable domain error such as `NO_DRAFT_ASSIGNMENTS`.
    - Test 5: unfilled slots are reported in the confirm summary but do not block confirmation, per the research recommendation unless implementation finds an explicit conflicting requirement.
    - Test 6: confirm revalidation includes worker confirmed-assignment/pay-preview tags so worker-facing reads refresh after publish.
  </behavior>
  <action>Implement `src/mutations/assignment/actions/confirmScheduleAssignments.ts` as the dedicated confirm entry point required by D-07, D-08, D-09, and ASGN-02. Parse exactly `scheduleId` in `src/mutations/assignment/schemas/confirmScheduleAssignments.ts`, then call a transactional DAL helper in `src/mutations/assignment/dal/assignmentDal.ts` that promotes all target-schedule `draft` rows to `confirmed`, stamps `confirmed_at` and `confirmed_by`, and updates the matching `schedules.status` row to `confirmed` in the same write path. Add `src/mutations/assignment/actions/submitScheduleAssignmentConfirm.ts` as the `FormData` wrapper and revalidate the assignment detail tag, the admin schedule list tag, and the worker-facing confirmed-assignment/pay-preview tags introduced in 03-01 so ASGN-02, ASGN-03, and PAY-04 cannot stay stale after confirm. Do not add reopen, rollback, or edit-after-confirm behavior in this plan.</action>
  <acceptance_criteria>The confirm workflow is distinct from draft save, runs transactionally, and is the only path that makes assignment rows worker-visible.</acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/mutations/assignment/schemas/confirmScheduleAssignments.test.ts src/mutations/assignment/dal/assignmentDal.test.ts src/mutations/assignment/actions/confirmScheduleAssignments.test.ts</automated>
  </verify>
  <done>The confirm mutation exists as its own backend workflow and publishes worker-visible confirmed data through tag-based invalidation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden the Phase 2 schedule status path so only explicit confirm can publish `confirmed`</name>
  <read_first>
    .planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
    .planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
    src/mutations/schedule/actions/updateScheduleStatus.ts
    src/mutations/schedule/schemas/updateScheduleStatus.ts
    src/flows/admin-schedules/components/ScheduleStatusForm.tsx
    src/flows/admin-schedules/components/ScheduleTable.tsx
  </read_first>
  <files>
    src/mutations/schedule/schemas/updateScheduleStatus.ts
    src/mutations/schedule/actions/updateScheduleStatus.ts
    src/mutations/schedule/actions/updateScheduleStatus.test.ts
    src/mutations/schedule/actions/submitScheduleStatus.ts
    src/flows/admin-schedules/components/ScheduleStatusForm.tsx
    src/flows/admin-schedules/components/ScheduleTable.tsx
  </files>
  <behavior>
    - Test 1: the admin schedule-list status mutation rejects or hides direct `confirmed` transitions so only `confirmScheduleAssignments` can publish final assignments.
    - Test 2: list-page status changes for `recruiting` and `assigning` continue to work.
    - Test 3: the list UI no longer presents `confirmed` as an inline publication action.
  </behavior>
  <action>Close the Phase 2 bypass described in research by constraining `src/mutations/schedule/actions/updateScheduleStatus.ts`, `src/mutations/schedule/schemas/updateScheduleStatus.ts`, `src/mutations/schedule/actions/submitScheduleStatus.ts`, `src/flows/admin-schedules/components/ScheduleStatusForm.tsx`, and `src/flows/admin-schedules/components/ScheduleTable.tsx` so the admin schedule list can no longer set `status = "confirmed"` directly. Keep lightweight list-page status changes for `recruiting` and `assigning`, but require `confirmScheduleAssignments` as the only publication path to final `confirmed` state. Keep this task backend-driven: it should harden behavior and remove the bypass, not introduce the new schedule-detail UI.</action>
  <acceptance_criteria>The list-page status path still supports lightweight operational transitions, but it cannot publish final assignments or final schedule state on its own.</acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/mutations/schedule/actions/updateScheduleStatus.test.ts</automated>
  </verify>
  <done>The older Phase 2 status path is hardened so explicit confirm is the sole route to `confirmed` publication.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/mutations/assignment/schemas/confirmScheduleAssignments.test.ts src/mutations/assignment/dal/assignmentDal.test.ts src/mutations/assignment/actions/confirmScheduleAssignments.test.ts src/mutations/schedule/actions/updateScheduleStatus.test.ts`.
Confirm that draft save and final confirm stay separate, `confirmed` is published only through `confirmScheduleAssignments`, and worker-facing confirmed/pay tags refresh after publish.
</verification>

<success_criteria>
The backend publication rules are explicit and safe: confirming assignments is separate from draft save, transactional, cache-aware, and the old list-page `confirmed` shortcut is gone.
</success_criteria>

<output>
After completion, create `.planning/phases/03-assignment-and-pay-preview/03-assignment-and-pay-preview-02-SUMMARY.md`
</output>
