---
phase: 03-assignment-and-pay-preview
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
  - src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
  - src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx
  - src/flows/admin-schedule-assignment/components/AssignmentSummaryCard.tsx
  - src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx
  - src/app/admin/schedules/[scheduleId]/page.tsx
  - src/flows/admin-schedules/components/ScheduleTable.tsx
autonomous: true
requirements:
  - APPL-03
  - ASGN-01
  - ASGN-02
must_haves:
  truths:
    - "An admin can open a schedule detail page, review applicants by slot, and save draft assignment changes without leaving the page."
    - "The admin UI keeps draft save and final confirm as two separate actions."
    - "The schedule list exposes a clear entry into the schedule-detail assignment workflow."
  artifacts:
    - path: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
      provides: "Admin schedule detail flow for applicant review, draft save, and confirm entry"
    - path: src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx
      provides: "Final confirm UX that summarizes filled slots, unfilled slots, and worker-visible consequences"
    - path: src/app/admin/schedules/[scheduleId]/page.tsx
      provides: "Thin schedule-detail route for the admin assignment page"
    - path: src/flows/admin-schedules/components/ScheduleTable.tsx
      provides: "Entry link from the admin schedule list into the detail workflow"
  key_links:
    - from: src/app/admin/schedules/[scheduleId]/page.tsx
      to: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
      via: "thin route to one flow per D-16"
      pattern: "AdminScheduleAssignmentPage"
    - from: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
      to: src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
      via: "server-first detail read before rendering controls"
      pattern: "getAdminScheduleAssignmentDetail"
    - from: src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
      to: src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts
      via: "in-place draft save posts through the submit wrapper into the Phase 3 draft-save action without redirecting away from the page"
      pattern: "submitScheduleAssignmentDraft|saveScheduleAssignmentDraft"
    - from: src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx
      to: src/mutations/assignment/actions/submitScheduleAssignmentConfirm.ts
      via: "final confirm UX posts into the dedicated backend confirm action from 03-02"
      pattern: "submitScheduleAssignmentConfirm"
---

<objective>
Build the admin schedule-detail route and interface on top of the Phase 3 draft-save and confirm backends.

Purpose: satisfy the admin-facing ASGN-02 workflow with a schedule-detail page that keeps in-place draft save and explicit confirm separate.
Output: schedule-detail route, admin assignment UI components, detail-page wiring, and UI tests.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-assignment-and-pay-preview/03-CONTEXT.md
@.planning/phases/03-assignment-and-pay-preview/03-RESEARCH.md
@.planning/phases/03-assignment-and-pay-preview/03-UI-SPEC.md
@.planning/phases/03-assignment-and-pay-preview/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
@src/queries/assignment/types/adminScheduleAssignmentDetail.ts
@src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts
@src/mutations/assignment/actions/submitScheduleAssignmentDraft.ts
@src/mutations/assignment/actions/confirmScheduleAssignments.ts
@src/mutations/assignment/actions/submitScheduleAssignmentConfirm.ts
@src/flows/admin-schedules/components/ScheduleTable.tsx
@src/app/admin/schedules/page.tsx

<interfaces>
From src/queries/assignment/types/adminScheduleAssignmentDetail.ts:
```ts
export interface AdminScheduleAssignmentDetail {
  schedule: {
    id: string;
    startsAt: string;
    endsAt: string;
    status: "recruiting" | "assigning" | "confirmed";
  };
  roleSlots: Array<{
    id: string;
    roleCode: string;
    headcount: number;
    assignedCount: number;
  }>;
  applicants: Array<{
    workerUserId: string;
    workerName: string | null;
    appliedAt: string;
    assignmentStatus: "unassigned" | "draft_assigned" | "confirmed_assigned";
    assignedRoleSlotId: string | null;
    assignedRoleCode: string | null;
  }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the admin schedule-detail route and draft-save page flow</name>
  <read_first>
    .planning/phases/03-assignment-and-pay-preview/03-UI-SPEC.md
    src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
    src/mutations/assignment/actions/saveScheduleAssignmentDraft.ts
    src/mutations/assignment/actions/submitScheduleAssignmentDraft.ts
    src/flows/admin-schedules/components/ScheduleTable.tsx
  </read_first>
  <files>
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
    src/flows/admin-schedule-assignment/components/ApplicantAssignmentPanel.tsx
    src/flows/admin-schedule-assignment/components/AssignmentSummaryCard.tsx
    src/app/admin/schedules/[scheduleId]/page.tsx
    src/flows/admin-schedules/components/ScheduleTable.tsx
  </files>
  <action>Create the thin route `src/app/admin/schedules/[scheduleId]/page.tsx` and the matching flow `src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx` per D-14 through D-17. Use the UI contract exactly: two-panel desktop layout, single-column mobile stack, slot fill summary first, applicant assignment controls second, sticky action area third. `ApplicantAssignmentPanel.tsx` should render applicants and slot selectors using the existing detail DTO; `AssignmentSummaryCard.tsx` should show filled and unfilled counts by role slot. Wire in-place draft saving from `AdminScheduleAssignmentPage.tsx` through the existing `submitScheduleAssignmentDraft` wrapper into `saveScheduleAssignmentDraft` so edits save without navigation, preserve the current page state, and surface inline success/error feedback near the sticky action area. Modify `src/flows/admin-schedules/components/ScheduleTable.tsx` so each row exposes a clear detail entry linking to `/admin/schedules/[scheduleId]`.</action>
  <acceptance_criteria>The admin can navigate from the schedule list to a detail page, review applicants per schedule, and save a draft in place without leaving the screen.</acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx</automated>
  </verify>
  <done>The schedule-detail admin flow exists, uses the schedule-centered read model, and keeps draft editing in place.</done>
</task>

<task type="auto">
  <name>Task 2: Add the separate confirm UX on the admin schedule-detail page</name>
  <read_first>
    .planning/phases/03-assignment-and-pay-preview/03-UI-SPEC.md
    src/mutations/assignment/actions/confirmScheduleAssignments.ts
    src/mutations/assignment/actions/submitScheduleAssignmentConfirm.ts
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
  </read_first>
  <files>
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx
    src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx
  </files>
  <action>Create `src/flows/admin-schedule-assignment/components/ConfirmAssignmentsDialog.tsx` and wire it into the sticky action area in `AdminScheduleAssignmentPage.tsx`. The dialog must summarize filled slots, unfilled slots, and worker-visible consequences before posting to `submitScheduleAssignmentConfirm.ts`, and it must stay separate from draft-save controls per D-07 through D-09. Use the approved applicant labels and copy contract from `03-UI-SPEC.md` exactly. Keep this plan UI-only: consume the explicit confirm backend from 03-02 rather than re-implementing confirm rules in React.</action>
  <acceptance_criteria>The admin page exposes a separate final-confirm dialog that calls the dedicated confirm backend and does not collapse draft save into the same action.</acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/mutations/assignment/actions/confirmScheduleAssignments.test.ts src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx</automated>
  </verify>
  <done>The admin assignment workflow is usable end-to-end from the schedule list into the detail page, with draft save and confirm kept as distinct UI actions.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/mutations/assignment/actions/saveScheduleAssignmentDraft.test.ts src/mutations/assignment/actions/confirmScheduleAssignments.test.ts src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.test.tsx`.
Confirm the admin page follows the approved two-panel hierarchy, draft save stays in place, the schedule list links into `/admin/schedules/[scheduleId]`, and final publication happens only through the separate confirm dialog.
</verification>

<success_criteria>
Admins can enter a schedule-detail workflow, manage slot assignments in place, and use a separate confirm dialog backed by the dedicated confirm mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-assignment-and-pay-preview/03-assignment-and-pay-preview-03-SUMMARY.md`
</output>
