---
phase: 07-application-admin-freshness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mutations/application/actions/submitScheduleApplication.test.ts
autonomous: true
requirements:
  - APPL-02
  - APPL-03
  - DASH-02
must_haves:
  truths:
    - "Phase 07 has a failing-first regression that proves a successful worker application must refresh the admin schedule-detail cache for the affected schedule."
    - "Phase 07 has a failing-first regression that proves a successful worker application must refresh the admin operations dashboard cache."
    - "The regression also proves duplicate or no-op application submits do not churn cache tags as if a write happened."
  artifacts:
    - path: src/mutations/application/actions/submitScheduleApplication.test.ts
      provides: "Wave 0 freshness regression coverage for worker submit success-path invalidation and duplicate no-op behavior."
  key_links:
    - from: src/mutations/application/actions/submitScheduleApplication.test.ts
      to: src/mutations/application/actions/submitScheduleApplication.ts
      via: "the new regression imports the real submit wrapper action and asserts its tag invalidation behavior"
      pattern: "#mutations/application/actions/submitScheduleApplication"
    - from: src/mutations/application/actions/submitScheduleApplication.test.ts
      to: src/shared/config/cacheTags.ts
      via: "the regression asserts exact schedule-detail and dashboard cache tag names"
      pattern: "assignments:detail:|dashboard:admin-operations|applications:worker-schedule-ids"
---

<objective>
Create the explicit Wave 0 freshness regression for Phase 07 before implementation changes land.

Purpose: make the admin-freshness gap executable by proving the worker application submit wrapper is currently missing admin-facing invalidation and by proving duplicate/no-op submits should not revalidate as if a write happened.
Output: a colocated Vitest file for `submitScheduleApplication` and a failing-first verification run.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-application-admin-freshness/07-CONTEXT.md
@.planning/phases/07-application-admin-freshness/07-RESEARCH.md
@.planning/phases/07-application-admin-freshness/07-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/mutations/application/actions/submitScheduleApplication.ts
@src/mutations/application/actions/createScheduleApplication.ts
@src/mutations/application/actions/createScheduleApplication.test.ts
@src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts
@src/mutations/assignment/actions/confirmScheduleAssignments.test.ts
@src/shared/config/cacheTags.ts
@vitest.config.ts

<interfaces>
From src/mutations/application/actions/submitScheduleApplication.ts:
```ts
export async function submitScheduleApplication(formData: FormData) {
  await createScheduleApplication(parseSubmitScheduleApplicationFormData(formData));
  revalidateTag(cacheTags.applications.all, "max");
  revalidateTag(cacheTags.applications.workerScheduleIds, "max");
  revalidateTag(cacheTags.schedules.recruitingList, "max");
}
```

From src/shared/config/cacheTags.ts:
```ts
applications: {
  all: "applications",
  workerScheduleIds: "applications:worker-schedule-ids",
},
assignments: {
  detail: (scheduleId: string) => `assignments:detail:${scheduleId}`,
},
dashboard: {
  all: "dashboard",
  adminOperations: "dashboard:admin-operations",
},
```

From src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts:
```ts
const revalidateTag = vi.fn();

vi.mock("next/cache", () => ({
  revalidateTag,
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing-first worker-application freshness regression file</name>
  <read_first>
    .planning/phases/07-application-admin-freshness/07-VALIDATION.md
    .planning/phases/07-application-admin-freshness/07-RESEARCH.md
    src/mutations/application/actions/submitScheduleApplication.ts
    src/mutations/application/actions/createScheduleApplication.ts
    src/mutations/application/actions/createScheduleApplication.test.ts
    src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts
    src/mutations/assignment/actions/confirmScheduleAssignments.test.ts
    src/shared/config/cacheTags.ts
    vitest.config.ts
  </read_first>
  <files>
    src/mutations/application/actions/submitScheduleApplication.test.ts
  </files>
  <behavior>
    - Test 1: when `createScheduleApplication` resolves to `{ status: "applied", application: ... }`, the submit wrapper revalidates `cacheTags.applications.all`, `cacheTags.applications.workerScheduleIds`, `cacheTags.schedules.recruitingList`, `cacheTags.assignments.detail("schedule-1")`, `cacheTags.dashboard.all`, and `cacheTags.dashboard.adminOperations`.
    - Test 2: when `createScheduleApplication` resolves to `{ status: "already_applied" }`, the submit wrapper does not call `revalidateTag` at all.
    - Test 3: the submit wrapper returns the underlying `createScheduleApplication` result so callers can distinguish `applied` from `already_applied`.
  </behavior>
  <action>Create `src/mutations/application/actions/submitScheduleApplication.test.ts` as the explicit Wave 0 regression file for Phase 07. Follow the mocking pattern already used in `src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts` and `src/mutations/assignment/actions/confirmScheduleAssignments.test.ts`: mock `next/cache` and `#mutations/application/actions/createScheduleApplication` with `vi.fn()`. Build a real `FormData` instance containing `scheduleId = "schedule-1"`. Add one success-path test that expects the exact tag calls `applications`, `applications:worker-schedule-ids`, `schedules:recruiting-list`, `assignments:detail:schedule-1`, `dashboard`, and `dashboard:admin-operations`, and expects the returned value to equal the mocked `applied` result. Add one duplicate/no-op test that expects the returned value to equal `{ status: "already_applied" }` and asserts `revalidateTag` was never called. Keep imports on `#` aliases only and do not add route-level or UI-level assertions in this file.</action>
  <acceptance_criteria>
    - `src/mutations/application/actions/submitScheduleApplication.test.ts` exists.
    - The test file mocks `next/cache`.
    - The test file mocks `#mutations/application/actions/createScheduleApplication`.
    - The test file contains the exact string `assignments:detail:schedule-1`.
    - The test file contains the exact string `dashboard:admin-operations`.
    - The test file contains the exact string `already_applied`.
    - The test file asserts `revalidateTag` is not called in the duplicate/no-op case.
  </acceptance_criteria>
  <verify>
    <automated>$env:CI='1'; pnpm test -- src/mutations/application/actions/submitScheduleApplication.test.ts; if ($LASTEXITCODE -eq 0) { throw 'Expected Phase 07 Wave 0 regression to fail before submitScheduleApplication is updated.' }</automated>
  </verify>
  <done>The new worker-application freshness regression file exists and fails against the current implementation, making the Phase 07 freshness gap executable.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/mutations/application/actions/submitScheduleApplication.test.ts` and confirm the command fails because `submitScheduleApplication.ts` does not yet invalidate the admin schedule-detail and dashboard tags and does not gate invalidation to the real write path.
</verification>

<success_criteria>
Phase 07 has a concrete failing-first regression for worker-application freshness, with exact cache-tag expectations and duplicate/no-op behavior encoded in an automated test.
</success_criteria>

<output>
After completion, create `.planning/phases/07-application-admin-freshness/07-application-admin-freshness-01-SUMMARY.md`
</output>
