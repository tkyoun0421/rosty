---
phase: 07-application-admin-freshness
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/mutations/application/actions/submitScheduleApplication.ts
  - src/mutations/application/actions/submitScheduleApplication.test.ts
autonomous: true
requirements:
  - APPL-02
  - APPL-03
  - DASH-02
must_haves:
  truths:
    - "A successful worker application refreshes the worker recruiting/apply reads plus the affected admin schedule-detail read on the next render."
    - "A successful worker application refreshes the admin operations dashboard on the next render."
    - "Duplicate/no-op application submits do not invalidate caches as if a new application row was written."
  artifacts:
    - path: src/mutations/application/actions/submitScheduleApplication.ts
      provides: "Success-gated application submit orchestration with explicit worker and admin cache-tag invalidation."
    - path: src/mutations/application/actions/submitScheduleApplication.test.ts
      provides: "Regression coverage that keeps the Phase 07 freshness contract from drifting."
  key_links:
    - from: src/mutations/application/actions/submitScheduleApplication.ts
      to: src/mutations/application/actions/createScheduleApplication.ts
      via: "the submit wrapper preserves the domain write and uses its returned status to decide invalidation"
      pattern: "const result = await createScheduleApplication\\(parsed\\)"
    - from: src/mutations/application/actions/submitScheduleApplication.ts
      to: src/shared/config/cacheTags.ts
      via: "the wrapper revalidates the exact admin detail and dashboard tags required by the phase"
      pattern: "cacheTags\\.assignments\\.detail\\(parsed\\.scheduleId\\)|cacheTags\\.dashboard\\.(all|adminOperations)"
    - from: src/mutations/application/actions/submitScheduleApplication.ts
      to: src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
      via: "schedule-detail freshness is delivered by the existing schedule-scoped assignment-detail cache tag"
      pattern: "assignments:detail:"
---

<objective>
Implement the Phase 07 freshness fix so the Wave 0 regression passes without widening scope.

Purpose: close the admin-freshness gap by updating the worker application submit wrapper to revalidate the existing admin schedule-detail and dashboard cache tags only when a real application write occurs.
Output: an updated `submitScheduleApplication` action, a passing regression test, and supporting verification against the existing admin read-model tests.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-application-admin-freshness/07-CONTEXT.md
@.planning/phases/07-application-admin-freshness/07-RESEARCH.md
@.planning/phases/07-application-admin-freshness/07-VALIDATION.md
@.planning/phases/07-application-admin-freshness/07-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/mutations/application/actions/submitScheduleApplication.ts
@src/mutations/application/actions/submitScheduleApplication.test.ts
@src/mutations/application/actions/createScheduleApplication.ts
@src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
@src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts
@src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts
@src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts
@src/shared/config/cacheTags.ts

<interfaces>
From src/mutations/application/actions/createScheduleApplication.ts:
```ts
export type CreateScheduleApplicationResult =
  | {
      status: "applied";
      application: ScheduleApplicationRecord;
    }
  | {
      status: "already_applied";
    };
```

From src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts:
```ts
const cachedQuery = unstable_cache(
  async () => await runGetAdminScheduleAssignmentDetail(scheduleId, { useAdminClient: true }),
  ["queries:assignment:getAdminScheduleAssignmentDetail", scheduleId],
  {
    tags: [cacheTags.assignments.all, cacheTags.assignments.detail(scheduleId)],
  },
);
```

From src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts:
```ts
{
  tags: [cacheTags.dashboard.all, cacheTags.dashboard.adminOperations],
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement success-gated admin freshness invalidation in the submit wrapper</name>
  <read_first>
    src/mutations/application/actions/submitScheduleApplication.test.ts
    .planning/phases/07-application-admin-freshness/07-RESEARCH.md
    src/mutations/application/actions/submitScheduleApplication.ts
    src/mutations/application/actions/createScheduleApplication.ts
    src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
    src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts
    src/shared/config/cacheTags.ts
  </read_first>
  <files>
    src/mutations/application/actions/submitScheduleApplication.ts
    src/mutations/application/actions/submitScheduleApplication.test.ts
  </files>
  <behavior>
    - Test 1: the submit wrapper parses once into `const parsed = parseSubmitScheduleApplicationFormData(formData)` and passes `parsed` into `createScheduleApplication`.
    - Test 2: when `result.status === "applied"`, the wrapper revalidates the worker tags `cacheTags.applications.all`, `cacheTags.applications.workerScheduleIds`, `cacheTags.schedules.recruitingList` plus the admin tags `cacheTags.assignments.detail(parsed.scheduleId)`, `cacheTags.dashboard.all`, and `cacheTags.dashboard.adminOperations`.
    - Test 3: when `result.status === "already_applied"`, the wrapper returns the result and does not call `revalidateTag`.
    - Test 4: the implementation remains tag-based and does not introduce `revalidatePath`.
  </behavior>
  <action>Update `src/mutations/application/actions/submitScheduleApplication.ts` so it first assigns `const parsed = parseSubmitScheduleApplicationFormData(formData)`, then assigns `const result = await createScheduleApplication(parsed)`, then conditionally revalidates tags only inside `if (result.status === "applied")`. Inside that success branch, keep the existing worker-facing invalidation calls to `cacheTags.applications.all`, `cacheTags.applications.workerScheduleIds`, and `cacheTags.schedules.recruitingList`, and add `revalidateTag(cacheTags.assignments.detail(parsed.scheduleId), "max")`, `revalidateTag(cacheTags.dashboard.all, "max")`, and `revalidateTag(cacheTags.dashboard.adminOperations, "max")`. Return `result` from the action. Do not add `cacheTags.assignments.all`, do not add `revalidatePath`, do not touch the DAL or admin query files in this phase, and adjust `src/mutations/application/actions/submitScheduleApplication.test.ts` only as needed to make the regression exact and passing.</action>
  <acceptance_criteria>
    - `src/mutations/application/actions/submitScheduleApplication.ts` contains `const parsed = parseSubmitScheduleApplicationFormData(formData)`.
    - `src/mutations/application/actions/submitScheduleApplication.ts` contains `const result = await createScheduleApplication(parsed)`.
    - `src/mutations/application/actions/submitScheduleApplication.ts` contains `if (result.status === "applied")`.
    - `src/mutations/application/actions/submitScheduleApplication.ts` contains `cacheTags.assignments.detail(parsed.scheduleId)`.
    - `src/mutations/application/actions/submitScheduleApplication.ts` contains `cacheTags.dashboard.all`.
    - `src/mutations/application/actions/submitScheduleApplication.ts` contains `cacheTags.dashboard.adminOperations`.
    - `src/mutations/application/actions/submitScheduleApplication.ts` contains `return result`.
    - `src/mutations/application/actions/submitScheduleApplication.ts` does not contain `revalidatePath`.
    - `pnpm test -- src/mutations/application/actions/submitScheduleApplication.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/mutations/application/actions/submitScheduleApplication.test.ts</automated>
  </verify>
  <done>The worker application submit wrapper now refreshes the exact admin schedule-detail and dashboard caches after a real application write and leaves duplicate/no-op submits alone.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/mutations/application/actions/submitScheduleApplication.test.ts src/queries/assignment/dal/getAdminScheduleAssignmentDetail.test.ts src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts`.
Confirm a successful worker application now invalidates `assignments:detail:<scheduleId>`, `dashboard`, and `dashboard:admin-operations`, while duplicate/no-op submits do not trigger revalidation.
</verification>

<success_criteria>
Phase 07 closes the admin-freshness gap through explicit success-path tag invalidation in the worker application submit wrapper, with no new UI or read-model scope added.
</success_criteria>

<output>
After completion, create `.planning/phases/07-application-admin-freshness/07-application-admin-freshness-02-SUMMARY.md`
</output>
