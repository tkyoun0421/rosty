---
phase: 06-admin-invite-route-guard
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/flows/admin-invites/components/AdminInvitesPage.tsx
  - src/app/admin/invites/page.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
must_haves:
  truths:
    - "A signed-in worker cannot open `/admin/invites` and reach invite-management controls."
    - "An admin can still open `/admin/invites` and use the existing invite-management entry point."
    - "The page guard runs before privileged UI renders and the thin route still delegates to the flow."
  artifacts:
    - path: src/flows/admin-invites/components/AdminInvitesPage.tsx
      provides: "Guarded invite flow entry that awaits `requireAdminUser()` before rendering invite controls."
    - path: src/app/admin/invites/page.tsx
      provides: "Async thin route entry that returns the guarded invite flow."
  key_links:
    - from: src/flows/admin-invites/components/AdminInvitesPage.tsx
      to: src/queries/access/dal/requireAdminUser.ts
      via: "the invite flow executes the strict DB-backed admin gate before any form JSX"
      pattern: "await requireAdminUser\\(\\)"
    - from: src/app/admin/invites/page.tsx
      to: src/flows/admin-invites/components/AdminInvitesPage.tsx
      via: "the route remains declarative and returns the guarded flow directly"
      pattern: "return await AdminInvitesPage\\(\\)"
    - from: src/flows/admin-invites/components/AdminInvitesPage.tsx
      to: src/mutations/invite/actions/createWorkerInvite.ts
      via: "the existing invite form stays wired to the current action after the guard succeeds"
      pattern: "<form action=\\{createWorkerInvite\\}>"
---

<objective>
Implement the route-level invite guard so the failing regression from Plan 01 passes without widening the phase scope.

Purpose: Close AUTH-01 and AUTH-03 by reusing the repo’s established `requireAdminUser()` page-guard pattern while preserving the thin-route contract.
Output: A guarded `AdminInvitesPage`, an async thin route entry, and a passing invite-route regression test run.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-invite-route-guard/06-RESEARCH.md
@.planning/phases/06-admin-invite-route-guard/06-VALIDATION.md
@.planning/phases/06-admin-invite-route-guard/06-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/flows/admin-invites/components/AdminInvitesPage.test.tsx
@src/flows/admin-invites/components/AdminInvitesPage.tsx
@src/app/admin/invites/page.tsx
@src/queries/access/dal/requireAdminUser.ts
@src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
@src/app/admin/worker-rates/page.tsx

<interfaces>
From src/queries/access/dal/requireAdminUser.ts:
```ts
export async function requireAdminUser() {
  // throws Error("FORBIDDEN") for missing session or non-admin role
  return {
    id: user.id,
    email: user.email,
    role: "admin" as const,
  };
}
```

From src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx:
```tsx
export async function AdminOperationsDashboardPage() {
  try {
    await requireAdminUser();
  } catch {
    return <main>Admin access required.</main>;
  }
```

From src/app/admin/worker-rates/page.tsx:
```tsx
export default async function AdminWorkerRatesRoute() {
  return await AdminWorkerRatesPage();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard the invite flow with `requireAdminUser()` and keep the route thin</name>
  <read_first>
    src/flows/admin-invites/components/AdminInvitesPage.test.tsx
    .planning/phases/06-admin-invite-route-guard/06-RESEARCH.md
    src/flows/admin-invites/components/AdminInvitesPage.tsx
    src/app/admin/invites/page.tsx
    src/queries/access/dal/requireAdminUser.ts
    src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
    src/app/admin/worker-rates/page.tsx
  </read_first>
  <files>
    src/flows/admin-invites/components/AdminInvitesPage.tsx
    src/app/admin/invites/page.tsx
  </files>
  <behavior>
    - Test 1: `AdminInvitesPage` awaits `requireAdminUser()` before returning any privileged invite UI.
    - Test 2: denied access returns the exact copy `Admin access required.` and hides the invite CTA.
    - Test 3: the route file stays declarative and returns `await AdminInvitesPage()` instead of embedding auth logic in `src/app`.
  </behavior>
  <action>Update `src/flows/admin-invites/components/AdminInvitesPage.tsx` so it matches the established admin-read guard pattern from `src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx`: import `requireAdminUser` from `#queries/access/dal/requireAdminUser`, convert `AdminInvitesPage` into an `async` server component, and call `await requireAdminUser()` before any invite-management JSX is returned. Wrap the guard in `try/catch` and return `<main>Admin access required.</main>` when the guard throws so worker sessions and anonymous sessions do not reach the form. Keep the existing heading and keep `<form action={createWorkerInvite}>` wired to the current invite action; do not widen this phase into a copy rewrite or mutation-auth refactor. Update `src/app/admin/invites/page.tsx` to follow the repo thin-route pattern shown by `src/app/admin/worker-rates/page.tsx`: make the route component `async` and return `await AdminInvitesPage()`. Do not add `requireAdminUser` logic to `src/app`, do not change `proxy.ts`, and do not introduce `forbidden()`, `forbidden.js`, or redirect-based handling in this phase.</action>
  <acceptance_criteria>
    - `src/flows/admin-invites/components/AdminInvitesPage.tsx` imports `requireAdminUser` from `#queries/access/dal/requireAdminUser`.
    - `src/flows/admin-invites/components/AdminInvitesPage.tsx` contains `await requireAdminUser()`.
    - `src/flows/admin-invites/components/AdminInvitesPage.tsx` returns the exact string `Admin access required.` in the denied branch.
    - `src/flows/admin-invites/components/AdminInvitesPage.tsx` still contains `<form action={createWorkerInvite}>`.
    - `src/app/admin/invites/page.tsx` exports an async default route and contains `return await AdminInvitesPage()`.
    - `src/app/admin/invites/page.tsx` does not import `requireAdminUser`.
    - `pnpm exec vitest run src/flows/admin-invites/components/AdminInvitesPage.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/flows/admin-invites/components/AdminInvitesPage.test.tsx</automated>
  </verify>
  <done>The `/admin/invites` flow now blocks non-admin sessions before rendering privileged UI, preserves the existing invite action wiring, and passes the Wave 0 regression test.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/flows/admin-invites/components/AdminInvitesPage.test.tsx` and confirm the formerly failing guard regression now passes.
Run `pnpm test` after the plan completes to confirm the broader suite remains green.
</verification>

<success_criteria>
The failing-first invite-route regression from Plan 01 passes, `/admin/invites` is guarded by the repo’s strict admin-read pattern, and the route file remains thin.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-invite-route-guard/06-admin-invite-route-guard-02-SUMMARY.md`
</output>
