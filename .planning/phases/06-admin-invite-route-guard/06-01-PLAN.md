---
phase: 06-admin-invite-route-guard
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/flows/admin-invites/components/AdminInvitesPage.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
must_haves:
  truths:
    - "Phase 06 has a runnable regression test that describes denied access for non-admin sessions and allowed access for admins."
    - "The thin `/admin/invites` route is covered by an automated test before implementation changes begin."
    - "The new test fails against the current unguarded invite route so the auth gap is executable, not only described."
  artifacts:
    - path: src/flows/admin-invites/components/AdminInvitesPage.test.tsx
      provides: "Wave 0 failing-first regression coverage for admin allow, worker deny, and thin-route wiring."
  key_links:
    - from: src/flows/admin-invites/components/AdminInvitesPage.test.tsx
      to: src/app/admin/invites/page.tsx
      via: "route test imports the real thin route entry"
      pattern: "#app/admin/invites/page"
    - from: src/flows/admin-invites/components/AdminInvitesPage.test.tsx
      to: src/queries/access/dal/requireAdminUser.ts
      via: "test controls admin and non-admin outcomes by mocking the DB-backed guard"
      pattern: "mock\\(\"#queries/access/dal/requireAdminUser\""
---

<objective>
Create the explicit Wave 0 regression harness for the `/admin/invites` auth gap before any implementation work lands.

Purpose: Make Nyquist sequencing real for AUTH-01 and AUTH-03 by generating the invite-route regression test first and proving it fails against the current unguarded implementation.
Output: A colocated Vitest file for `AdminInvitesPage` and a failing-first verification run.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-invite-route-guard/06-RESEARCH.md
@.planning/phases/06-admin-invite-route-guard/06-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/app/admin/invites/page.tsx
@src/flows/admin-invites/components/AdminInvitesPage.tsx
@src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx
@vitest.config.ts

<interfaces>
From src/app/admin/invites/page.tsx:
```tsx
import { AdminInvitesPage } from "#flows/admin-invites/components/AdminInvitesPage";

export default function AdminInvitesRoute() {
  return <AdminInvitesPage />;
}
```

From src/flows/admin-invites/components/AdminInvitesPage.tsx:
```tsx
import { createWorkerInvite } from "#mutations/invite/actions/createWorkerInvite";

export function AdminInvitesPage() {
  return (
    <main>
      <form action={createWorkerInvite}>
```

From src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx:
```tsx
const requireAdminUser = vi.fn();

vi.mock("#queries/access/dal/requireAdminUser", () => ({
  requireAdminUser,
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing-first invite-route regression test file</name>
  <read_first>
    .planning/phases/06-admin-invite-route-guard/06-VALIDATION.md
    .planning/phases/06-admin-invite-route-guard/06-RESEARCH.md
    src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx
    src/flows/admin-invites/components/AdminInvitesPage.tsx
    src/app/admin/invites/page.tsx
    vitest.config.ts
  </read_first>
  <files>
    src/flows/admin-invites/components/AdminInvitesPage.test.tsx
  </files>
  <behavior>
    - Test 1: an admin session mocked as `{ id: "admin-1", email: "admin@example.com", role: "admin" }` renders the existing invite-management heading and invite CTA.
    - Test 2: a rejected `requireAdminUser` call with `new Error("FORBIDDEN")` renders the exact denied copy `Admin access required.` and does not render the invite CTA.
    - Test 3: rendering `await page.default()` from `#app/admin/invites/page` still calls `requireAdminUser` exactly once through the thin route.
  </behavior>
  <action>Create `src/flows/admin-invites/components/AdminInvitesPage.test.tsx` as the explicit Wave 0 file required by `06-VALIDATION.md`. Follow the async server-component test style already used in `src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx`: use `render(await ...)`, mock `#queries/access/dal/requireAdminUser` with `vi.fn()`, and keep imports on `#` aliases only. Include one admin-success test, one denied-access test using the exact string `Admin access required.`, and one thin-route wiring test that imports `#app/admin/invites/page`, renders `await page.default()`, and asserts the guard mock was called once. In the denied-access test, assert the invite submit button is absent so the test proves privileged controls do not leak.</action>
  <acceptance_criteria>
    - `src/flows/admin-invites/components/AdminInvitesPage.test.tsx` exists.
    - The test file mocks `#queries/access/dal/requireAdminUser`.
    - The test file contains the exact denied string `Admin access required.`.
    - The test file imports `#app/admin/invites/page`.
    - The denied-access test asserts the invite submit button is absent.
    - The route-wiring test asserts `requireAdminUser` is called exactly once.
  </acceptance_criteria>
  <verify>
    <automated>$env:CI='1'; pnpm exec vitest run src/flows/admin-invites/components/AdminInvitesPage.test.tsx; if ($LASTEXITCODE -eq 0) { throw 'Expected Phase 06 Wave 0 regression test to fail before the route guard is implemented.' }</automated>
  </verify>
  <done>The new invite-route regression file exists and fails against the current unguarded implementation, making the Phase 06 auth gap executable.</done>
</task>

</tasks>

<verification>
Run the invite-page Vitest file and confirm the command fails because the route is still unguarded before Plan 02 starts.
</verification>

<success_criteria>
Phase 06 has a concrete failing-first regression harness for the `/admin/invites` admin-guard gap, with no placeholder verification text.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-invite-route-guard/06-admin-invite-route-guard-01-SUMMARY.md`
</output>
