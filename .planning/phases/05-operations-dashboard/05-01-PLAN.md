---
phase: 05-operations-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/config/cacheTags.ts
  - src/queries/operations-dashboard/types/operationsDashboard.ts
  - src/queries/operations-dashboard/utils/operationsDashboard.ts
  - src/queries/operations-dashboard/utils/operationsDashboard.test.ts
  - src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts
  - src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts
autonomous: true
requirements:
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "Phase 05 can supply `Today` and `Upcoming` schedule cards for the configured operations window from one dashboard data source."
    - "Each schedule card supplied to the dashboard includes schedule context, exact local start-time display fields, and application, staffing, and attendance summary counts."
    - "The dashboard data source supplies the correct top anomaly using `unfilled slots > missing check-ins > lateness` and never surfaces missing check-ins before the attendance window opens."
  artifacts:
    - path: src/queries/operations-dashboard/types/operationsDashboard.ts
      provides: "Dashboard card, section, and anomaly contracts for the Phase 05 read model"
    - path: src/queries/operations-dashboard/utils/operationsDashboard.ts
      provides: "Pure dashboard window, grouping, and anomaly-priority helpers"
    - path: src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts
      provides: "One-query admin dashboard read model for schedules, staffing, applications, and attendance"
    - path: src/shared/config/cacheTags.ts
      provides: "Dedicated dashboard cache tags for `/admin` read invalidation"
  key_links:
    - from: src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts
      to: src/queries/operations-dashboard/utils/operationsDashboard.ts
      via: "DAL maps raw schedule rows into render-ready dashboard cards with top anomaly selection"
      pattern: "map.*OperationsDashboard|getTop.*Anomaly|group.*Dashboard"
    - from: src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts
      to: src/shared/config/cacheTags.ts
      via: "dashboard read model is cached with dedicated dashboard tags"
      pattern: "cacheTags\\.dashboard"
    - from: src/queries/operations-dashboard/utils/operationsDashboard.ts
      to: src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
      via: "dashboard missing-check-in timing mirrors the existing attendance open-window rule from Phase 4"
      pattern: "08:20|110|100|opensAt"
---

<objective>
Define the Phase 05 dashboard data contract before any `/admin` UI wiring depends on it.

Purpose: satisfy the read-model foundation for DASH-02 and DASH-03 by introducing explicit dashboard types, pure anomaly logic, cache tags, and a single aggregated admin query.
Output: dashboard query slice, utility tests, DAL tests, and reusable dashboard cache tags.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operations-dashboard/05-CONTEXT.md
@.planning/phases/05-operations-dashboard/05-RESEARCH.md
@.planning/phases/05-operations-dashboard/05-VALIDATION.md
@.planning/phases/05-operations-dashboard/05-UI-SPEC.md
@.planning/phases/04-attendance-check-in/04-CONTEXT.md
@.planning/phases/04-attendance-check-in/04-attendance-check-in-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
@src/shared/config/cacheTags.ts
@src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
@src/queries/assignment/types/adminScheduleAssignmentDetail.ts
@src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
@src/queries/attendance/types/adminScheduleAttendanceDetail.ts

<interfaces>
From src/shared/config/cacheTags.ts:
```ts
export const cacheTags = {
  schedules: {
    all: "schedules",
    adminList: "schedules:admin-list",
  },
  assignments: {
    all: "assignments",
    detail: (scheduleId: string) => `assignments:detail:${scheduleId}`,
  },
  attendance: {
    all: "attendance",
    scheduleDetail: (scheduleId: string) => `attendance:schedule:${scheduleId}`,
  },
} as const;
```

From src/queries/assignment/types/adminScheduleAssignmentDetail.ts:
```ts
export interface AdminScheduleAssignmentDetail {
  schedule: {
    id: string;
    startsAt: string;
    endsAt: string;
    status: ScheduleStatus;
  };
  roleSlots: Array<{
    id: string;
    roleCode: string;
    headcount: number;
    assignedCount: number;
  }>;
  applicants: Array<{
    workerUserId: string;
    workerName: string | null;
    appliedAt: string;
    assignmentStatus: AdminApplicantAssignmentStatus;
    assignedRoleSlotId: string | null;
    assignedRoleCode: string | null;
  }>;
}
```

From src/queries/attendance/types/adminScheduleAttendanceDetail.ts:
```ts
export interface AdminScheduleAttendanceDetail {
  schedule: {
    id: string;
    startsAt: string;
    endsAt: string;
    opensAt: string;
  };
  summary: {
    confirmedWorkerCount: number;
    checkedInCount: number;
    lateCount: number;
    notCheckedInCount: number;
    notOpenYetCount: number;
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the dashboard card contract and pure anomaly helpers</name>
  <read_first>
    .planning/phases/05-operations-dashboard/05-CONTEXT.md
    .planning/phases/05-operations-dashboard/05-RESEARCH.md
    .planning/phases/05-operations-dashboard/05-VALIDATION.md
    .planning/phases/04-attendance-check-in/04-CONTEXT.md
    src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
    src/queries/assignment/types/adminScheduleAssignmentDetail.ts
  </read_first>
  <files>
    src/queries/operations-dashboard/types/operationsDashboard.ts
    src/queries/operations-dashboard/utils/operationsDashboard.ts
    src/queries/operations-dashboard/utils/operationsDashboard.test.ts
  </files>
  <behavior>
    - Test 1: `getTopOperationsDashboardAnomaly` returns `unfilled_slots` before `missing_check_ins`, `missing_check_ins` before `late_arrivals`, and `on_track` when no anomaly exists, per D-03.
    - Test 2: missing check-ins are not emitted before the computed attendance open time, preserving the Phase 4 first-ceremony rule.
    - Test 3: `getOperationsDashboardWindow` returns a `today` boundary plus a nearby-upcoming boundary so the page can separate `Today` and `Upcoming`, per D-05 and D-06.
    - Test 4: grouping helpers classify schedules into `today` and `upcoming` sections without introducing a weekly dashboard or worker-centric summary, per D-01, D-05, and deferred-scope rules.
    - Test 5: the card contract exposes render-ready schedule context fields so Phase 02 can show a card title plus exact local date and start time without re-deriving those values in JSX.
  </behavior>
  <action>Create `src/queries/operations-dashboard/types/operationsDashboard.ts` with exported contracts for `OperationsDashboardScheduleCard`, `OperationsDashboardSectionKey`, and the anomaly union using exact kinds `unfilled_slots`, `missing_check_ins`, `late_arrivals`, and `on_track`. The card contract must include render-ready fields for schedule context and exact local time display: `title`, `dateLabel`, `startTimeLabel`, and `startsAtIso`, in addition to the summary counts and `detailHref`. Implement `src/queries/operations-dashboard/utils/operationsDashboard.ts` with pure helpers for the dashboard time window, section grouping, and top-anomaly derivation. Use the Phase 4 attendance timing rule from `04-CONTEXT.md` and `getAdminScheduleAttendanceDetail.ts` so missing-check-in logic does not drift. Keep all anomaly ordering and display-label derivation in the utility layer, not in JSX, per D-03 and D-04.</action>
  <acceptance_criteria>
    - `src/queries/operations-dashboard/types/operationsDashboard.ts` exports `OperationsDashboardSectionKey`.
    - `src/queries/operations-dashboard/types/operationsDashboard.ts` contains `title: string`.
    - `src/queries/operations-dashboard/types/operationsDashboard.ts` contains `dateLabel: string`.
    - `src/queries/operations-dashboard/types/operationsDashboard.ts` contains `startTimeLabel: string`.
    - `src/queries/operations-dashboard/utils/operationsDashboard.ts` exports `getTopOperationsDashboardAnomaly`.
    - `src/queries/operations-dashboard/utils/operationsDashboard.ts` exports `getOperationsDashboardWindow`.
    - `src/queries/operations-dashboard/utils/operationsDashboard.test.ts` contains the strings `unfilled_slots`, `missing_check_ins`, `late_arrivals`, and `on_track`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/queries/operations-dashboard/utils/operationsDashboard.test.ts</automated>
  </verify>
  <done>The dashboard anomaly contract is explicit, testable, and aligned with the locked schedule-card and anomaly-priority decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the aggregated admin dashboard DAL and cache tags</name>
  <read_first>
    .planning/phases/05-operations-dashboard/05-CONTEXT.md
    .planning/phases/05-operations-dashboard/05-RESEARCH.md
    src/shared/config/cacheTags.ts
    src/queries/schedule/dal/listAdminSchedules.ts
    src/queries/assignment/dal/getAdminScheduleAssignmentDetail.ts
    src/queries/attendance/dal/getAdminScheduleAttendanceDetail.ts
    src/queries/operations-dashboard/utils/operationsDashboard.ts
  </read_first>
  <files>
    src/shared/config/cacheTags.ts
    src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts
    src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts
  </files>
  <behavior>
    - Test 1: the DAL fetches schedules in one dashboard window and maps one schedule-card summary per schedule rather than calling detail queries in a loop.
    - Test 2: application count, confirmed staffing count, checked-in count, and late count are all present on each schedule card, satisfying D-02.
    - Test 3: only confirmed assignments count toward staffed coverage; draft assignments do not clear an `unfilled_slots` anomaly.
    - Test 4: the cached read uses dedicated dashboard cache tags so later schedule, assignment, and attendance writes can explicitly refresh `/admin`.
    - Test 5: each mapped card includes `title`, `dateLabel`, and `startTimeLabel` so the UI can satisfy the exact-time copy contract without recomputing schedule metadata in the component layer.
  </behavior>
  <action>Add `cacheTags.dashboard.all` and `cacheTags.dashboard.adminOperations` to `src/shared/config/cacheTags.ts`. Create `src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts` as a server-only query slice that uses one Supabase nested select over `schedules`, `schedule_role_slots`, `schedule_applications`, `schedule_assignments`, and `attendance_check_ins`, then maps rows through the Phase 05 utility functions into `today` and `upcoming` schedule cards. The DAL output must include render-ready schedule identity and exact local time fields required by `05-UI-SPEC.md`: `title`, `dateLabel`, `startTimeLabel`, and `startsAtIso` on every `OperationsDashboardScheduleCard`. Do not call `getAdminScheduleAssignmentDetail` or `getAdminScheduleAttendanceDetail` inside a loop. Count only confirmed assignments toward staffed coverage, keep time-sensitive anomaly classification outside any long-lived cached raw row mapping where needed, and cache the read with the new dashboard tags.</action>
  <acceptance_criteria>
    - `src/shared/config/cacheTags.ts` contains `dashboard: {`.
    - `src/shared/config/cacheTags.ts` contains `adminOperations`.
    - `src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts` contains `unstable_cache`.
    - `src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts` contains `cacheTags.dashboard`.
    - `src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts` contains `title`.
    - `src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts` contains `startTimeLabel`.
    - `src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts` contains the string `draft`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts src/queries/operations-dashboard/utils/operationsDashboard.test.ts</automated>
  </verify>
  <done>The dashboard query slice exposes a single cached admin read model with dedicated tags and verified staffing/anomaly semantics for Phase 05.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/queries/operations-dashboard/utils/operationsDashboard.test.ts src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts`.
Confirm the query slice returns schedule-card summaries with D-03 anomaly ordering, confirmed-only staffing counts, and dedicated dashboard cache tags.
</verification>

<success_criteria>
Phase 05 has a stable dashboard data contract: one query slice can supply today/upcoming schedule cards with application, staffing, and attendance signals plus correct top-anomaly ordering.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operations-dashboard/05-operations-dashboard-01-SUMMARY.md`
</output>
