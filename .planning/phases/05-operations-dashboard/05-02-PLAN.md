---
phase: 05-operations-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
  - src/flows/admin-operations-dashboard/components/AdminOperationsDashboardSection.tsx
  - src/flows/admin-operations-dashboard/components/OperationsDashboardCard.tsx
  - src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx
  - src/app/admin/page.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "An admin landing on `/admin` sees `Today` and `Upcoming` dashboard sections from one summary-first page."
    - "Each schedule card shows application, confirmed staffing, and attendance status together and routes drill-down to the existing schedule detail page."
    - "The highest-priority anomaly is visually elevated on every schedule card, while detailed editing remains in `/admin/schedules/[scheduleId]`."
  artifacts:
    - path: src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
      provides: "Admin dashboard page composition with Today and Upcoming sections"
    - path: src/flows/admin-operations-dashboard/components/OperationsDashboardCard.tsx
      provides: "Single schedule-card UI with anomaly badge, summary metrics, and drill-down CTA"
    - path: src/app/admin/page.tsx
      provides: "Thin `/admin` route entry for the dashboard flow"
  key_links:
    - from: src/app/admin/page.tsx
      to: src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
      via: "thin route delegates `/admin` directly to the new dashboard flow"
      pattern: "AdminOperationsDashboardPage"
    - from: src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
      to: src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts
      via: "dashboard page awaits the Phase 05 read model and renders `Today` and `Upcoming` from query-backed sections"
      pattern: "await listAdminOperationsDashboardSchedules|Today|Upcoming"
    - from: src/flows/admin-operations-dashboard/components/OperationsDashboardCard.tsx
      to: /admin/schedules/[scheduleId]
      via: "summary-first card links to existing schedule detail review"
      pattern: "Review schedule|/admin/schedules/"
---

<objective>
Turn the Phase 05 dashboard read contract into the default admin landing page.

Purpose: satisfy DASH-01, complete the visible DASH-02 dashboard surface, and deliver the D-04 triage experience on `/admin` without duplicating schedule-detail controls inline.
Output: dashboard page flow, schedule-card UI, thin route wiring, and component tests.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operations-dashboard/05-CONTEXT.md
@.planning/phases/05-operations-dashboard/05-RESEARCH.md
@.planning/phases/05-operations-dashboard/05-VALIDATION.md
@.planning/phases/05-operations-dashboard/05-UI-SPEC.md
@.planning/phases/04-attendance-check-in/04-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/app/admin/page.tsx
@src/flows/admin-shell/components/AdminShellPage.tsx
@src/flows/admin-schedules/components/AdminSchedulesPage.tsx
@src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx

<interfaces>
From Phase 05 Plan 01 contract:
```ts
export type OperationsDashboardSectionKey = "today" | "upcoming";

export interface OperationsDashboardScheduleCard {
  id: string;
  section: OperationsDashboardSectionKey;
  title: string;
  dateLabel: string;
  startTimeLabel: string;
  startsAtIso: string;
  topAnomaly: {
    kind: "unfilled_slots" | "missing_check_ins" | "late_arrivals" | "on_track";
    label: string;
    count: number;
  };
  applicantCount: number;
  confirmedCount: number;
  checkedInCount: number;
  lateCount: number;
  detailHref: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the summary-first admin dashboard flow and card components</name>
  <read_first>
    .planning/phases/05-operations-dashboard/05-CONTEXT.md
    .planning/phases/05-operations-dashboard/05-UI-SPEC.md
    src/flows/admin-schedules/components/AdminSchedulesPage.tsx
    src/flows/admin-schedule-assignment/components/AdminScheduleAssignmentPage.tsx
    src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts
    src/queries/operations-dashboard/types/operationsDashboard.ts
  </read_first>
  <files>
    src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
    src/flows/admin-operations-dashboard/components/AdminOperationsDashboardSection.tsx
    src/flows/admin-operations-dashboard/components/OperationsDashboardCard.tsx
    src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx
  </files>
  <behavior>
    - Test 1: `AdminOperationsDashboardPage` awaits `listAdminOperationsDashboardSchedules` and renders query-backed `Today` before `Upcoming`, per D-01, D-02, D-05, and D-06.
    - Test 2: each card renders the Plan 01 display fields `title`, `dateLabel`, and `startTimeLabel` so the UI satisfies the exact-time copy contract from `05-UI-SPEC.md`.
    - Test 3: each card shows application, confirmed staffing, and attendance metrics together and includes the exact CTA text `Review schedule`, satisfying DASH-02 and D-07.
    - Test 4: the top anomaly badge is rendered first with the locked priority labels `Unfilled slots`, `Missing check-ins`, `Late arrivals`, or `On track`, satisfying D-03 and D-04.
    - Test 5: the empty state uses the approved copy `No schedules need attention right now`.
  </behavior>
  <action>Create `src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx`, `AdminOperationsDashboardSection.tsx`, and `OperationsDashboardCard.tsx` using the expanded Plan 01 dashboard contract. In `AdminOperationsDashboardPage.tsx`, explicitly `await listAdminOperationsDashboardSchedules(...)` from `src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.ts` and render the returned sections as query-backed `Today` cards first and `Upcoming` cards second. In `OperationsDashboardCard.tsx`, render `title`, `dateLabel`, and `startTimeLabel` directly from the Phase 01 card contract so the card shows schedule context plus exact local start time without recomputing display labels in JSX. Follow `05-UI-SPEC.md` exactly: page headline and lightweight triage summary first, anomaly badge before supporting metrics inside each card, and the primary CTA text `Review schedule`. Keep the experience summary-first per D-07 and D-08: do not inline applicant editing, assignment controls, or attendance forms inside dashboard cards. Use official shadcn primitives already present in the repo and preserve the admin shell visual direction established in Phase 4.</action>
  <acceptance_criteria>
    - `src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx` contains `listAdminOperationsDashboardSchedules`.
    - `src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx` contains `await`.
    - `src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx` contains `Today`.
    - `src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx` contains `Upcoming`.
    - `src/flows/admin-operations-dashboard/components/OperationsDashboardCard.tsx` contains `title`.
    - `src/flows/admin-operations-dashboard/components/OperationsDashboardCard.tsx` contains `dateLabel`.
    - `src/flows/admin-operations-dashboard/components/OperationsDashboardCard.tsx` contains `startTimeLabel`.
    - `src/flows/admin-operations-dashboard/components/OperationsDashboardCard.tsx` contains `Review schedule`.
    - `src/flows/admin-operations-dashboard/components/OperationsDashboardCard.tsx` contains `/admin/schedules/`.
    - `src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx` contains `No schedules need attention right now`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx</automated>
  </verify>
  <done>The dashboard flow renders schedule-centric Today/Upcoming sections with the required triage emphasis and drill-down link behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the `/admin` placeholder with the dashboard route flow</name>
  <read_first>
    .planning/phases/05-operations-dashboard/05-RESEARCH.md
    src/app/admin/page.tsx
    src/flows/admin-shell/components/AdminShellPage.tsx
    src/queries/access/dal/requireAdminUser.ts
    src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.tsx
  </read_first>
  <files>
    src/app/admin/page.tsx
    src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx
  </files>
  <behavior>
    - Test 1: `/admin` delegates to the dashboard flow through a thin route file, not the old placeholder shell, satisfying DASH-01.
    - Test 2: admin gating still happens before dashboard data is shown.
    - Test 3: the page-level test covers the query-backed Today/Upcoming admin landing behavior rather than the old placeholder copy.
  </behavior>
  <action>Update `src/app/admin/page.tsx` to import the new `AdminOperationsDashboardPage` flow directly and keep the route thin. Follow the research recommendation to prefer one route mapping to one flow instead of preserving `AdminShellPage` as the landing implementation. Keep admin access enforcement in the dashboard flow or a directly adjacent query call, but do not leave `/admin` on the placeholder `AdminShellPage` output. Update the dashboard page test so the verified landing experience is the Phase 05 summary dashboard rather than the old placeholder shell.</action>
  <acceptance_criteria>
    - `src/app/admin/page.tsx` contains `AdminOperationsDashboardPage`.
    - `src/app/admin/page.tsx` does not contain `AdminShellPage`.
    - `src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx` contains `Admin access required.` or the existing admin-forbidden copy path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx</automated>
  </verify>
  <done>The default admin route is now the operations dashboard and remains thin, gated, and test-covered.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/flows/admin-operations-dashboard/components/AdminOperationsDashboardPage.test.tsx`.
Confirm `AdminOperationsDashboardPage` awaits `listAdminOperationsDashboardSchedules`, `/admin` renders query-backed `Today` before `Upcoming`, and cards link to `/admin/schedules/[scheduleId]`.
</verification>

<success_criteria>
Admins land on a summary-first `/admin` dashboard that highlights the most urgent schedule anomalies and shows staffing and attendance state together.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operations-dashboard/05-operations-dashboard-02-SUMMARY.md`
</output>
