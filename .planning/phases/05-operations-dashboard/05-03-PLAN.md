---
phase: 05-operations-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/mutations/schedule/actions/submitSchedule.ts
  - src/mutations/schedule/actions/submitSchedule.test.ts
  - src/mutations/schedule/actions/submitScheduleStatus.ts
  - src/mutations/schedule/actions/submitScheduleStatus.test.ts
  - src/mutations/assignment/actions/confirmScheduleAssignments.ts
  - src/mutations/assignment/actions/confirmScheduleAssignments.test.ts
  - src/mutations/attendance/actions/submitAttendanceCheckIn.ts
  - src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "After schedule writes, `/admin` can reflect the updated dashboard state without relying on route-level invalidation."
    - "After assignment confirmation writes, `/admin` can reflect the updated staffing and anomaly state without manual refresh."
    - "After attendance submissions, `/admin` can reflect the updated check-in and lateness state without manual refresh."
  artifacts:
    - path: src/mutations/schedule/actions/submitSchedule.ts
      provides: "Dashboard cache revalidation after schedule creation or edits"
    - path: src/mutations/schedule/actions/submitScheduleStatus.ts
      provides: "Dashboard cache revalidation after schedule status changes"
    - path: src/mutations/assignment/actions/confirmScheduleAssignments.ts
      provides: "Dashboard cache revalidation after staffing confirmation changes"
    - path: src/mutations/attendance/actions/submitAttendanceCheckIn.ts
      provides: "Dashboard cache revalidation after attendance submissions"
  key_links:
    - from: src/mutations/schedule/actions/submitSchedule.ts
      to: src/shared/config/cacheTags.ts
      via: "schedule changes revalidate dashboard tags instead of relying on route-level invalidation"
      pattern: "cacheTags\\.dashboard\\.(all|adminOperations)"
    - from: src/mutations/assignment/actions/confirmScheduleAssignments.ts
      to: src/shared/config/cacheTags.ts
      via: "assignment confirmation changes revalidate dashboard tags alongside existing assignment tags"
      pattern: "cacheTags\\.dashboard\\.(all|adminOperations)"
    - from: src/mutations/attendance/actions/submitAttendanceCheckIn.ts
      to: src/shared/config/cacheTags.ts
      via: "attendance writes revalidate dashboard tags alongside existing attendance tags"
      pattern: "cacheTags\\.dashboard\\.(all|adminOperations)"
---

<objective>
Wire dedicated dashboard cache-tag revalidation into the mutations that change dashboard-visible schedule, staffing, and attendance state.

Purpose: preserve Phase 05 dashboard freshness after operational writes while keeping the `/admin` route/UI plan under the file-count warning threshold.
Output: mutation updates and focused action-test coverage for dashboard tag revalidation.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operations-dashboard/05-CONTEXT.md
@.planning/phases/05-operations-dashboard/05-RESEARCH.md
@.planning/phases/05-operations-dashboard/05-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/shared/config/cacheTags.ts
@src/mutations/schedule/actions/submitSchedule.ts
@src/mutations/schedule/actions/submitScheduleStatus.ts
@src/mutations/assignment/actions/confirmScheduleAssignments.ts
@src/mutations/attendance/actions/submitAttendanceCheckIn.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revalidate dashboard tags from schedule writes</name>
  <read_first>
    .planning/phases/05-operations-dashboard/05-RESEARCH.md
    src/shared/config/cacheTags.ts
    src/mutations/schedule/actions/submitSchedule.ts
    src/mutations/schedule/actions/submitScheduleStatus.ts
  </read_first>
  <files>
    src/mutations/schedule/actions/submitSchedule.ts
    src/mutations/schedule/actions/submitSchedule.test.ts
    src/mutations/schedule/actions/submitScheduleStatus.ts
    src/mutations/schedule/actions/submitScheduleStatus.test.ts
  </files>
  <behavior>
    - Test 1: schedule creation or edits revalidate `cacheTags.dashboard.all` and `cacheTags.dashboard.adminOperations` alongside existing tags.
    - Test 2: schedule status updates revalidate the same dashboard tags so `/admin` stays fresh when schedule visibility changes.
    - Test 3: the implementation stays tag-based and does not fall back to `revalidatePath("/admin")`.
  </behavior>
  <action>Update `submitSchedule.ts` and `submitScheduleStatus.ts` to call `revalidateTag(cacheTags.dashboard.all, "max")` and `revalidateTag(cacheTags.dashboard.adminOperations, "max")` wherever the mutation already revalidates schedule-visible reads. Create `submitSchedule.test.ts` and `submitScheduleStatus.test.ts` to assert the wrapper actions call the dashboard tags alongside the existing schedule tags and never call `revalidatePath("/admin")`. Keep the change orchestration-only and tag-based per repo rules; do not introduce route-level invalidation. This split exists to address the Phase 05 checker scope warning while preserving the dashboard freshness requirement.</action>
  <acceptance_criteria>
    - `src/mutations/schedule/actions/submitSchedule.ts` contains `cacheTags.dashboard.all`.
    - `src/mutations/schedule/actions/submitScheduleStatus.ts` contains `cacheTags.dashboard.adminOperations`.
    - `src/mutations/schedule/actions/submitSchedule.test.ts` contains `cacheTags.dashboard.all`.
    - `src/mutations/schedule/actions/submitScheduleStatus.test.ts` contains `cacheTags.dashboard.adminOperations`.
    - `src/mutations/schedule/actions/submitSchedule.ts` does not contain `revalidatePath("/admin")`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/mutations/schedule/actions/submitSchedule.test.ts src/mutations/schedule/actions/submitScheduleStatus.test.ts</automated>
  </verify>
  <done>Schedule mutations explicitly refresh the dashboard cache tags without path-based invalidation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revalidate dashboard tags from assignment and attendance writes with focused test coverage</name>
  <read_first>
    .planning/phases/05-operations-dashboard/05-RESEARCH.md
    src/shared/config/cacheTags.ts
    src/mutations/assignment/actions/confirmScheduleAssignments.ts
    src/mutations/assignment/actions/confirmScheduleAssignments.test.ts
    src/mutations/attendance/actions/submitAttendanceCheckIn.ts
    src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts
  </read_first>
  <files>
    src/mutations/assignment/actions/confirmScheduleAssignments.ts
    src/mutations/assignment/actions/confirmScheduleAssignments.test.ts
    src/mutations/attendance/actions/submitAttendanceCheckIn.ts
    src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts
  </files>
  <behavior>
    - Test 1: confirming assignments revalidates dashboard cache tags in addition to existing assignment and schedule tags.
    - Test 2: attendance submit revalidates dashboard cache tags in addition to existing attendance and worker-confirmed tags.
    - Test 3: the available action tests assert the dashboard tag calls explicitly.
  </behavior>
  <action>Update `confirmScheduleAssignments.ts` and `submitAttendanceCheckIn.ts` to revalidate `cacheTags.dashboard.all` and `cacheTags.dashboard.adminOperations` alongside their existing invalidation. Extend `confirmScheduleAssignments.test.ts` and `submitAttendanceCheckIn.test.ts` to assert the dashboard-tag calls directly. Keep the tag contract explicit so later `/admin` maintenance does not rely on implicit overlap with assignment or attendance cache keys.</action>
  <acceptance_criteria>
    - `src/mutations/assignment/actions/confirmScheduleAssignments.ts` contains `cacheTags.dashboard.all`.
    - `src/mutations/attendance/actions/submitAttendanceCheckIn.ts` contains `cacheTags.dashboard.adminOperations`.
    - `src/mutations/assignment/actions/confirmScheduleAssignments.test.ts` contains `dashboard`.
    - `src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts` contains `dashboard`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/mutations/assignment/actions/confirmScheduleAssignments.test.ts src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts</automated>
  </verify>
  <done>Assignment and attendance mutations keep the dashboard fresh with explicit tag-based invalidation backed by tests.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/mutations/schedule/actions/submitSchedule.test.ts src/mutations/schedule/actions/submitScheduleStatus.test.ts src/mutations/assignment/actions/confirmScheduleAssignments.test.ts src/mutations/attendance/actions/submitAttendanceCheckIn.test.ts`.
Confirm schedule, assignment, and attendance writes call `cacheTags.dashboard.all` and `cacheTags.dashboard.adminOperations`, with no `revalidatePath("/admin")` fallback.
</verification>

<success_criteria>
Phase 05 dashboard cache freshness is maintained through explicit schedule, assignment, and attendance tag revalidation without bloating the route/UI implementation plan.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operations-dashboard/05-operations-dashboard-03-SUMMARY.md`
</output>
