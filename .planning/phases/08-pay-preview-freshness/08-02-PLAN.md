---
phase: 08-pay-preview-freshness
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/mutations/worker-rate/actions/submitWorkerRate.ts
  - src/mutations/worker-rate/actions/submitWorkerRate.test.ts
  - src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
  - src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
autonomous: true
requirements:
  - PAY-01
  - PAY-02
  - PAY-04
must_haves:
  truths:
    - "A successful admin worker-rate write refreshes the affected worker pay-preview cache on the next render without widening invalidation to unrelated workers."
    - "The cached worker confirmed-assignment/pay-preview read subscribes to the dedicated worker pay-preview tag."
    - "Workers can review the existing confirmed-assignment/pay-preview flow and see totals recalculated from the latest saved hourly rate."
  artifacts:
    - path: src/mutations/worker-rate/actions/submitWorkerRate.ts
      provides: "Success-gated worker pay-preview cache invalidation after admin rate writes."
    - path: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      provides: "Worker pay-preview cache contract that includes the dedicated worker pay-preview tag."
    - path: src/mutations/worker-rate/actions/submitWorkerRate.test.ts
      provides: "Regression coverage that locks the Phase 08 invalidation contract."
    - path: src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
      provides: "Regression coverage that locks the worker pay-preview query tag contract."
  key_links:
    - from: src/mutations/worker-rate/actions/submitWorkerRate.ts
      to: src/mutations/worker-rate/actions/upsertWorkerRate.ts
      via: "the submit wrapper preserves the domain write and invalidates only after the rate write succeeds"
      pattern: "await upsertWorkerRate\\("
    - from: src/mutations/worker-rate/actions/submitWorkerRate.ts
      to: src/shared/config/cacheTags.ts
      via: "the wrapper revalidates the exact worker pay-preview tag required by the phase"
      pattern: "cacheTags\\.assignments\\.workerPayPreview\\("
    - from: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      to: src/shared/config/cacheTags.ts
      via: "the cached query subscribes to the dedicated worker pay-preview tag"
      pattern: "cacheTags\\.assignments\\.workerPayPreview\\(workerUserId\\)"
    - from: src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
      to: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      via: "the existing worker preview page consumes the refreshed pay-preview query"
      pattern: "listConfirmedWorkerAssignments\\(currentUser\\.id\\)"
---

<objective>
Implement the Phase 08 pay-preview freshness fix so the Wave 0 regressions pass without widening scope.

Purpose: close the worker pay-preview freshness gap by updating the admin worker-rate submit wrapper to revalidate the dedicated worker pay-preview tag and by updating the cached worker pay-preview query to subscribe to that tag.
Output: an updated `submitWorkerRate` action, an updated `listConfirmedWorkerAssignments` cache contract, and passing freshness regressions.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pay-preview-freshness/08-CONTEXT.md
@.planning/phases/08-pay-preview-freshness/08-RESEARCH.md
@.planning/phases/08-pay-preview-freshness/08-VALIDATION.md
@.planning/phases/08-pay-preview-freshness/08-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/mutations/worker-rate/actions/submitWorkerRate.ts
@src/mutations/worker-rate/actions/submitWorkerRate.test.ts
@src/mutations/worker-rate/actions/upsertWorkerRate.ts
@src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
@src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
@src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
@src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx
@src/shared/config/cacheTags.ts

<interfaces>
From src/shared/config/cacheTags.ts:
```ts
assignments: {
  all: "assignments",
  workerConfirmed: (workerUserId: string) => `assignments:worker-confirmed:${workerUserId}`,
  workerPayPreview: (workerUserId: string) => `assignments:worker-pay-preview:${workerUserId}`,
},
```

From src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx:
```ts
const [assignments, attendanceStatuses] = await Promise.all([
  listConfirmedWorkerAssignments(currentUser.id),
  listWorkerAttendanceStatuses(currentUser.id),
]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement success-gated worker pay-preview invalidation in the submit wrapper</name>
  <read_first>
    src/mutations/worker-rate/actions/submitWorkerRate.test.ts
    .planning/phases/08-pay-preview-freshness/08-RESEARCH.md
    src/mutations/worker-rate/actions/submitWorkerRate.ts
    src/mutations/worker-rate/actions/upsertWorkerRate.ts
    src/shared/config/cacheTags.ts
  </read_first>
  <files>
    src/mutations/worker-rate/actions/submitWorkerRate.ts
    src/mutations/worker-rate/actions/submitWorkerRate.test.ts
  </files>
  <behavior>
    - Test 1: when `upsertWorkerRate` succeeds, the wrapper calls `revalidateTag(cacheTags.assignments.workerPayPreview(userId), "max")`.
    - Test 2: when `upsertWorkerRate` rejects, the wrapper rethrows and does not call `revalidateTag`.
    - Test 3: the implementation remains tag-based and does not introduce `revalidatePath`.
  </behavior>
  <action>Update `src/mutations/worker-rate/actions/submitWorkerRate.ts` so it keeps the existing `userId` and `hourlyRateCents` extraction, awaits `upsertWorkerRate({ userId, hourlyRateCents })`, and then immediately calls `revalidateTag(cacheTags.assignments.workerPayPreview(userId), "max")`. Import `revalidateTag` from `next/cache` and `cacheTags` from `#shared/config/cacheTags`. Keep invalidation outside any `catch` block so failures still reject without cache churn. Do not add `revalidatePath`, do not widen invalidation to `cacheTags.assignments.all`, and do not move cache logic into `upsertWorkerRate.ts`. Adjust `src/mutations/worker-rate/actions/submitWorkerRate.test.ts` only as needed to make the regression exact and passing.</action>
  <acceptance_criteria>
    - `src/mutations/worker-rate/actions/submitWorkerRate.ts` contains `revalidateTag(cacheTags.assignments.workerPayPreview(userId), "max")`.
    - `src/mutations/worker-rate/actions/submitWorkerRate.ts` imports `revalidateTag` from `next/cache`.
    - `src/mutations/worker-rate/actions/submitWorkerRate.ts` imports `cacheTags` from `#shared/config/cacheTags`.
    - `src/mutations/worker-rate/actions/submitWorkerRate.ts` does not contain `revalidatePath`.
    - `pnpm test -- src/mutations/worker-rate/actions/submitWorkerRate.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/mutations/worker-rate/actions/submitWorkerRate.test.ts</automated>
  </verify>
  <done>The admin worker-rate submit wrapper now refreshes the exact worker pay-preview cache tag after a successful write and leaves failed submissions alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the dedicated pay-preview tag into the cached worker read</name>
  <read_first>
    src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
    .planning/phases/08-pay-preview-freshness/08-RESEARCH.md
    src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
    src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx
    src/shared/config/cacheTags.ts
  </read_first>
  <files>
    src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
    src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
  </files>
  <behavior>
    - Test 1: the cached query attaches `cacheTags.assignments.workerPayPreview(workerUserId)` alongside `assignments.all` and `assignments.workerConfirmed(workerUserId)`.
    - Test 2: the existing VITEST-path data-shape tests still pass unchanged.
    - Test 3: the worker preview page smoke test still passes against the unchanged query signature.
  </behavior>
  <action>Update `src/queries/assignment/dal/listConfirmedWorkerAssignments.ts` so the `unstable_cache(...)` tag list becomes `[cacheTags.assignments.all, cacheTags.assignments.workerConfirmed(workerUserId), cacheTags.assignments.workerPayPreview(workerUserId)]`. Do not change the query key, data mapping, sort order, or return shape. Keep the `process.env.VITEST` short-circuit intact. Update `src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts` so the cached-branch assertion expects the three-tag array exactly while preserving the existing uncached data-shape tests. Do not modify `src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.tsx` in this phase; use its existing smoke test only as downstream verification.</action>
  <acceptance_criteria>
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.ts` contains `cacheTags.assignments.workerPayPreview(workerUserId)`.
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.ts` still contains `cacheTags.assignments.workerConfirmed(workerUserId)`.
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.ts` still contains `process.env.VITEST`.
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts` expects the three-tag array in the cached branch.
    - `pnpm test -- src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx</automated>
  </verify>
  <done>The cached worker pay-preview read now subscribes to the dedicated pay-preview tag without changing the existing worker preview page contract.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/mutations/worker-rate/actions/submitWorkerRate.test.ts src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts src/flows/worker-assignment-preview/components/WorkerAssignmentPreviewPage.test.tsx`.
Confirm a successful admin worker-rate write invalidates `assignments:worker-pay-preview:<userId>`, the cached worker pay-preview read subscribes to that tag, and the existing worker preview page smoke coverage remains green.
</verification>

<success_criteria>
Phase 08 closes the worker pay-preview freshness gap through explicit success-path tag invalidation in the admin worker-rate submit wrapper and a dedicated pay-preview tag on the cached worker read, with no new UI or payroll scope added.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pay-preview-freshness/08-pay-preview-freshness-02-SUMMARY.md`
</output>
