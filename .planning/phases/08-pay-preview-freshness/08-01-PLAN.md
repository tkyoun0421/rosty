---
phase: 08-pay-preview-freshness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mutations/worker-rate/actions/submitWorkerRate.test.ts
  - src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
autonomous: true
requirements:
  - PAY-01
  - PAY-02
must_haves:
  truths:
    - "Phase 08 has a failing-first regression that proves a successful admin worker-rate write must invalidate only the affected worker pay-preview cache tag."
    - "Phase 08 has a failing-first cache-contract regression that proves the worker pay-preview query subscribes to the dedicated worker pay-preview tag."
    - "The regression suite also proves failed worker-rate submissions do not churn pay-preview cache tags."
  artifacts:
    - path: src/mutations/worker-rate/actions/submitWorkerRate.test.ts
      provides: "Wave 0 freshness regression coverage for success-path worker pay-preview invalidation and failure-path non-invalidation."
    - path: src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
      provides: "Cached-query contract coverage for the dedicated worker pay-preview cache tag."
  key_links:
    - from: src/mutations/worker-rate/actions/submitWorkerRate.test.ts
      to: src/mutations/worker-rate/actions/submitWorkerRate.ts
      via: "the new regression imports the real submit wrapper action and asserts its tag invalidation behavior"
      pattern: "#mutations/worker-rate/actions/submitWorkerRate"
    - from: src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
      to: src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
      via: "the regression asserts the exact unstable_cache tag contract for the worker pay-preview read"
      pattern: "workerPayPreview|unstable_cache"
    - from: src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
      to: src/shared/config/cacheTags.ts
      via: "the regression proves the dedicated worker pay-preview tag is attached to the cached query"
      pattern: "assignments:worker-pay-preview:"
---

<objective>
Create the explicit Wave 0 freshness regressions for Phase 08 before implementation changes land.

Purpose: make the pay-preview freshness gap executable by proving the admin worker-rate submit wrapper is currently missing worker pay-preview invalidation and by proving the cached worker pay-preview read does not yet subscribe to the dedicated `workerPayPreview` tag.
Output: a new `submitWorkerRate` action test, an updated cached-query contract test, and a failing-first verification run.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pay-preview-freshness/08-CONTEXT.md
@.planning/phases/08-pay-preview-freshness/08-RESEARCH.md
@.planning/phases/08-pay-preview-freshness/08-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/mutations/worker-rate/actions/submitWorkerRate.ts
@src/mutations/worker-rate/actions/upsertWorkerRate.ts
@src/mutations/worker-rate/actions/upsertWorkerRate.test.ts
@src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
@src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
@src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts
@src/shared/config/cacheTags.ts
@vitest.config.ts

<interfaces>
From src/mutations/worker-rate/actions/submitWorkerRate.ts:
```ts
export async function submitWorkerRate(formData: FormData) {
  const userId = String(formData.get("userId") ?? "");
  const hourlyRateCents = Number(formData.get("hourlyRateCents"));

  await upsertWorkerRate({ userId, hourlyRateCents });
}
```

From src/queries/assignment/dal/listConfirmedWorkerAssignments.ts:
```ts
const cachedQuery = unstable_cache(
  async () => await runListConfirmedWorkerAssignments(workerUserId),
  ["queries:assignment:listConfirmedWorkerAssignments", workerUserId],
  {
    tags: [cacheTags.assignments.all, cacheTags.assignments.workerConfirmed(workerUserId)],
  },
);
```

From src/shared/config/cacheTags.ts:
```ts
assignments: {
  all: "assignments",
  workerConfirmed: (workerUserId: string) => `assignments:worker-confirmed:${workerUserId}`,
  workerPayPreview: (workerUserId: string) => `assignments:worker-pay-preview:${workerUserId}`,
},
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing-first worker-rate freshness action regression</name>
  <read_first>
    .planning/phases/08-pay-preview-freshness/08-VALIDATION.md
    .planning/phases/08-pay-preview-freshness/08-RESEARCH.md
    src/mutations/worker-rate/actions/submitWorkerRate.ts
    src/mutations/worker-rate/actions/upsertWorkerRate.ts
    src/mutations/worker-rate/actions/upsertWorkerRate.test.ts
    src/shared/config/cacheTags.ts
    vitest.config.ts
  </read_first>
  <files>
    src/mutations/worker-rate/actions/submitWorkerRate.test.ts
  </files>
  <behavior>
    - Test 1: when `upsertWorkerRate` resolves successfully, the submit wrapper calls `revalidateTag(cacheTags.assignments.workerPayPreview("worker-1"), "max")`.
    - Test 2: the wrapper passes `{ userId: "worker-1", hourlyRateCents: 20000 }` into `upsertWorkerRate`.
    - Test 3: when `upsertWorkerRate` rejects, the wrapper rethrows and does not call `revalidateTag`.
  </behavior>
  <action>Create `src/mutations/worker-rate/actions/submitWorkerRate.test.ts` as the explicit Wave 0 regression file for Phase 08. Follow the mocking pattern already used in `src/mutations/application/actions/submitScheduleApplication.test.ts` and `src/mutations/assignment/actions/confirmScheduleAssignments.test.ts`: mock `next/cache` with `revalidateTag = vi.fn()` and mock `#mutations/worker-rate/actions/upsertWorkerRate` with `vi.fn()`. Build a real `FormData` instance containing `userId = "worker-1"` and `hourlyRateCents = "20000"`. Add one success-path test that expects `upsertWorkerRate` to receive `{ userId: "worker-1", hourlyRateCents: 20000 }` and expects `revalidateTag` to be called exactly once with `assignments:worker-pay-preview:worker-1`. Add one failure-path test that makes `upsertWorkerRate` reject with `new Error("FORBIDDEN")`, asserts `submitWorkerRate(formData)` rejects with that error, and asserts `revalidateTag` was never called. Keep imports on `#` aliases only and do not add route-level or UI-level assertions in this file.</action>
  <acceptance_criteria>
    - `src/mutations/worker-rate/actions/submitWorkerRate.test.ts` exists.
    - The test file mocks `next/cache`.
    - The test file mocks `#mutations/worker-rate/actions/upsertWorkerRate`.
    - The test file contains the exact string `assignments:worker-pay-preview:worker-1`.
    - The test file contains the exact string `FORBIDDEN`.
    - The test file asserts `revalidateTag` is not called in the failure case.
  </acceptance_criteria>
  <verify>
    <automated>$env:CI='1'; pnpm test -- src/mutations/worker-rate/actions/submitWorkerRate.test.ts; if ($LASTEXITCODE -eq 0) { throw 'Expected Phase 08 action regression to fail before submitWorkerRate is updated.' }</automated>
  </verify>
  <done>The new worker-rate freshness action regression exists and fails against the current implementation, making the missing pay-preview invalidation executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the failing-first worker pay-preview cache-contract regression</name>
  <read_first>
    .planning/phases/08-pay-preview-freshness/08-VALIDATION.md
    .planning/phases/08-pay-preview-freshness/08-RESEARCH.md
    src/queries/assignment/dal/listConfirmedWorkerAssignments.ts
    src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
    src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts
    src/shared/config/cacheTags.ts
    vitest.config.ts
  </read_first>
  <files>
    src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts
  </files>
  <behavior>
    - Test 1: in the cached branch, `unstable_cache` receives tags `cacheTags.assignments.all`, `cacheTags.assignments.workerConfirmed("worker-1")`, and `cacheTags.assignments.workerPayPreview("worker-1")`.
    - Test 2: the existing data-shape tests stay intact for the uncached `process.env.VITEST = "true"` path.
  </behavior>
  <action>Extend `src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts` with a caching-contract regression modeled on `src/queries/operations-dashboard/dal/listAdminOperationsDashboardSchedules.test.ts`. Mock `next/cache` so `unstable_cache = vi.fn((callback, _keys, _options) => callback)`. Keep the existing tests for the uncached VITEST path. Add one new test that resets modules, sets `process.env.VITEST = ""`, imports `cacheTags` plus `listConfirmedWorkerAssignments`, calls `await listConfirmedWorkerAssignments("worker-1")`, and expects `unstable_cache` to be called with `tags: [cacheTags.assignments.all, cacheTags.assignments.workerConfirmed("worker-1"), cacheTags.assignments.workerPayPreview("worker-1")]`. Do not change the production query in this task; the new assertion should fail until the dedicated tag is added to the query.</action>
  <acceptance_criteria>
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts` contains `unstable_cache`.
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts` contains `cacheTags.assignments.workerPayPreview("worker-1")`.
    - `src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts` contains `process.env.VITEST = ""`.
    - The new cached-branch test asserts the exact tag array passed to `unstable_cache`.
  </acceptance_criteria>
  <verify>
    <automated>$env:CI='1'; pnpm test -- src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts; if ($LASTEXITCODE -eq 0) { throw 'Expected Phase 08 cache-contract regression to fail before listConfirmedWorkerAssignments subscribes to workerPayPreview.' }</automated>
  </verify>
  <done>The worker pay-preview query has a concrete failing-first cache-contract regression that proves the dedicated tag is not yet wired.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/mutations/worker-rate/actions/submitWorkerRate.test.ts src/queries/assignment/dal/listConfirmedWorkerAssignments.test.ts` and confirm the command fails because `submitWorkerRate.ts` does not yet invalidate `assignments:worker-pay-preview:<userId>` and `listConfirmedWorkerAssignments.ts` does not yet attach the dedicated pay-preview tag.
</verification>

<success_criteria>
Phase 08 has concrete failing-first regressions for both the admin worker-rate write wrapper and the worker pay-preview cache contract, making the freshness gap executable before implementation changes land.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pay-preview-freshness/08-pay-preview-freshness-01-SUMMARY.md`
</output>
