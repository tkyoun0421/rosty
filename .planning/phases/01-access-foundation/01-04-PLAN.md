---
phase: 01-access-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/mutations/worker-rate/schemas/worker-rate.ts
  - src/mutations/worker-rate/schemas/worker-rate.test.ts
  - src/mutations/worker-rate/dal/worker-rate-dal.ts
  - src/mutations/worker-rate/actions/upsert-worker-rate.ts
  - src/mutations/worker-rate/actions/upsert-worker-rate.test.ts
  - src/queries/worker-rate/dal/list-worker-rates.ts
  - src/flows/admin-worker-rates/components/AdminWorkerRatesPage.tsx
  - src/flows/admin-worker-rates/components/AdminWorkerRatesPage.test.tsx
  - src/flows/admin-worker-rates/components/WorkerRateForm.tsx
  - src/flows/admin-worker-rates/components/WorkerRateTable.tsx
  - src/app/admin/worker-rates/page.tsx
autonomous: true
requirements:
  - PAY-01
must_haves:
  truths:
    - "Admins can view workers and create or update a worker's current hourly rate."
    - "Worker-rate writes remain admin-only at the write boundary."
    - "Worker-rate storage stays in the current-value table with audit fields only."
  artifacts:
    - path: src/mutations/worker-rate/schemas/worker-rate.ts
      provides: positive integer-cent worker-rate schema
    - path: src/mutations/worker-rate/dal/worker-rate-dal.ts
      provides: write-side current-value persistence
    - path: src/mutations/worker-rate/actions/upsert-worker-rate.ts
      provides: admin-only worker-rate command
    - path: src/queries/worker-rate/dal/list-worker-rates.ts
      provides: read-side worker/rate listing
    - path: src/flows/admin-worker-rates/components/AdminWorkerRatesPage.tsx
      provides: flow-owned admin worker-rate UI
    - path: src/app/admin/worker-rates/page.tsx
      provides: thin route entry for worker-rate management
  key_links:
    - from: src/app/admin/worker-rates/page.tsx
      to: src/flows/admin-worker-rates/components/AdminWorkerRatesPage.tsx
      via: one route to one flow
      pattern: AdminWorkerRatesPage
    - from: src/mutations/worker-rate/actions/upsert-worker-rate.ts
      to: src/mutations/worker-rate/schemas/worker-rate.ts
      via: shared validation before write
      pattern: workerRateSchema
    - from: src/mutations/worker-rate/dal/worker-rate-dal.ts
      to: supabase/migrations/20260331_phase1_access_foundation.sql
      via: current-value `worker_rates` table
      pattern: worker_rates
---

<objective>
Close Phase 1 by implementing the admin-only worker-rate workflow on top of the completed auth, invite, and RBAC foundation.

Purpose: satisfy `PAY-01` with a real protected write path, read path, and operational UI while preserving the current-value-only storage rule.
Output: worker-rate schema, read/write slices, admin rate command, and a flow-owned admin worker-rate page.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-foundation/01-CONTEXT.md
@.planning/phases/01-access-foundation/01-RESEARCH.md
@.planning/phases/01-access-foundation/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-access-foundation/01-03-PLAN.md
@CLAUDE.md
@src/shared/model/access.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add worker-rate validation and write-side command path</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/shared/model/access.ts
  </read_first>
  <files>
    src/mutations/worker-rate/schemas/worker-rate.ts
    src/mutations/worker-rate/schemas/worker-rate.test.ts
    src/mutations/worker-rate/dal/worker-rate-dal.ts
    src/mutations/worker-rate/actions/upsert-worker-rate.ts
    src/mutations/worker-rate/actions/upsert-worker-rate.test.ts
  </files>
  <action>Create `src/mutations/worker-rate/schemas/worker-rate.ts` with a Zod schema that accepts only positive integer-cent rates. Add a colocated test that rejects zero, negative, decimal, and invalid values. Create `src/mutations/worker-rate/dal/worker-rate-dal.ts` to upsert the current `worker_rates` row and write `updated_by` and `updated_at`. Create `src/mutations/worker-rate/actions/upsert-worker-rate.ts` with a colocated test asserting the command is admin-only and does not create any history or ledger behavior.</action>
  <verify>
    <automated>pnpm exec vitest run src/mutations/worker-rate/schemas/worker-rate.test.ts src/mutations/worker-rate/actions/upsert-worker-rate.test.ts</automated>
  </verify>
  <acceptance_criteria>The worker-rate schema rejects invalid values, the write DAL updates only the current row plus audit columns, and the colocated mutation tests pass without introducing a history table or append-only event model.</acceptance_criteria>
  <done>The server-side write path for `PAY-01` is complete and still inside the current-value-only Phase 1 boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add read-side listing and flow-owned admin worker-rate UI</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    src/mutations/worker-rate/schemas/worker-rate.ts
    src/shared/model/access.ts
  </read_first>
  <files>
    src/queries/worker-rate/dal/list-worker-rates.ts
    src/flows/admin-worker-rates/components/AdminWorkerRatesPage.tsx
    src/flows/admin-worker-rates/components/AdminWorkerRatesPage.test.tsx
    src/flows/admin-worker-rates/components/WorkerRateForm.tsx
    src/flows/admin-worker-rates/components/WorkerRateTable.tsx
    src/app/admin/worker-rates/page.tsx
  </files>
  <action>Create `src/queries/worker-rate/dal/list-worker-rates.ts` for the read-side worker/rate listing. Create `src/flows/admin-worker-rates/components/AdminWorkerRatesPage.tsx` with `WorkerRateForm.tsx` and `WorkerRateTable.tsx` under the same flow. Wire `src/app/admin/worker-rates/page.tsx` as a thin route entry to that flow. Add `src/flows/admin-worker-rates/components/AdminWorkerRatesPage.test.tsx` asserting admins can see current worker/rate rows, submit valid updates, and that the UI does not expose or imply rate-history features.</action>
  <verify>
    <automated>pnpm exec vitest run src/flows/admin-worker-rates/components/AdminWorkerRatesPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>`src/app/admin/worker-rates/page.tsx` is a thin route file, the admin worker-rate flow test passes, and the rendered UI shows only current values plus update capability with no history section or version list.</acceptance_criteria>
  <done>Phase 1 closes with a usable admin-only worker-rate workflow aligned to the current-value storage decision.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/mutations/worker-rate/schemas/worker-rate.test.ts src/mutations/worker-rate/actions/upsert-worker-rate.test.ts src/flows/admin-worker-rates/components/AdminWorkerRatesPage.test.tsx`. Confirm worker-rate writes remain admin-only and the UI reflects only the current stored value.
</verification>

<success_criteria>
Admins can manage current worker rates through a protected page, the command path is validated and server-authorized, and Phase 1 still avoids out-of-scope history work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-foundation/01-access-foundation-04-SUMMARY.md`
</output>
