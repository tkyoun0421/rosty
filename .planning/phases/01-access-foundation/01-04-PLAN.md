---
phase: 01-access-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/lib/validation/worker-rate.ts
  - src/lib/dal/admin.ts
  - src/app/(admin)/workers/page.tsx
  - src/app/(admin)/workers/_components/worker-rate-form.tsx
  - src/app/(admin)/workers/_components/worker-rate-table.tsx
  - tests/access/worker-rates.test.ts
autonomous: true
requirements:
  - PAY-01
must_haves:
  truths:
    - "Admins can view workers and create or update a worker's current hourly rate."
    - "Worker-rate writes remain admin-only at the DAL boundary."
    - "Worker-rate storage stays in the current-value table with audit fields only."
  artifacts:
    - path: src/lib/validation/worker-rate.ts
      provides: shared worker-rate validation rules
    - path: src/lib/dal/admin.ts
      provides: admin-only worker list and worker-rate mutation logic
    - path: src/app/(admin)/workers/page.tsx
      provides: protected admin worker-rate screen
    - path: src/app/(admin)/workers/_components/worker-rate-form.tsx
      provides: rate edit form
    - path: src/app/(admin)/workers/_components/worker-rate-table.tsx
      provides: worker/rate listing UI
  key_links:
    - from: src/app/(admin)/workers/page.tsx
      to: src/lib/dal/admin.ts
      via: load and save worker rates
      pattern: listWorkersWithRates|upsertWorkerRate
    - from: src/app/(admin)/workers/_components/worker-rate-form.tsx
      to: src/lib/validation/worker-rate.ts
      via: shared input validation
      pattern: hourlyRateCents
    - from: src/lib/dal/admin.ts
      to: supabase/migrations/20260331_phase1_access_foundation.sql
      via: current-value worker_rates schema
      pattern: worker_rates
---

<objective>
Add the admin-only worker-rate management workflow on top of the completed auth, invite, and RBAC foundation.

Purpose: close Phase 1 by satisfying `PAY-01` with a real protected mutation path and usable admin UI.
Output: worker-rate validation, admin DAL queries and mutation, protected admin workers page, and UI components for listing and updating current rates.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-foundation/01-CONTEXT.md
@.planning/phases/01-access-foundation/01-RESEARCH.md
@.planning/phases/01-access-foundation/01-03-PLAN.md
@CLAUDE.md
@.agents/skills/next-best-practices/SKILL.md

<interfaces>
From src/lib/auth/types.ts:
```ts
export interface WorkerRateRecord {
  userId: string;
  hourlyRateCents: number;
  updatedBy: string;
  updatedAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validation and admin-only worker-rate DAL behavior</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/lib/dal/admin.ts
    tests/access/worker-rates.test.ts
  </read_first>
  <files>
    src/lib/validation/worker-rate.ts
    src/lib/dal/admin.ts
    tests/access/worker-rates.test.ts
  </files>
  <behavior>
    - Test 1: worker-rate input rejects zero, negative, decimal, and invalid values.
    - Test 2: admin-only DAL writes one current worker-rate row with audit updates.
    - Test 3: no worker-rate history or ledger table behavior is introduced in Phase 1.
  </behavior>
  <action>Create `src/lib/validation/worker-rate.ts` with a shared Zod schema for positive integer-cent rate updates. Extend `src/lib/dal/admin.ts` with `listWorkersWithRates` and `upsertWorkerRate` using the existing `worker_rates` current-value table. Preserve D-09, D-10, and D-11 by updating the single current row, writing `updated_by` and `updated_at`, and explicitly avoiding any history table, append-only event record, or rate-versioning feature.</action>
  <verify>
    <automated>pnpm exec vitest run tests/access/worker-rates.test.ts</automated>
  </verify>
  <acceptance_criteria>Worker-rate validation is shared, the DAL mutation is admin-only, and rate writes update the current row plus audit columns without creating out-of-scope history behavior.</acceptance_criteria>
  <done>The server-side path for `PAY-01` is complete and still aligned to the Phase 1 storage decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the protected admin worker-rate page and components</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    src/lib/validation/worker-rate.ts
    src/lib/dal/admin.ts
    tests/access/worker-rates.test.ts
  </read_first>
  <files>
    src/app/(admin)/workers/page.tsx
    src/app/(admin)/workers/_components/worker-rate-form.tsx
    src/app/(admin)/workers/_components/worker-rate-table.tsx
    tests/access/worker-rates.test.ts
  </files>
  <behavior>
    - Test 1: admins can load the protected worker-rate page and see current worker/rate data.
    - Test 2: valid submissions update the displayed current rate.
    - Test 3: invalid inputs stay blocked and worker users cannot use the page.
  </behavior>
  <action>Create the admin UI under `src/app/(admin)/workers/`. The page should load worker and current-rate data from `src/lib/dal/admin.ts`. The table component should show worker identity plus the current stored rate. The form component should submit updates using the shared validation schema and display validation or save feedback. Keep the UI intentionally operational for an internal tool; do not add rate-history UI because D-11 defers that feature.</action>
  <verify>
    <automated>pnpm exec vitest run tests/access/worker-rates.test.ts && pnpm build</automated>
  </verify>
  <acceptance_criteria>Admins have a functioning protected page to view and update current worker rates, invalid input is rejected, and the UI does not imply nonexistent history or ledger features.</acceptance_criteria>
  <done>Phase 1 closes with a usable admin-only worker-rate workflow that respects the current-value storage boundary.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run tests/access/worker-rates.test.ts` and `pnpm build`. Confirm worker-rate writes remain admin-only in DAL code and that the UI reflects only the current stored value plus audit-backed updates.
</verification>

<success_criteria>
Admins can manage current worker rates through a protected page, the mutation path is validated and server-authorized, and Phase 1 still avoids out-of-scope history or ledger work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-foundation/01-access-foundation-04-SUMMARY.md`
</output>
