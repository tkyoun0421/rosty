---
phase: 01-access-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - vitest.config.ts
  - .env.example
  - src/app/layout.tsx
  - src/lib/auth/types.ts
  - src/lib/auth/config.ts
  - tests/access/invite-flow.test.ts
  - tests/access/google-auth.test.ts
  - tests/access/rbac.test.ts
  - tests/access/worker-rates.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - PAY-01
must_haves:
  truths:
    - "Phase 1 begins from a real Next.js and Vitest scaffold instead of planning-only paths."
    - "Every Phase 1 requirement has an executable test target before implementation starts."
    - "Downstream plans share one auth contract and env contract instead of inventing them per file."
  artifacts:
    - path: tsconfig.json
      provides: "@/* alias for deliberate new src scaffold"
    - path: vitest.config.ts
      provides: matching test alias resolution for the new scaffold
    - path: src/app/layout.tsx
      provides: minimal Next.js app entry so later routes can compile
    - path: src/lib/auth/types.ts
      provides: shared role, invite, and worker-rate contracts
    - path: src/lib/auth/config.ts
      provides: centralized auth env and route constants
    - path: tests/access/invite-flow.test.ts
      provides: AUTH-01 contract coverage scaffold
    - path: tests/access/google-auth.test.ts
      provides: AUTH-02 contract coverage scaffold
    - path: tests/access/rbac.test.ts
      provides: AUTH-03 contract coverage scaffold
    - path: tests/access/worker-rates.test.ts
      provides: PAY-01 contract coverage scaffold
  key_links:
    - from: tests/access/google-auth.test.ts
      to: src/lib/auth/config.ts
      via: callback and post-login route constants
      pattern: ROOT_REDIRECT_PATH|AUTH_CALLBACK_PATH
    - from: tests/access/rbac.test.ts
      to: src/lib/auth/types.ts
      via: shared role contract
      pattern: AppRole
    - from: vitest.config.ts
      to: tsconfig.json
      via: matching alias contract for new files
      pattern: "@/"
---

<objective>
Create the executable skeleton for Phase 1 in a repository that currently has planning artifacts and root config, but almost no runtime files.

Purpose: turn the workspace from plan-only into a deliberate scaffold that later auth, invite, RBAC, and pay tasks can build on safely.
Output: alias updates, minimal Next.js app entry, shared auth contracts, env contract, and one runnable test target per Phase 1 requirement.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-foundation/01-CONTEXT.md
@.planning/phases/01-access-foundation/01-RESEARCH.md
@.planning/phases/01-access-foundation/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
@package.json
@tsconfig.json
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebaseline the workspace for a deliberate `src/` scaffold</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/codebase/ARCHITECTURE.md
    .planning/codebase/CONVENTIONS.md
    tsconfig.json
    vitest.config.ts
  </read_first>
  <files>
    tsconfig.json
    vitest.config.ts
    src/app/layout.tsx
  </files>
  <action>Update `tsconfig.json` and `vitest.config.ts` so the fresh scaffold can use `@/*` imports pointing at `src/*` while preserving the existing proven config. Create `src/app/layout.tsx` as the minimal App Router shell required for a previously empty repository to compile. Do not assume any legacy app directories exist; scaffold only what this phase needs, per the refreshed architecture map.</action>
  <verify>
    <automated>pnpm exec vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>`src/app/layout.tsx` exists, TypeScript and Vitest both resolve the same `@/*` alias contract, and the plan no longer assumes missing legacy runtime files are already present.</acceptance_criteria>
  <done>The repository has a deliberate `src/` entrypoint and aligned alias configuration for downstream Phase 1 work.</done>
</task>

<task type="auto">
  <name>Task 2: Define shared auth contracts and the explicit env surface</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    .planning/codebase/CONCERNS.md
    .env.example
  </read_first>
  <files>
    .env.example
    src/lib/auth/types.ts
    src/lib/auth/config.ts
  </files>
  <action>Create `src/lib/auth/types.ts` with the contracts downstream plans must share: `AppRole`, `AuthSessionUser`, `InviteStatus`, `InviteTokenRecord`, and `WorkerRateRecord`. Create `src/lib/auth/config.ts` as the only source of truth for auth env names and route constants, including `/`, `/auth/callback`, `/sign-in`, and `/unauthorized`. Update `.env.example` with the exact Phase 1 env surface for Supabase SSR auth and Google provider setup. Keep the root redirect constant aligned with D-06 so later callback work always returns to `/` first.</action>
  <verify>
    <automated>pnpm exec vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>The repo has one shared runtime contract for roles, invites, worker rates, and auth env vars, and the post-login root path is explicitly defined as `/` for later use per D-06.</acceptance_criteria>
  <done>Shared auth and env contracts exist and downstream plans can import them without re-deciding route or role shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add executable Phase 1 contract tests for the empty-repo restart</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-VALIDATION.md
    src/lib/auth/types.ts
    src/lib/auth/config.ts
    vitest.config.ts
  </read_first>
  <files>
    tests/access/invite-flow.test.ts
    tests/access/google-auth.test.ts
    tests/access/rbac.test.ts
    tests/access/worker-rates.test.ts
  </files>
  <behavior>
    - Test 1: invite acceptance contract is token-based and not email-match enforced per D-01, D-02, and D-03.
    - Test 2: login callback contract returns to `/` first and role routing happens there per D-06 and D-08.
    - Test 3: unauthorized role access resolves to `/unauthorized` per D-07.
    - Test 4: worker rates are current-value only with audit fields and no history ledger per D-09, D-10, and D-11.
  </behavior>
  <action>Create the four Phase 1 test files referenced in `01-VALIDATION.md`. These are not placeholder TODO files; each should encode concrete contract assertions against exported constants, schemas, or helper stubs so the suite runs in the restarted repo. Write the expectations to reflect the new locked context decisions, not the older research recommendation for email matching.</action>
  <verify>
    <automated>pnpm exec vitest run tests/access/invite-flow.test.ts tests/access/google-auth.test.ts tests/access/rbac.test.ts tests/access/worker-rates.test.ts</automated>
  </verify>
  <acceptance_criteria>All four Phase 1 requirement areas have executable test targets, and those tests encode the revised token-only invite rule, root-first routing rule, unauthorized route, and current-value worker-rate rule.</acceptance_criteria>
  <done>The phase now starts from runnable contracts instead of stale assumptions embedded only in markdown.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run tests/access/invite-flow.test.ts tests/access/google-auth.test.ts tests/access/rbac.test.ts tests/access/worker-rates.test.ts`. Confirm the test assertions reflect D-01 through D-11 where relevant and that `@/*` resolves in both TypeScript and Vitest.
</verification>

<success_criteria>
The previously empty runtime scaffold is replaced with a deliberate Phase 1 skeleton, every requirement has an executable contract test, and downstream plans inherit one consistent auth/env contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-foundation/01-access-foundation-01-SUMMARY.md`
</output>
