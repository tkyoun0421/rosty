---
phase: 01-access-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - supabase/migrations/20260331_phase1_access_foundation.sql
  - supabase/seed.sql
  - src/lib/dal/current-user.ts
  - src/lib/dal/admin.ts
  - src/lib/auth/guards.ts
  - src/lib/auth/invites.ts
  - src/app/(admin)/layout.tsx
  - src/app/(worker)/layout.tsx
  - src/app/(admin)/invites/page.tsx
  - src/app/(public)/invite/[token]/page.tsx
  - src/app/auth/callback/route.ts
  - tests/access/invite-flow.test.ts
  - tests/access/rbac.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
user_setup:
  - service: supabase
    why: "Schema migration, JWT claim verification, and RLS behavior need a real Supabase project or linked CLI target."
    env_vars:
      - name: SUPABASE_PROJECT_REF
        source: "Supabase Dashboard -> General Settings"
      - name: SUPABASE_DB_PASSWORD
        source: "Supabase Dashboard -> Database"
    dashboard_config:
      - task: "Apply the Phase 1 SQL migration through linked CLI or SQL editor"
        location: "Supabase CLI or Supabase Dashboard -> SQL Editor"
must_haves:
  truths:
    - "The first admin is created by explicit bootstrap data, not by first-login auto-promotion."
    - "Admins can issue and revoke invite links."
    - "Invite acceptance depends on a valid token and authenticated session, not on email-match enforcement."
    - "Secure admin and worker access is enforced in DAL and guard code, not by proxy alone."
  artifacts:
    - path: supabase/migrations/20260331_phase1_access_foundation.sql
      provides: profiles, user_roles, invites, worker_rates schema plus RLS and JWT claim hook
    - path: supabase/seed.sql
      provides: manual admin bootstrap data
    - path: src/lib/dal/current-user.ts
      provides: DB-canonical current-user lookup
    - path: src/lib/auth/guards.ts
      provides: admin and worker server guards
    - path: src/lib/auth/invites.ts
      provides: invite create, revoke, lookup, and accept logic
    - path: src/app/(admin)/invites/page.tsx
      provides: admin invite management UI
    - path: src/app/(public)/invite/[token]/page.tsx
      provides: token-preserving public invite entrypoint
  key_links:
    - from: supabase/seed.sql
      to: supabase/migrations/20260331_phase1_access_foundation.sql
      via: bootstrap admin row aligned to created schema
      pattern: user_roles|profiles
    - from: src/app/auth/callback/route.ts
      to: src/lib/auth/invites.ts
      via: token-based invite acceptance after session exchange
      pattern: acceptInvite
    - from: src/lib/auth/guards.ts
      to: src/lib/dal/current-user.ts
      via: secure role enforcement
      pattern: requireAdminUser|requireWorkerUser
---

<objective>
Implement the database-backed access model for invites and RBAC, using the new token-based invite rule and explicit admin bootstrap rule.

Purpose: move security-sensitive decisions into the database, DAL, and server guards once the auth transport shell already exists.
Output: Phase 1 schema and seed files, admin bootstrap path, invite lifecycle logic, server-side role guards, and invite/admin route surfaces.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-foundation/01-CONTEXT.md
@.planning/phases/01-access-foundation/01-RESEARCH.md
@.planning/phases/01-access-foundation/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-access-foundation/01-02-PLAN.md
@CLAUDE.md
@.agents/skills/supabase-postgres-best-practices/SKILL.md

<interfaces>
From src/lib/auth/types.ts:
```ts
export type AppRole = "admin" | "worker";
export type InviteStatus = "pending" | "accepted" | "revoked" | "expired";
export interface InviteTokenRecord {
  tokenHash: string;
  role: AppRole;
  expiresAt: string;
  acceptedAt: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 1 schema, JWT role claim, and manual admin bootstrap</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    .planning/codebase/CONCERNS.md
    tests/access/invite-flow.test.ts
    tests/access/rbac.test.ts
  </read_first>
  <files>
    supabase/migrations/20260331_phase1_access_foundation.sql
    supabase/seed.sql
    tests/access/invite-flow.test.ts
    tests/access/rbac.test.ts
  </files>
  <behavior>
    - Test 1: the schema defines `profiles`, `user_roles`, `invites`, and `worker_rates` with RLS enabled.
    - Test 2: `worker_rates` stores one current value per worker with `updated_by` and `updated_at`, not a history ledger.
    - Test 3: admin bootstrap is explicit seed data and not first-login auto-promotion.
  </behavior>
  <action>Write a single Phase 1 migration that creates the canonical access tables, the `app_role` enum, the JWT access-token hook, and RLS policies. Implement D-09, D-10, and D-11 by making `worker_rates` a one-row-per-worker current-value table with `hourly_rate_cents`, `updated_by`, and `updated_at` only; do not add rate history or append-only compensation events. Implement D-04 and D-05 by adding explicit admin bootstrap records in `supabase/seed.sql`; do not auto-promote the first user who logs in.</action>
  <verify>
    <automated>pnpm exec vitest run tests/access/invite-flow.test.ts tests/access/rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>The schema creates the minimum Phase 1 access model, the JWT claim and RLS contract are encoded in SQL, the admin bootstrap is explicit seed/setup data, and no auto-promotion or rate-history ledger slips into scope.</acceptance_criteria>
  <done>The database becomes the canonical source of Phase 1 roles, invites, and current worker rates, with bootstrap handled manually.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement token-based invite lifecycle and callback completion</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/lib/auth/session.ts
    src/app/auth/callback/route.ts
    tests/access/invite-flow.test.ts
  </read_first>
  <files>
    src/lib/dal/admin.ts
    src/lib/auth/invites.ts
    src/app/(admin)/invites/page.tsx
    src/app/(public)/invite/[token]/page.tsx
    src/app/auth/callback/route.ts
    tests/access/invite-flow.test.ts
  </files>
  <behavior>
    - Test 1: admins can create and revoke invite tokens.
    - Test 2: invite acceptance requires a valid token and authenticated session, but does not require email-match enforcement.
    - Test 3: successful invite completion still returns to `/` first after callback processing.
  </behavior>
  <action>Implement the app-owned invite flow. `src/lib/auth/invites.ts` should expose `createInvite`, `revokeInvite`, `getInviteByToken`, and `acceptInvite`. `src/lib/dal/admin.ts` should own admin-only invite reads and writes. Build `src/app/(admin)/invites/page.tsx` for invite issuance and revocation and `src/app/(public)/invite/[token]/page.tsx` to preserve token possession through Google login. Update `src/app/auth/callback/route.ts` so, after session exchange, it completes onboarding with token validation per D-01, D-02, and D-03: token validity is required, authenticated email-match enforcement is not. Redirect to `/` after success per D-06.</action>
  <verify>
    <automated>pnpm exec vitest run tests/access/invite-flow.test.ts</automated>
  </verify>
  <acceptance_criteria>Invite issuance and revocation work for admins, invite completion is token-based without email-match blocking, and callback success still returns to the common root before any role-specific internal route.</acceptance_criteria>
  <done>The onboarding flow now matches the captured user decision instead of the older email-match research recommendation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add DAL-first current-user lookup and secure role guards</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/lib/auth/session.ts
    tests/access/rbac.test.ts
  </read_first>
  <files>
    src/lib/dal/current-user.ts
    src/lib/auth/guards.ts
    src/app/(admin)/layout.tsx
    src/app/(worker)/layout.tsx
    tests/access/rbac.test.ts
  </files>
  <behavior>
    - Test 1: secure admin guards reject worker and anonymous access.
    - Test 2: secure worker guards reject anonymous or unauthorized access.
    - Test 3: protected layouts depend on DAL-backed role checks rather than proxy-only hints.
  </behavior>
  <action>Implement `src/lib/dal/current-user.ts` as the DB-canonical lookup for profile and role. Add `src/lib/auth/guards.ts` with `requireAdminUser` and `requireWorkerUser` for use in pages, route handlers, and server actions. Create minimal `(admin)` and `(worker)` layouts that call those guards. Keep D-08 intact by treating `proxy.ts` as optional routing help only; secure authorization must live in these DAL and guard modules.</action>
  <verify>
    <automated>pnpm exec vitest run tests/access/rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>Protected route groups perform DB-backed server authorization, and proxy-based hints are no longer the only control standing between roles and restricted pages.</acceptance_criteria>
  <done>Role separation is enforced at the secure server boundary required for Phase 1.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run tests/access/invite-flow.test.ts tests/access/rbac.test.ts`. Apply the Phase 1 migration through linked Supabase CLI or SQL editor and confirm the seed/bootstrap path creates the first admin explicitly rather than through first-login behavior.
</verification>

<success_criteria>
Phase 1 has DB-canonical roles and invites, token-based invite acceptance, explicit admin bootstrap, and secure DAL-backed role enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-foundation/01-access-foundation-03-SUMMARY.md`
</output>
