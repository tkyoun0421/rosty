---
phase: 01-access-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - supabase/migrations/20260331_phase1_access_foundation.sql
  - supabase/seed.sql
  - src/queries/access/dal/get-current-user.ts
  - src/queries/access/dal/get-current-user.test.ts
  - src/mutations/invite/dal/invite-dal.ts
  - src/mutations/invite/actions/create-invite.ts
  - src/mutations/invite/actions/create-invite.test.ts
  - src/mutations/invite/actions/accept-invite.ts
  - src/mutations/invite/actions/accept-invite.test.ts
  - src/flows/admin-shell/components/AdminShellPage.tsx
  - src/flows/admin-shell/components/AdminShellPage.test.tsx
  - src/flows/worker-shell/components/WorkerShellPage.tsx
  - src/flows/worker-shell/components/WorkerShellPage.test.tsx
  - src/flows/admin-invites/components/AdminInvitesPage.tsx
  - src/app/admin/page.tsx
  - src/app/admin/invites/page.tsx
  - src/app/worker/page.tsx
  - src/app/invite/[token]/page.tsx
  - src/app/auth/callback/route.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
user_setup:
  - service: supabase
    why: "Schema migration, JWT claim verification, and RLS behavior need a real Supabase project or linked CLI target."
    env_vars:
      - name: SUPABASE_PROJECT_REF
        source: "Supabase Dashboard -> General Settings"
      - name: SUPABASE_DB_PASSWORD
        source: "Supabase Dashboard -> Database"
must_haves:
  truths:
    - "The first admin is created by explicit bootstrap data, not first-login auto-promotion."
    - "Admins can create and revoke invite links."
    - "Invite acceptance depends on valid token possession after authentication, not on email-match enforcement."
    - "Secure role access is enforced through DB-backed reads and server guards, not by `proxy.ts` alone."
  artifacts:
    - path: supabase/migrations/20260331_phase1_access_foundation.sql
      provides: Phase 1 schema, RLS, and JWT role-claim hook
    - path: supabase/seed.sql
      provides: manual first-admin bootstrap
    - path: src/mutations/invite/dal/invite-dal.ts
      provides: write-side invite persistence
    - path: src/mutations/invite/actions/create-invite.ts
      provides: admin-only invite issuance command
    - path: src/mutations/invite/actions/accept-invite.ts
      provides: token-based invite completion command
    - path: src/app/invite/[token]/page.tsx
      provides: token-preserving invite entry route
    - path: src/flows/admin-shell/components/AdminShellPage.tsx
      provides: admin route flow shell with secure role dependency
    - path: src/flows/worker-shell/components/WorkerShellPage.tsx
      provides: worker route flow shell with secure role dependency
  key_links:
    - from: supabase/seed.sql
      to: supabase/migrations/20260331_phase1_access_foundation.sql
      via: bootstrap data aligned to created schema
      pattern: profiles|user_roles
    - from: src/app/auth/callback/route.ts
      to: src/mutations/invite/actions/accept-invite.ts
      via: token-based onboarding completion after session exchange
      pattern: acceptInvite
    - from: src/app/admin/page.tsx
      to: src/flows/admin-shell/components/AdminShellPage.tsx
      via: one route to one flow
      pattern: AdminShellPage
---

<objective>
Move the access boundary into the database, read/write slices, and server-owned route shells using the new token-based invite rule and manual admin bootstrap rule.

Purpose: replace transport-only auth with DB-canonical roles, invite lifecycle control, and secure route access.
Output: Phase 1 schema and seed files, invite DAL/actions, token invite route, callback completion, and admin/worker route shells that depend on secure role reads.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-foundation/01-CONTEXT.md
@.planning/phases/01-access-foundation/01-RESEARCH.md
@.planning/phases/01-access-foundation/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-access-foundation/01-02-PLAN.md
@CLAUDE.md
@src/shared/model/access.ts
@src/shared/config/auth-config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 1 schema, JWT role claim, and manual admin bootstrap</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/shared/model/access.ts
  </read_first>
  <files>
    supabase/migrations/20260331_phase1_access_foundation.sql
    supabase/seed.sql
    src/queries/access/dal/get-current-user.test.ts
  </files>
  <action>Write `supabase/migrations/20260331_phase1_access_foundation.sql` to create `profiles`, `user_roles`, `invites`, and `worker_rates`, enable RLS, and add the JWT role-claim hook. Implement `worker_rates` as a one-row-per-worker current-value table with `hourly_rate_cents`, `updated_by`, and `updated_at` only. Write `supabase/seed.sql` with explicit first-admin bootstrap data. Do not auto-promote the first user who logs in. Extend `src/queries/access/dal/get-current-user.test.ts` so it asserts the role comes from the DB-backed model contract, not client-only metadata.</action>
  <verify>
    <automated>pnpm exec vitest run src/queries/access/dal/get-current-user.test.ts</automated>
  </verify>
  <acceptance_criteria>The migration file contains `profiles`, `user_roles`, `invites`, and `worker_rates`, `worker_rates` contains `updated_by` and `updated_at` but no history table, and `supabase/seed.sql` contains explicit admin bootstrap inserts or updates.</acceptance_criteria>
  <done>The database becomes the canonical source of Phase 1 identity, role, invite, and current-rate data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement token-based invite write path and callback completion</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/app/auth/callback/route.ts
    src/shared/model/access.ts
  </read_first>
  <files>
    src/mutations/invite/dal/invite-dal.ts
    src/mutations/invite/actions/create-invite.ts
    src/mutations/invite/actions/create-invite.test.ts
    src/mutations/invite/actions/accept-invite.ts
    src/mutations/invite/actions/accept-invite.test.ts
    src/flows/admin-invites/components/AdminInvitesPage.tsx
    src/app/admin/invites/page.tsx
    src/app/invite/[token]/page.tsx
    src/app/auth/callback/route.ts
  </files>
  <action>Create `src/mutations/invite/dal/invite-dal.ts` for invite persistence. Create `src/mutations/invite/actions/create-invite.ts` and `src/mutations/invite/actions/accept-invite.ts`. The accept action must validate token possession and authenticated session, but must not enforce email equality. Add colocated tests asserting token-based acceptance, invalid/revoked handling, and admin-only invite creation. Create `src/flows/admin-invites/components/AdminInvitesPage.tsx` and wire `src/app/admin/invites/page.tsx` to it. Create `src/app/invite/[token]/page.tsx` so the token survives the Google sign-in handoff. Update `src/app/auth/callback/route.ts` so onboarding completion runs through `accept-invite.ts` and still redirects to `/` after success.</action>
  <verify>
    <automated>pnpm exec vitest run src/mutations/invite/actions/create-invite.test.ts src/mutations/invite/actions/accept-invite.test.ts</automated>
  </verify>
  <acceptance_criteria>`create-invite.ts` and `accept-invite.ts` exist under `src/mutations/invite/actions/`, `src/app/invite/[token]/page.tsx` exists, the two colocated invite tests pass, and no email-match enforcement is introduced in the accept path.</acceptance_criteria>
  <done>The onboarding path now matches the token-only invite decision instead of the earlier email-match research preference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add secure admin and worker route shells backed by role reads</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/codebase/ARCHITECTURE.md
    src/queries/access/dal/get-current-user.ts
  </read_first>
  <files>
    src/queries/access/dal/get-current-user.ts
    src/flows/admin-shell/components/AdminShellPage.tsx
    src/flows/admin-shell/components/AdminShellPage.test.tsx
    src/flows/worker-shell/components/WorkerShellPage.tsx
    src/flows/worker-shell/components/WorkerShellPage.test.tsx
    src/app/admin/page.tsx
    src/app/worker/page.tsx
  </files>
  <action>Extend `src/queries/access/dal/get-current-user.ts` so it performs DB-canonical profile and role lookup needed for secure route entry. Create `src/flows/admin-shell/components/AdminShellPage.tsx` and `src/flows/worker-shell/components/WorkerShellPage.tsx` with colocated tests asserting that admin routes require admin role and worker routes reject unauthorized access. Wire `src/app/admin/page.tsx` and `src/app/worker/page.tsx` as thin route entries to those flows. Keep secure route access in read-side server logic and route flow composition, not in `proxy.ts` alone.</action>
  <verify>
    <automated>pnpm exec vitest run src/queries/access/dal/get-current-user.test.ts src/flows/admin-shell/components/AdminShellPage.test.tsx src/flows/worker-shell/components/WorkerShellPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>`src/app/admin/page.tsx` and `src/app/worker/page.tsx` are thin flow entries, the admin and worker shell tests pass, and the secure role check depends on DB-backed current-user lookup rather than edge-only hints.</acceptance_criteria>
  <done>Role separation is enforced at the secure server boundary needed for Phase 1.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/queries/access/dal/get-current-user.test.ts src/mutations/invite/actions/create-invite.test.ts src/mutations/invite/actions/accept-invite.test.ts src/flows/admin-shell/components/AdminShellPage.test.tsx src/flows/worker-shell/components/WorkerShellPage.test.tsx`. Apply the Phase 1 migration and confirm the first admin is created explicitly through seed/bootstrap data.
</verification>

<success_criteria>
Phase 1 has DB-canonical roles and invites, token-based invite acceptance, explicit admin bootstrap, and secure admin/worker route entry backed by server reads.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-foundation/01-access-foundation-03-SUMMARY.md`
</output>
