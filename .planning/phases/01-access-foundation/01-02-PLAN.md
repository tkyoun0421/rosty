---
phase: 01-access-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/shared/lib/supabase/browser-client.ts
  - src/shared/lib/supabase/server-client.ts
  - src/shared/lib/supabase/admin-client.ts
  - src/queries/access/dal/get-current-user.ts
  - src/queries/access/dal/get-current-user.test.ts
  - src/mutations/auth/actions/start-google-sign-in.ts
  - src/mutations/auth/actions/start-google-sign-in.test.ts
  - src/flows/auth-shell/components/SignInPage.tsx
  - src/flows/auth-shell/components/RootRedirectPage.tsx
  - src/flows/auth-shell/components/RootRedirectPage.test.tsx
  - src/app/sign-in/page.tsx
  - src/app/page.tsx
  - src/app/auth/callback/route.ts
  - src/app/auth/callback/route.test.ts
  - src/app/unauthorized/page.tsx
  - proxy.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
user_setup:
  - service: supabase
    why: "Google OAuth and SSR session exchange need a real Supabase project and provider configuration."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API"
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> OAuth client"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console -> OAuth client"
must_haves:
  truths:
    - "Users can start Google sign-in from the app."
    - "Successful OAuth callback returns to `/` first."
    - "The root route performs the internal role split."
    - "The proxy remains coarse and does not become the final security boundary."
  artifacts:
    - path: src/shared/lib/supabase/browser-client.ts
      provides: browser-side Supabase SSR client wrapper
    - path: src/shared/lib/supabase/server-client.ts
      provides: server-side Supabase SSR client wrapper
    - path: src/shared/lib/supabase/admin-client.ts
      provides: service-role client wrapper for later admin-side work
    - path: src/queries/access/dal/get-current-user.ts
      provides: read-side current-user DTO lookup
    - path: src/mutations/auth/actions/start-google-sign-in.ts
      provides: write-side Google sign-in entry action
    - path: src/flows/auth-shell/components/RootRedirectPage.tsx
      provides: flow-owned root-first route composition
    - path: src/app/auth/callback/route.ts
      provides: OAuth code-exchange route
    - path: proxy.ts
      provides: coarse redirect logic only
  key_links:
    - from: src/app/sign-in/page.tsx
      to: src/flows/auth-shell/components/SignInPage.tsx
      via: one route to one flow
      pattern: SignInPage
    - from: src/flows/auth-shell/components/RootRedirectPage.tsx
      to: src/queries/access/dal/get-current-user.ts
      via: root-first role lookup
      pattern: getCurrentUser
    - from: src/app/auth/callback/route.ts
      to: src/shared/config/auth-config.ts
      via: callback redirect constants
      pattern: AUTH_CALLBACK_PATH|ROOT_PATH
---

<objective>
Build the auth transport shell and root-first routing flow required by the new Phase 1 decisions and the refreshed codebase contract.

Purpose: establish Google sign-in transport, SSR session access, and root-entry role routing before database-backed invite and RBAC logic lands.
Output: Supabase client wrappers, current-user read DAL, sign-in action, auth flow UI, callback route, root route, unauthorized page, and narrow `proxy.ts` behavior.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-foundation/01-CONTEXT.md
@.planning/phases/01-access-foundation/01-RESEARCH.md
@.planning/phases/01-access-foundation/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-access-foundation/01-01-PLAN.md
@CLAUDE.md
@package.json
@src/shared/config/auth-config.ts
@src/shared/model/access.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build shared Supabase wrappers and read-side current-user lookup</name>
  <read_first>
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/shared/config/auth-config.ts
    src/shared/model/access.ts
  </read_first>
  <files>
    src/shared/lib/supabase/browser-client.ts
    src/shared/lib/supabase/server-client.ts
    src/shared/lib/supabase/admin-client.ts
    src/queries/access/dal/get-current-user.ts
    src/queries/access/dal/get-current-user.test.ts
  </files>
  <action>Create `src/shared/lib/supabase/browser-client.ts`, `src/shared/lib/supabase/server-client.ts`, and `src/shared/lib/supabase/admin-client.ts` as low-level third-party wrappers only. Create `src/queries/access/dal/get-current-user.ts` as the read-side function that returns the Phase 1 current-user DTO shaped around `AppRole | null`. Add `src/queries/access/dal/get-current-user.test.ts` asserting that the read function returns `{ id, email, role }` and does not embed write or invite-completion behavior.</action>
  <verify>
    <automated>pnpm exec vitest run src/queries/access/dal/get-current-user.test.ts</automated>
  </verify>
  <acceptance_criteria>The Supabase wrappers live only under `src/shared/lib/supabase/*`, the current-user read exists under `src/queries/access/dal/get-current-user.ts`, and the colocated test passes without introducing `src/lib/*` files.</acceptance_criteria>
  <done>The repo has the low-level transport and read-side access primitives needed for the auth flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Google sign-in action, callback route, and root-first auth flow UI</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/shared/config/auth-config.ts
    src/queries/access/dal/get-current-user.ts
  </read_first>
  <files>
    src/mutations/auth/actions/start-google-sign-in.ts
    src/mutations/auth/actions/start-google-sign-in.test.ts
    src/flows/auth-shell/components/SignInPage.tsx
    src/flows/auth-shell/components/RootRedirectPage.tsx
    src/flows/auth-shell/components/RootRedirectPage.test.tsx
    src/app/sign-in/page.tsx
    src/app/page.tsx
    src/app/auth/callback/route.ts
    src/app/auth/callback/route.test.ts
    src/app/unauthorized/page.tsx
  </files>
  <action>Create `src/mutations/auth/actions/start-google-sign-in.ts` that starts Google OAuth using the configured callback route. Create `src/flows/auth-shell/components/SignInPage.tsx` and `src/flows/auth-shell/components/RootRedirectPage.tsx` as the composed UI for `/sign-in` and `/`. Wire `src/app/sign-in/page.tsx` to `SignInPage` and `src/app/page.tsx` to `RootRedirectPage`. Create `src/app/auth/callback/route.ts` so successful code exchange redirects to `/` exactly, not directly to `/admin` or `/worker`. Create `src/app/unauthorized/page.tsx` as the explicit unauthorized destination required by D-07. Add colocated tests asserting Google sign-in starts correctly, callback success returns to `/`, and root routing performs the internal role split.</action>
  <verify>
    <automated>pnpm exec vitest run src/mutations/auth/actions/start-google-sign-in.test.ts src/flows/auth-shell/components/RootRedirectPage.test.tsx src/app/auth/callback/route.test.ts</automated>
  </verify>
  <acceptance_criteria>`src/app/sign-in/page.tsx` and `src/app/page.tsx` are thin route files, callback success redirects to `/`, unauthorized routing is explicit, and the three colocated tests pass.</acceptance_criteria>
  <done>The app can start Google auth and land every successful login on the common root entrypoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add coarse proxy routing without leaking final authorization into the edge layer</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/codebase/ARCHITECTURE.md
    src/shared/config/auth-config.ts
    src/flows/auth-shell/components/RootRedirectPage.test.tsx
  </read_first>
  <files>
    proxy.ts
    src/flows/auth-shell/components/RootRedirectPage.test.tsx
  </files>
  <action>Create `proxy.ts` using the Next.js 16 file convention. Restrict it to optimistic redirect behavior only: anonymous traffic can be redirected away from protected routes, authenticated traffic can be redirected away from `/sign-in`, and clear mismatch can be nudged to `/unauthorized`. Do not perform final admin/worker authorization in `proxy.ts`, and keep secure checks for later DAL and guard code.</action>
  <verify>
    <automated>pnpm exec vitest run src/flows/auth-shell/components/RootRedirectPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>`proxy.ts` exists, contains only coarse redirect logic, and no DAL-grade authorization function is implemented in the proxy file.</acceptance_criteria>
  <done>Root-first UX routing is in place without turning the edge layer into the security boundary.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run src/queries/access/dal/get-current-user.test.ts src/mutations/auth/actions/start-google-sign-in.test.ts src/flows/auth-shell/components/RootRedirectPage.test.tsx src/app/auth/callback/route.test.ts`. Confirm successful callback flow returns to `/` and `proxy.ts` remains coarse-only.
</verification>

<success_criteria>
Users can initiate Google login, the callback creates a session and returns to `/`, the root route performs the internal role split, and the proxy remains an optimistic UX helper only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-foundation/01-access-foundation-02-SUMMARY.md`
</output>
