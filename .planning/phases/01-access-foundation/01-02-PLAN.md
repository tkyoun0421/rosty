---
phase: 01-access-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/page.tsx
  - src/app/(public)/sign-in/page.tsx
  - src/app/auth/callback/route.ts
  - src/app/unauthorized/page.tsx
  - src/lib/supabase/browser.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/admin.ts
  - src/lib/auth/session.ts
  - proxy.ts
  - tests/access/google-auth.test.ts
  - tests/access/rbac.test.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
user_setup:
  - service: supabase
    why: "Google OAuth and SSR session exchange need a real Supabase project and provider configuration."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API"
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> OAuth client"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console -> OAuth client"
    dashboard_config:
      - task: "Enable Google provider and register the callback URL for `/auth/callback`"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
must_haves:
  truths:
    - "Users can start Google sign-in from the app."
    - "The OAuth callback exchanges the code and returns every successful login to `/` first."
    - "The common root route decides the internal landing path based on the current role."
    - "Anonymous or clearly wrong-role traffic is redirected optimistically, while proxy stays coarse."
  artifacts:
    - path: src/lib/supabase/browser.ts
      provides: browser SSR client
    - path: src/lib/supabase/server.ts
      provides: server SSR client
    - path: src/lib/supabase/admin.ts
      provides: service-role client for later admin server work
    - path: src/lib/auth/session.ts
      provides: session and current-user helpers
    - path: src/app/(public)/sign-in/page.tsx
      provides: Google sign-in entrypoint
    - path: src/app/auth/callback/route.ts
      provides: OAuth code exchange route
    - path: src/app/page.tsx
      provides: root-first post-login router
    - path: proxy.ts
      provides: coarse route gating only
  key_links:
    - from: src/app/(public)/sign-in/page.tsx
      to: src/lib/supabase/browser.ts
      via: Google OAuth start
      pattern: signInWithOAuth
    - from: src/app/auth/callback/route.ts
      to: src/lib/supabase/server.ts
      via: code exchange
      pattern: exchangeCodeForSession
    - from: src/app/page.tsx
      to: src/lib/auth/session.ts
      via: root role-based internal routing
      pattern: getCurrentUser
    - from: proxy.ts
      to: src/lib/auth/config.ts
      via: redirect rules
      pattern: UNAUTHORIZED_PATH|SIGN_IN_PATH
---

<objective>
Implement the auth transport shell for Google sign-in, SSR session access, and the root-first routing model required by the new Phase 1 context.

Purpose: establish login transport and coarse route flow before database-backed invite and RBAC logic lands.
Output: Supabase SSR clients, session helpers, sign-in and callback routes, root router page, unauthorized page, and narrow `proxy.ts` redirects.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-foundation/01-CONTEXT.md
@.planning/phases/01-access-foundation/01-RESEARCH.md
@.planning/phases/01-access-foundation/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-access-foundation/01-01-PLAN.md
@CLAUDE.md
@package.json
@.agents/skills/next-best-practices/SKILL.md

<interfaces>
From src/lib/auth/types.ts:
```ts
export type AppRole = "admin" | "worker";
export interface AuthSessionUser {
  id: string;
  email: string;
  role: AppRole | null;
}
```

From src/lib/auth/config.ts:
```ts
export const ROOT_REDIRECT_PATH = "/";
export const SIGN_IN_PATH = "/sign-in";
export const AUTH_CALLBACK_PATH = "/auth/callback";
export const UNAUTHORIZED_PATH = "/unauthorized";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Supabase SSR clients and session helpers for the new scaffold</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/lib/auth/types.ts
    src/lib/auth/config.ts
    package.json
  </read_first>
  <files>
    src/lib/supabase/browser.ts
    src/lib/supabase/server.ts
    src/lib/supabase/admin.ts
    src/lib/auth/session.ts
    tests/access/google-auth.test.ts
    tests/access/rbac.test.ts
  </files>
  <behavior>
    - Test 1: server code can resolve the current session and a role-shaped current user DTO.
    - Test 2: browser code can start Google OAuth with the configured callback route.
  </behavior>
  <action>Implement the baseline transport using `@supabase/ssr`. Create browser, server, and service-role clients under `src/lib/supabase/`. Add `src/lib/auth/session.ts` with `getSession()` and `getCurrentUser()` returning the `AuthSessionUser` shape. Keep this layer transport-only: it should not decide invite acceptance, admin bootstrap, or final authorization yet.</action>
  <verify>
    <automated>pnpm exec vitest run tests/access/google-auth.test.ts tests/access/rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>SSR-safe Supabase clients exist for browser, server, and service-role use, and session helpers expose one consistent server-side contract for later plans.</acceptance_criteria>
  <done>Phase 1 has a reusable auth transport layer instead of scattering Supabase client creation across routes and pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Google sign-in, callback exchange, and root-first routing</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/phases/01-access-foundation/01-RESEARCH.md
    src/lib/auth/config.ts
    src/lib/auth/session.ts
    tests/access/google-auth.test.ts
  </read_first>
  <files>
    src/app/(public)/sign-in/page.tsx
    src/app/auth/callback/route.ts
    src/app/page.tsx
    src/app/unauthorized/page.tsx
    tests/access/google-auth.test.ts
    tests/access/rbac.test.ts
  </files>
  <behavior>
    - Test 1: the sign-in page starts Google OAuth and passes `/auth/callback` as the redirect target.
    - Test 2: the callback exchanges the code and redirects successful logins to `/`, not directly to a role subtree.
    - Test 3: the root page decides internal routing after login by reading the current user role.
  </behavior>
  <action>Create `src/app/(public)/sign-in/page.tsx`, `src/app/auth/callback/route.ts`, `src/app/page.tsx`, and `src/app/unauthorized/page.tsx`. Enforce D-06 by always redirecting successful logins to `/` first. Enforce D-07 by keeping unauthorized outcomes on `/unauthorized`. Implement D-08 by letting `/` perform the role-based internal routing using `getCurrentUser()` while leaving secure authorization to later DAL and guard code.</action>
  <verify>
    <automated>pnpm exec vitest run tests/access/google-auth.test.ts tests/access/rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>Successful Google login lands on `/` first, the root route performs the internal role split, and unauthorized routing is explicit without embedding secure authorization into the callback or client UI.</acceptance_criteria>
  <done>The post-login flow matches the new Phase 1 decision set and is no longer coupled to direct role-specific callback redirects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add coarse `proxy.ts` routing that stops short of final authorization</name>
  <read_first>
    .planning/phases/01-access-foundation/01-CONTEXT.md
    .planning/codebase/ARCHITECTURE.md
    src/lib/auth/config.ts
    src/lib/auth/session.ts
    tests/access/rbac.test.ts
  </read_first>
  <files>
    proxy.ts
    tests/access/rbac.test.ts
  </files>
  <behavior>
    - Test 1: anonymous users are redirected away from protected subtrees.
    - Test 2: authenticated users are redirected away from `/sign-in`.
    - Test 3: clear role mismatch can be redirected to `/unauthorized`, but secure checks remain server-side.
  </behavior>
  <action>Create `proxy.ts` using the Next.js 16 convention, not `middleware.ts`. Keep it deliberately narrow per D-08: read cookies/session state for optimistic redirects, prevent anonymous access to protected route groups, and avoid placing final RBAC or invite acceptance logic in the proxy. The proxy is a UX shortcut only, not the security boundary.</action>
  <verify>
    <automated>pnpm exec vitest run tests/access/rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>`proxy.ts` exists, handles coarse redirects only, and leaves the secure role and invite decisions to the later DAL/guard layer.</acceptance_criteria>
  <done>Optimistic routing is in place without violating the root-first and DAL-first authorization decisions.</done>
</task>

</tasks>

<verification>
Run `pnpm exec vitest run tests/access/google-auth.test.ts tests/access/rbac.test.ts` and `pnpm build`. Confirm successful callback flow returns to `/`, and confirm `proxy.ts` is coarse-only rather than the final authorization layer.
</verification>

<success_criteria>
Users can initiate Google login, the callback creates a session and returns to `/`, the root route handles the role split, and the proxy only performs optimistic redirects.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-foundation/01-access-foundation-02-SUMMARY.md`
</output>
