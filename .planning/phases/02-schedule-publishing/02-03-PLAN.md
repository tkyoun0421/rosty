---
phase: 02-schedule-publishing
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/queries/schedule/dal/listRecruitingSchedules.ts
  - src/queries/schedule/dal/listRecruitingSchedules.test.ts
  - src/queries/application/dal/listMyScheduleApplicationIds.ts
  - src/mutations/application/dal/scheduleApplicationDal.ts
  - src/mutations/application/actions/createScheduleApplication.ts
  - src/mutations/application/actions/createScheduleApplication.test.ts
  - src/mutations/application/actions/submitScheduleApplication.ts
  - src/mutations/application/components/ApplyToScheduleButton.tsx
  - src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
  - src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx
  - src/flows/worker-schedules/components/WorkerScheduleList.tsx
  - src/app/worker/schedules/page.tsx
autonomous: true
requirements:
  - APPL-01
  - APPL-02
must_haves:
  truths:
    - "A worker can open a lightweight list of recruiting schedules."
    - "The list shows only the minimal Phase 2 fields: date, time, recruiting/open state, and apply/applied state."
    - "A worker can submit exactly one application per schedule without choosing a role."
  artifacts:
    - path: src/queries/schedule/dal/listRecruitingSchedules.ts
      provides: "Worker recruiting list read model"
    - path: src/queries/application/dal/listMyScheduleApplicationIds.ts
      provides: "Worker-specific applied-state lookup"
    - path: src/mutations/application/dal/scheduleApplicationDal.ts
      provides: "Write-side application persistence"
    - path: src/mutations/application/actions/createScheduleApplication.ts
      provides: "Worker-only schedule-level application action"
    - path: src/mutations/application/components/ApplyToScheduleButton.tsx
      provides: "Mutation-scoped apply trigger UI"
    - path: src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
      provides: "Worker recruiting list page flow"
    - path: src/app/worker/schedules/page.tsx
      provides: "Thin worker schedule route"
  key_links:
    - from: src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
      to: src/queries/schedule/dal/listRecruitingSchedules.ts
      via: "server-first worker read"
      pattern: "listRecruitingSchedules"
    - from: src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
      to: src/queries/application/dal/listMyScheduleApplicationIds.ts
      via: "applied-state merge for lightweight list UI"
      pattern: "listMyScheduleApplicationIds"
    - from: src/mutations/application/actions/createScheduleApplication.ts
      to: src/mutations/application/dal/scheduleApplicationDal.ts
      via: "RLS-backed insert for one application per worker per schedule"
      pattern: "schedule_applications"
---

<objective>
Build the worker recruiting list and the one-click schedule-level application path.

Purpose: satisfy APPL-01 and APPL-02 with server-first reads and RLS-backed worker writes while keeping Phase 2 intentionally narrow.
Output: worker recruiting queries, application mutation slice, lightweight worker flow, and the `/worker/schedules` route.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schedule-publishing/02-CONTEXT.md
@.planning/phases/02-schedule-publishing/02-RESEARCH.md
@.planning/phases/02-schedule-publishing/02-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-schedule-publishing/02-01-PLAN.md
@CLAUDE.md
@src/shared/lib/supabase/serverClient.ts
@src/queries/access/dal/getCurrentUser.ts
@src/app/worker/page.tsx
@src/flows/worker-shell/components/WorkerShellPage.tsx

<interfaces>
From src/shared/lib/supabase/serverClient.ts:
```ts
export async function getServerSupabaseClient();
```

From src/queries/access/dal/getCurrentUser.ts:
```ts
export interface CurrentUser {
  id: string;
  email: string;
  role: AppRole | null;
}

export async function getCurrentUser(): Promise<CurrentUser | null>;
```

From src/app/worker/page.tsx:
```ts
export default async function WorkerPage();
```

From src/flows/worker-shell/components/WorkerShellPage.tsx:
```ts
export async function WorkerShellPage();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worker recruiting read contracts and missing test scaffolds</name>
  <files>
    src/queries/schedule/dal/listRecruitingSchedules.ts
    src/queries/schedule/dal/listRecruitingSchedules.test.ts
    src/queries/application/dal/listMyScheduleApplicationIds.ts
    src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx
    src/mutations/application/actions/createScheduleApplication.test.ts
  </files>
  <action>Create the server-first recruiting read DAL and application-id helper needed for worker visibility. The recruiting query must only surface schedules in `recruiting` status and should shape a minimal list item contract consistent with D-11 through D-13. Also create the missing Wave 0 files called out by validation: `src/queries/schedule/dal/listRecruitingSchedules.test.ts`, `src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx`, and `src/mutations/application/actions/createScheduleApplication.test.ts`. Do not add worker detail reads, role-choice reads, or applicant-count reads in this plan.</action>
  <action>Create the server-first recruiting read DAL and application-id helper needed for worker visibility. The recruiting query must only surface schedules in `recruiting` status and should shape a minimal worker-facing list item contract consistent with D-11 through D-13. If worker reads later reuse admin schedule list types, make that explicit during execution; otherwise keep a worker-specific DTO local to this plan so Wave 2 remains dependency-safe. Also create the missing Wave 0 files called out by validation: `src/queries/schedule/dal/listRecruitingSchedules.test.ts`, `src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx`, and `src/mutations/application/actions/createScheduleApplication.test.ts`. Do not add worker detail reads, role-choice reads, or applicant-count reads in this plan.</action>
  <verify>
    <automated>pnpm test -- src/queries/schedule/dal/listRecruitingSchedules.test.ts src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx src/mutations/application/actions/createScheduleApplication.test.ts</automated>
  </verify>
  <done>The worker-facing read contracts and all missing Phase 2 worker/application test files exist before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the worker application mutation slice with RLS-backed writes</name>
  <files>
    src/mutations/application/dal/scheduleApplicationDal.ts
    src/mutations/application/actions/createScheduleApplication.ts
    src/mutations/application/actions/createScheduleApplication.test.ts
    src/mutations/application/actions/submitScheduleApplication.ts
    src/mutations/application/components/ApplyToScheduleButton.tsx
  </files>
  <behavior>
    - Test 1: a worker can create one schedule-level application for a recruiting schedule.
    - Test 2: duplicate submissions for the same schedule and worker are rejected cleanly.
    - Test 3: non-worker or anonymous callers are denied.
    - Test 4: the mutation never asks for or persists a chosen role in Phase 2.
  </behavior>
  <action>Implement the worker application write path using `getServerSupabaseClient()` and RLS-backed inserts, not the service-role admin client. `createScheduleApplication.ts` should authorize worker users, verify the schedule is still open to recruiting, insert one `(schedule_id, worker_user_id)` row, and translate unique-constraint conflicts into a stable already-applied outcome. `submitScheduleApplication.ts` should parse `FormData`, call the action, and revalidate `/worker/schedules`. Keep `ApplyToScheduleButton.tsx` mutation-scoped per D-20 because it is tightly coupled to one write action. Do not add role choice, application status columns, or any Phase 3 review behavior.</action>
  <verify>
    <automated>pnpm test -- src/mutations/application/actions/createScheduleApplication.test.ts</automated>
  </verify>
  <done>Workers can submit exactly one schedule-level application through an RLS-safe mutation path.</done>
</task>

<task type="auto">
  <name>Task 3: Build the lightweight worker schedules page</name>
  <files>
    src/flows/worker-schedules/components/WorkerSchedulesPage.tsx
    src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx
    src/flows/worker-schedules/components/WorkerScheduleList.tsx
    src/app/worker/schedules/page.tsx
  </files>
  <action>Create `src/app/worker/schedules/page.tsx` as a thin route that delegates to `WorkerSchedulesPage`. Implement `WorkerSchedulesPage.tsx` as a server-first flow that reads recruiting schedules and the current worker's application ids, then renders `WorkerScheduleList.tsx` with minimal information only: date, time, recruiting/open indicator, and apply/applied state. The page test should assert APPL-01 visibility and APPL-02 applied-state feedback. Keep the page lightweight: no separate detail route, no role-choice controls, and no assignment/review language.</action>
  <verify>
    <automated>pnpm test -- src/queries/schedule/dal/listRecruitingSchedules.test.ts src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx src/mutations/application/actions/createScheduleApplication.test.ts</automated>
  </verify>
  <done>Workers can open `/worker/schedules`, see a minimal recruiting list, and submit one application per schedule without crossing into deferred scope.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/queries/schedule/dal/listRecruitingSchedules.test.ts src/flows/worker-schedules/components/WorkerSchedulesPage.test.tsx src/mutations/application/actions/createScheduleApplication.test.ts`. Confirm the worker path uses `getServerSupabaseClient()` rather than `getAdminSupabaseClient()` and that the UI remains list-first and minimal.
</verification>

<success_criteria>
Workers can browse recruiting schedules and apply once per schedule through a minimal, server-first, RLS-backed flow with no role-choice or detail-screen requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/02-schedule-publishing/02-schedule-publishing-03-SUMMARY.md`
</output>
