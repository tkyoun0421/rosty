---
phase: 02-schedule-publishing
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/queries/schedule/types/scheduleList.ts
  - src/queries/schedule/dal/listAdminSchedules.ts
  - src/mutations/schedule/actions/updateScheduleStatus.ts
  - src/mutations/schedule/actions/updateScheduleStatus.test.ts
  - src/flows/admin-schedules/components/AdminSchedulesPage.tsx
  - src/flows/admin-schedules/components/ScheduleTable.tsx
  - src/flows/admin-schedules/components/ScheduleStatusForm.tsx
autonomous: true
requirements:
  - SCHD-03
must_haves:
  truths:
    - "An admin can see created schedules in the admin publishing screen."
    - "An admin can change schedule status among `recruiting`, `assigning`, and `confirmed` without a heavy workflow engine."
    - "Obvious invalid status inputs are rejected, but the system does not over-constrain operational decisions."
  artifacts:
    - path: src/queries/schedule/dal/listAdminSchedules.ts
      provides: "Admin read model for schedule management"
    - path: src/queries/schedule/types/scheduleList.ts
      provides: "Schedule list item contract shared by admin and worker reads"
    - path: src/mutations/schedule/actions/updateScheduleStatus.ts
      provides: "Admin-only status management server action"
    - path: src/flows/admin-schedules/components/ScheduleTable.tsx
      provides: "Admin list UI for existing schedules"
    - path: src/flows/admin-schedules/components/ScheduleStatusForm.tsx
      provides: "Status update control coupled to one mutation"
    - path: src/flows/admin-schedules/components/AdminSchedulesPage.tsx
      provides: "Composed admin schedule page with create plus status management"
  key_links:
    - from: src/flows/admin-schedules/components/AdminSchedulesPage.tsx
      to: src/queries/schedule/dal/listAdminSchedules.ts
      via: "server-first admin list read"
      pattern: "listAdminSchedules"
    - from: src/flows/admin-schedules/components/ScheduleStatusForm.tsx
      to: src/mutations/schedule/actions/updateScheduleStatus.ts
      via: "mutation-scoped UI for one write action per D-20"
      pattern: "updateScheduleStatus"
    - from: src/mutations/schedule/actions/updateScheduleStatus.ts
      to: src/shared/model/schedule.ts
      via: "status validation against the shared enum contract"
      pattern: "recruiting|assigning|confirmed"
---

<objective>
Finish the admin publishing surface by adding schedule reads and lightweight status management.

Purpose: satisfy SCHD-03 without turning Phase 2 into a full assignment workflow or strict lifecycle engine.
Output: admin read model, status mutation, and the admin page UI for viewing schedules and updating status.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schedule-publishing/02-CONTEXT.md
@.planning/phases/02-schedule-publishing/02-RESEARCH.md
@.planning/phases/02-schedule-publishing/02-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-schedule-publishing/02-01-PLAN.md
@CLAUDE.md
@src/flows/admin-schedules/components/AdminSchedulesPage.tsx
@src/queries/access/dal/getCurrentUser.ts
@src/shared/model/access.ts

<interfaces>
From src/queries/access/dal/getCurrentUser.ts:
```ts
export interface CurrentUser {
  id: string;
  email: string;
  role: AppRole | null;
}

export async function getCurrentUser(): Promise<CurrentUser | null>;
```

From Plan 02-01 output:
```ts
export type ScheduleStatus = "recruiting" | "assigning" | "confirmed";

export interface ScheduleRoleSlotInput {
  roleCode: string;
  headcount: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the admin schedule read contract and missing status test scaffold</name>
  <files>
    src/queries/schedule/types/scheduleList.ts
    src/queries/schedule/dal/listAdminSchedules.ts
    src/mutations/schedule/actions/updateScheduleStatus.test.ts
  </files>
  <action>Create the admin schedule list DTO and read DAL for server-first rendering. The list contract should expose only what the admin page needs in Phase 2: schedule id, starts/ends, status, and role-slot summary. Also add the missing Wave 0 test file `src/mutations/schedule/actions/updateScheduleStatus.test.ts`, because validation says it does not exist yet. Keep non-component files camelCase and keep list DTOs in `queries/schedule/types` so later worker reads can reuse the same primitives where appropriate.</action>
  <verify>
    <automated>pnpm test -- src/mutations/schedule/actions/updateScheduleStatus.test.ts</automated>
  </verify>
  <done>The admin list read contract exists and the required status-action test file is in place before status logic is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lightweight admin status management</name>
  <files>
    src/mutations/schedule/actions/updateScheduleStatus.ts
    src/mutations/schedule/actions/updateScheduleStatus.test.ts
  </files>
  <behavior>
    - Test 1: admins can set `recruiting`, `assigning`, or `confirmed` on an existing schedule.
    - Test 2: invalid enum values are rejected.
    - Test 3: non-admin callers get `FORBIDDEN`.
    - Test 4: the helper stays lightweight and does not encode a heavy state machine beyond blocking obviously invalid transitions.
  </behavior>
  <action>Implement `updateScheduleStatus.ts` as a server action under `mutations/schedule/actions`. Authorize via `getCurrentUser()`, validate against the shared schedule status contract from Plan 01, persist through the schedule write DAL, and revalidate `/admin/schedules`. Follow D-07 through D-10 exactly: reject malformed or clearly invalid transitions, but do not introduce assignment workflow side effects, reviewer states, or over-strict sequencing logic.</action>
  <verify>
    <automated>pnpm test -- src/mutations/schedule/actions/updateScheduleStatus.test.ts</automated>
  </verify>
  <done>Schedule status management exists as an admin-only server action with intentionally light transition enforcement.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the admin publishing page with list and status controls</name>
  <files>
    src/flows/admin-schedules/components/AdminSchedulesPage.tsx
    src/flows/admin-schedules/components/ScheduleTable.tsx
    src/flows/admin-schedules/components/ScheduleStatusForm.tsx
  </files>
  <action>Expand `AdminSchedulesPage.tsx` to perform a server-first read through `listAdminSchedules.ts` and render the created schedules alongside the create form from Plan 01. Add `ScheduleTable.tsx` and `ScheduleStatusForm.tsx`. Use `ScheduleStatusForm.tsx` as mutation-scoped UI tightly bound to `updateScheduleStatus.ts`, which matches D-20. Keep the admin screen intentionally utilitarian: no applicant review, no assignment UI, and no schedule detail route. The page should surface the current status and offer status changes inline.</action>
  <verify>
    <automated>pnpm test -- src/mutations/schedule/actions/createSchedule.test.ts src/mutations/schedule/actions/updateScheduleStatus.test.ts</automated>
  </verify>
  <done>The admin publishing screen supports creation plus status operations in one thin route and one composed flow.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/mutations/schedule/actions/createSchedule.test.ts src/mutations/schedule/actions/updateScheduleStatus.test.ts`. Confirm the admin page reads schedules server-side and that status controls remain lightweight rather than acting as a full workflow engine.
</verification>

<success_criteria>
Admins can view published schedules and manage `recruiting`, `assigning`, and `confirmed` states from the admin publishing page without crossing into Phase 3 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-schedule-publishing/02-schedule-publishing-02-SUMMARY.md`
</output>
