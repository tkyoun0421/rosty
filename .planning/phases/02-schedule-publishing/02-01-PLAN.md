---
phase: 02-schedule-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260401_phase2_schedule_publishing.sql
  - src/shared/model/schedule.ts
  - src/shared/model/schedule.test.ts
  - src/mutations/schedule/schemas/schedule.ts
  - src/mutations/schedule/schemas/schedule.test.ts
  - src/mutations/schedule/dal/scheduleDal.ts
  - src/mutations/schedule/actions/createSchedule.ts
  - src/mutations/schedule/actions/createSchedule.test.ts
  - src/mutations/schedule/actions/submitSchedule.ts
  - src/flows/admin-schedules/components/AdminSchedulesPage.tsx
  - src/flows/admin-schedules/components/CreateScheduleForm.tsx
  - src/app/admin/schedules/page.tsx
autonomous: true
requirements:
  - SCHD-01
  - SCHD-02
must_haves:
  truths:
    - "An admin can create one schedule that contains date, start time, end time, and multiple role-slot headcounts."
    - "New schedules default to `recruiting` immediately after creation."
    - "Schedule creation is atomic, so a schedule cannot exist without its intended role slots."
  artifacts:
    - path: supabase/migrations/20260401_phase2_schedule_publishing.sql
      provides: "Phase 2 tables, enum, constraints, indexes, RPC, and RLS for schedules, role slots, and applications"
    - path: src/shared/model/schedule.ts
      provides: "Shared primitive schedule status and role-slot contracts"
    - path: src/mutations/schedule/schemas/schedule.ts
      provides: "Zod validation for admin schedule creation input"
    - path: src/mutations/schedule/dal/scheduleDal.ts
      provides: "Write-side atomic schedule-plus-slots persistence"
    - path: src/mutations/schedule/actions/createSchedule.ts
      provides: "Admin-only server action for schedule creation"
    - path: src/flows/admin-schedules/components/AdminSchedulesPage.tsx
      provides: "Admin schedule management entry flow"
    - path: src/app/admin/schedules/page.tsx
      provides: "Thin admin schedule route"
  key_links:
    - from: src/mutations/schedule/actions/createSchedule.ts
      to: src/mutations/schedule/schemas/schedule.ts
      via: "server action parses admin form payload before persistence"
      pattern: "scheduleSchema"
    - from: src/mutations/schedule/dal/scheduleDal.ts
      to: supabase/migrations/20260401_phase2_schedule_publishing.sql
      via: "RPC or equivalent single-transaction create path against the three-table model"
      pattern: "create_schedule_with_slots|schedule_role_slots|schedule_status"
    - from: src/app/admin/schedules/page.tsx
      to: src/flows/admin-schedules/components/AdminSchedulesPage.tsx
      via: "one route maps to one flow per D-16"
      pattern: "AdminSchedulesPage"
---

<objective>
Create the persistence and admin create path for schedule publishing using the narrow Phase 2 model.

Purpose: satisfy schedule creation and role-slot headcount setup without leaking assignment workflow or rich detail scope into Phase 2.
Output: Phase 2 migration, shared schedule primitives, validated create action, and an admin route/flow for creating schedules.
</objective>

<execution_context>
@C:/code/rosty/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/rosty/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schedule-publishing/02-CONTEXT.md
@.planning/phases/02-schedule-publishing/02-RESEARCH.md
@.planning/phases/02-schedule-publishing/02-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-access-foundation/01-access-foundation-03-SUMMARY.md
@.planning/phases/01-access-foundation/01-access-foundation-04-SUMMARY.md
@CLAUDE.md
@src/app/admin/worker-rates/page.tsx
@src/flows/admin-worker-rates/components/AdminWorkerRatesPage.tsx
@src/mutations/worker-rate/actions/upsertWorkerRate.ts
@src/queries/access/dal/getCurrentUser.ts
@src/shared/lib/supabase/adminClient.ts

<interfaces>
From src/queries/access/dal/getCurrentUser.ts:
```ts
export interface CurrentUser {
  id: string;
  email: string;
  role: AppRole | null;
}

export async function getCurrentUser(): Promise<CurrentUser | null>;
```

From src/shared/lib/supabase/adminClient.ts:
```ts
export function getAdminSupabaseClient();
```

From src/app/admin/worker-rates/page.tsx:
```ts
export default async function AdminWorkerRatesRoute();
```

From src/flows/admin-worker-rates/components/AdminWorkerRatesPage.tsx:
```ts
export async function AdminWorkerRatesPage();
```

From src/mutations/worker-rate/actions/upsertWorkerRate.ts:
```ts
export async function upsertWorkerRate(input: {
  userId: string;
  hourlyRateCents: number;
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 2 database contract and test scaffolding</name>
  <files>
    supabase/migrations/20260401_phase2_schedule_publishing.sql
    src/shared/model/schedule.ts
    src/shared/model/schedule.test.ts
    src/mutations/schedule/schemas/schedule.ts
    src/mutations/schedule/schemas/schedule.test.ts
    src/mutations/schedule/actions/createSchedule.test.ts
  </files>
  <action>Per D-01 through D-10 and the research recommendation, add a single Phase 2 migration that introduces exactly the minimal three-table model: `schedules`, `schedule_role_slots`, and `schedule_applications`, plus `public.schedule_status` with `recruiting`, `assigning`, and `confirmed`. Put invariants in Postgres: `ends_at > starts_at`, positive slot headcount, unique `(schedule_id, role_code)`, unique `(schedule_id, worker_user_id)`, and indexes for recruiting reads and application lookups. Add the atomic schedule creation function or RPC entry the write DAL will use. Enable RLS so worker reads/writes use session-bound safety and admin writes can remain admin-gated. Create `src/shared/model/schedule.ts` for shared primitives such as `ScheduleStatus` and role-slot DTOs. Add Wave 0 scaffolding tests for `src/shared/model/schedule.test.ts`, `src/mutations/schedule/schemas/schedule.test.ts`, and `src/mutations/schedule/actions/createSchedule.test.ts`, because validation marks these files as missing.</action>
  <verify>
    <automated>pnpm test -- src/shared/model/schedule.test.ts src/mutations/schedule/schemas/schedule.test.ts src/mutations/schedule/actions/createSchedule.test.ts</automated>
  </verify>
  <done>The repo has the exact Phase 2 data contract and colocated test files needed before implementation proceeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the admin schedule creation mutation slice</name>
  <files>
    src/mutations/schedule/schemas/schedule.ts
    src/mutations/schedule/schemas/schedule.test.ts
    src/mutations/schedule/dal/scheduleDal.ts
    src/mutations/schedule/actions/createSchedule.ts
    src/mutations/schedule/actions/createSchedule.test.ts
    src/mutations/schedule/actions/submitSchedule.ts
  </files>
  <behavior>
    - Test 1: valid admin input with multiple role slots is accepted and normalized into a create payload.
    - Test 2: schedules with end time before start time or non-positive slot counts are rejected.
    - Test 3: non-admin callers get `FORBIDDEN`.
    - Test 4: persisted schedules come back with default status `recruiting`.
  </behavior>
  <action>Create the Zod schema, write DAL, and server actions for admin schedule creation. Keep server actions under `mutations/schedule/actions` per D-19, and keep write DAL under `mutations/schedule/dal` per D-21. Use `getCurrentUser()` to authorize admins. Persist schedules and role slots atomically through the DB function added in Task 1 instead of sequential inserts. `submitSchedule.ts` should parse `FormData`, call `createSchedule.ts`, and revalidate `/admin/schedules`. Do not add edit-after-create or role assignment logic; this plan is only the Phase 2 create path for SCHD-01 and SCHD-02.</action>
  <verify>
    <automated>pnpm test -- src/mutations/schedule/schemas/schedule.test.ts src/mutations/schedule/actions/createSchedule.test.ts</automated>
  </verify>
  <done>Admin schedule creation is validated, admin-only, atomic, and aligned with the narrow three-table model.</done>
</task>

<task type="auto">
  <name>Task 3: Add the thin admin route and create-focused flow shell</name>
  <files>
    src/flows/admin-schedules/components/AdminSchedulesPage.tsx
    src/flows/admin-schedules/components/CreateScheduleForm.tsx
    src/app/admin/schedules/page.tsx
  </files>
  <action>Create the first admin schedule management route following D-14 through D-20: `src/app/admin/schedules/page.tsx` stays thin and delegates to `AdminSchedulesPage`. Build `CreateScheduleForm.tsx` as the form UI owned by the admin schedule flow, unless a tiny control is better placed in `mutations/schedule/components`; keep route composition in `flows`. The form must capture date, start time, end time, and repeatable role-slot headcounts, but should stay intentionally simple. Do not add a separate detail screen or any Phase 3 review UI. This task may render placeholder list/status areas that Plan 02 will complete.</action>
  <verify>
    <automated>pnpm test -- src/mutations/schedule/actions/createSchedule.test.ts</automated>
  </verify>
  <done>The admin schedule route exists, follows the route-to-flow contract, and exposes the create form needed to use the new mutation path.</done>
</task>

</tasks>

<verification>
Run `pnpm test -- src/shared/model/schedule.test.ts src/mutations/schedule/schemas/schedule.test.ts src/mutations/schedule/actions/createSchedule.test.ts`. Confirm the migration contains only the minimal three-table schedule model plus supporting enum/RLS/indexes, and that `/admin/schedules` is wired through one thin route and one flow.
</verification>

<success_criteria>
Admins can create a schedule with multiple role-slot headcounts, new schedules default to `recruiting`, and the data model is atomic and Phase-2-narrow.
</success_criteria>

<output>
After completion, create `.planning/phases/02-schedule-publishing/02-schedule-publishing-01-SUMMARY.md`
</output>
